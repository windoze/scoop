// EXPECT: pass
// 目标：T0712 单态化（monomorphization）：`id(1)` 与 `id("s")` 生成两个实例。
// 备注：当前后端仍以“最小 main（ret 0）”为主，因此该 fixture 目前只作为：
// - 前端检查（parse/resolve/typecheck）能通过的回归；
// - `scoop run`/run-pass phase 的 smoke（能编译 + 能执行）。

package fixtures.codegen

import scoop.core.*

fun id<T>(x: T): T {
    return x
}

fun f() {
    val a = id(1)
    val b = id("s")
}

// 说明：run-pass phase 会通过 `scoop run` 真正执行该文件，因此需要提供入口 `main`。
// 当前阶段（T0802/T0808）后端仍只 codegen `main` 的最小子集，因此这里让它返回 0 即可。
fun main(): Int {
    return 0
}
