package sample

import scoop.core.*

effect Boom {
    fun next(): Int
}

fun helper(k: Continuation<Int, Unit, eff Boom>): Unit / (Raise<RuntimeError> + Boom) {
    k.resume(1)
}

fun main(): Int {
    return 0
}
