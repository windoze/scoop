// EXPECT: pass
// 目标：T0319：当同名多个顶层函数同时可见时，resolve 阶段不提前报歧义，而是保留候选集合交给后续 typecheck 决议。

package fixtures.resolve_multi.ambiguous_call.use

import fixtures.resolve_multi.ambiguous_call.p1.foo
import fixtures.resolve_multi.ambiguous_call.p2.foo

fun test() {
    foo()
}
