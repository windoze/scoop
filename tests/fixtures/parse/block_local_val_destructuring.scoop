// EXPECT: pass
// EXPECT-AST: block_local_val_destructuring.ast
// 目标：验证函数体 block 内的 `val` 支持 tuple/struct 解构绑定（T0244）。

package fixtures.parse

fun f() {
    val (a, b) = pair
    val Point { x, y } = p
    a
}

