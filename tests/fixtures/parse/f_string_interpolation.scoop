// EXPECT: pass
// EXPECT-AST: f_string_interpolation.ast
// 目标：验证 f-string `${...}` interpolation、literal braces 与 `$x` text（spec §8.2）。

package fixtures.parse

val s = f"hi ${name}!"
val raw = f"""raw ${name}""".trimIndent()
val json = f"""{"name": "${name}", "shape": {}}"""
val nested = f"${if (true) { 1 } else { 2 }}"
val char_brace = f"${'}'}"
val no_shorthand = f"$name stays literal"
val old_braces = f"{name} stays literal"

fun main() {}
