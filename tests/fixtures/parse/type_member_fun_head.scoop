// EXPECT: pass
// EXPECT-AST: type_member_fun_head.ast
// 目标：验证 type body 内的 `fun` 成员声明头会进入 AST（函数体仍仅保留 span）。

package fixtures.parse

class C {
    fun f(x: Int): Int { x }
    fun g(): Int
}

fun main() {}

