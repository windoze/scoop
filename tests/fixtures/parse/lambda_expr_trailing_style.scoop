// EXPECT: pass
// EXPECT-AST: lambda_expr_trailing_style.ast
// 目标：验证 Kotlin 风格 trailing lambda 能作为“最后一个调用实参”绑定到 call 上（Appendix B.5.4）。
// 覆盖形态：
// - 无括号参数列表：`callee { ... }`
// - 有括号参数列表：`callee(args...) { ... }`

package fixtures.parse

val y = list.map { it }
val z = f(1) { x -> x }

fun main() {}
