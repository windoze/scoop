// EXPECT: fail
// EXPECT-ERROR: 顶层声明
// EXPECT-ERROR-CODE: scoop::parse::expected
// EXPECT-ERROR-AT: 9:1
// 目标：package-level `comptime if` surface 已删除；顶层 `comptime` 只走普通 parse 失败路径。

package fixtures.parse

comptime if (cond1) {
    fun foo() {}
    class Bar {}
} else if (cond2) {
    fun foo() {}
    class Bar {}
} else {
    fun foo() {}
    class Bar {}
}

fun after() {}
