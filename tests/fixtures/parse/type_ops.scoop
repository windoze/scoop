// EXPECT: pass
// EXPECT-AST: type_ops.ast
// 目标：验证 `is`/`!is`/`as`/`as?` 的表达式解析与 span 覆盖。

package fixtures.parse

val a = x is Foo
val b = x !is Foo
val c = x as Foo
val d = x as? Foo

fun main() {
    // `if` 已实现为表达式（T0214）：这里验证 `is` 作为 if 条件时仍能正确解析。
    if (x is Foo) { x }
}
