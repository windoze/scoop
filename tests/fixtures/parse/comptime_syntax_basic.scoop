// EXPECT: pass
// 目标：statement-level `comptime` surface 已删除；`comptime` 不再是 parser keyword，
// 因此这里按普通 identifier + trailing closure 形态解析，后续阶段自然处理语义。

package fixtures.parse

fun add(a: Int, b: Int): Int {
    return a + b
}

fun f(value: T) {
    comptime {
        val y = 1
    }
}
