// EXPECT: fail
// EXPECT-ERROR: 语句
// EXPECT-ERROR-CODE: scoop::parse::expected
// EXPECT-ERROR-AT: 14:5
// 目标：statement-level `comptime` surface 已删除，block 内 `comptime` 只走普通 parse 失败路径。

package fixtures.parse

const fun add(a: Int, b: Int): Int {
    return a + b
}

fun f(value: T) {
    comptime {
        val y = 1
    }
}
