// EXPECT: pass
//
// T4015R：带 cone 边界的 compilation-unit trim 也必须复用 typechecked binding，
// 让 package-level `comptime if` 条件中的 imported generic `const fun` 正常通过。

package fixtures.typecheck_cone.package_level_comptime_if_cross_cone_const_fun.app

import scoop.core.*
import fixtures.typecheck_cone.package_level_comptime_if_cross_cone_const_fun.lib.*

comptime if (truthy<Int>(1)) {
    fun selected(): Int {
        return 7
    }
}

fun useSelected(): Int {
    return selected()
}
