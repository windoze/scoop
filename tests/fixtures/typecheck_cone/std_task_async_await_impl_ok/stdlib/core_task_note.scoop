// EXPECT: pass
// 占位文件：保留一个非空 stdlib cone，确保该 fixture 继续覆盖“多 cone 输入下不依赖 `scoop.task`”。

package fixtures.typecheck_cone.std_task_async_await_impl_ok.stdlib

import scoop.core.*

fun taskCoreIdentity(task: Task<Int>): Task<Int> {
    return task
}
