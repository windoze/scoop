// EXPECT: ok
// UMB-FIX-PHASE: comptime
// SPEC: docs/spec/language_spec-part5.md#6-静态反射-intrinsic
// COVERS: NONE
// BUCKETS: B-24
// REASON: P7-C1 implements reflection/comptime intrinsic lowering; retired IDs are covered by audit/UMB_retired.csv.
// SOURCE-FIXTURE: tests/fixtures/comptime/reflection_intrinsics_v0_basic.scoop

package fixtures.comptime_

import scoop.core.*

struct Point(val x: Int, val y: Int) {
    val tag: String
    // 计算属性：v0 的 fieldsOf 不应把它当作字段。
    val computed: Int get() = 0
}

const val N: String = nameOf<Point>()
const val S: Int = sizeOf<Int64>()
const val F: ComptimeList<FieldMeta> = fieldsOf<Point>()
const val F0N: String = fieldsOf<Point>()._0.name
const val F0T: String = fieldsOf<Point>()._0.type.name
