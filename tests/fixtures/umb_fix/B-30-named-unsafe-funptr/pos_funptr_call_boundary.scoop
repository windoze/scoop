// EXPECT: ok
// SPEC: docs/spec/language_spec-part6.md#7-function-pointer
// COVERS: NONE
// BUCKETS: B-30
// REASON: P7-B3.5 retired B-30 rows; coverage is owned by the retired ledger.
// SOURCE-FIXTURE: tests/fixtures/run-pass/unsafe_funptr_extern_call_basic.scoop

import scoop.core.*
import scoop.unsafe.*

@Extern("scoop_test_get_add_int_funptr")
fun get_add_int_funptr(): FunPtr<(Int, Int) -> Int>

fun main() {
    @Unsafe do {
        val fp: FunPtr<(Int, Int) -> Int> = get_add_int_funptr()
        println(fp(1, 2))
        println(fp.invoke(1, 2))
    }
}
