# B-03 call abi

- Category: ../../../../audit/UMB_categories/B-03.md
- Strategy: ../../../../audit/strategies/B-03.md
- Spec matrix rows: ../../../../audit/spec_coverage_matrix.md entries containing `B-03`
- Fixture index: ../_index.csv rows with `bucket=B-03`

## Scope

U5-T01 creates this directory skeleton only. U5-T02 and U5-T03 must add `.scoop` fixtures and append matching `_index.csv` rows.

## Runner Status

The Python fixture runner (`python3 tools/run_fixtures.py`) recognizes `tests/fixtures/umb_fix/**`. All B-03 fixtures are active after P8 and must remain runnable.
