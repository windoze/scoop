// EXPECT: pass
// ARGS: --emit-llvm
// BUILD-LLVM-CONTAINS: @__scoop_str_lit_
// BUILD-LLVM-NOT-CONTAINS: scoop_alloc_typed
// P0-T03/P5-T02 metric fixture: keep this source limited to String literal and Platform reads.

package fixtures.umb_fix.p0_t03_gc_metrics

import scoop.core.*

fun stringLiteralMetric(): String {
    return "metric_literal"
}

fun platformMetric(): Platform {
    return getPlatform()
}

fun main(): Int {
    val s: String = stringLiteralMetric()
    val p: Platform = platformMetric()
    return s.length() + p.os.length()
}
