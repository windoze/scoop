// EXPECT: pass
// RUN-STDOUT: pos_array_composite_transport.stdout
// SPEC: docs/spec/language_spec-part3.md#12-数组字面量
// COVERS: NONE
// BUCKETS: B-13
// REASON: P7-C3 retired B-13 rows; runtime fixture keeps array composite transport codegen covered.
// SOURCE-FIXTURE: tests/fixtures/run-pass/array_lit_infer_string_char_float_basic.scoop

import scoop.core.*

fun main(): Int {
    val names = ["a", "b", "c"]
    println(names.size())
    println(names.get(1))

    val chars = ['a', 'b', 'c']
    println(chars.size())
    val last: Char = chars.get(2)
    println(last.toInt())

    val nums = [1.0, 2.0, 3.0]
    println(nums.size())
    println(nums.get(0).toInt())
    println(nums.get(2).toInt())

    return 0
}
