// EXPECT: pass
// 目标：T0629b：同一个 consumer cone 内可以同时存在 `main` 与多个导出入口；导出入口按 entry point 规则强制 `Pure!`。

package fixtures.typecheck_cone_archive.program_boundary_export_entry_points.app

import scoop.core.*

public fun main() / Pure! {
    ()
}

