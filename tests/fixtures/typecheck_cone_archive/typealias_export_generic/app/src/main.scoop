// EXPECT: pass
// 目标：T1302：consumer 通过读取依赖 `.cone` 的 `api.scoopir`，能展开并实例化依赖包导出的 typealias（含泛型）。

package fixtures.typecheck_cone_archive.typealias_export_generic.app

import scoop.core.*
import fixtures.typecheck_cone_archive.typealias_export_generic.lib.*

public fun use(h: Handler<UserId>) / Pure! {
}

public fun main() / Pure! {
    val id: UserId = 1
}

