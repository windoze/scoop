// 目标：P6-T05 build matrix 固定较高优化级别样本；优化只允许改变 P5 post-opt / LLVM pass 结果，不能换 stage。
// ARGS: --emit-asm -O2

package fixtures.build

effect Ping {
    fun hit(): Int
}

fun main(): Int {
    return handle {
        Ping.hit()
    } with {
        Ping.hit() -> 11
    }
}
