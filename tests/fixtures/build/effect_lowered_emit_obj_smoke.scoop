// 目标：P6-T05 build matrix 证明 `--emit-obj` 也走同一 LLVM stage/helper。
// ARGS: --emit-obj --opt-level 0

package fixtures.build

effect Ping {
    fun hit(): Int
}

fun main(): Int {
    return handle {
        Ping.hit()
    } with {
        Ping.hit() -> 7
    }
}
