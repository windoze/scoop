// EXPECT: pass
// P4-T01d：`@Intrinsic("dummy_runtime")` 应走 RuntimeCall entry，而不是生成对 declaration-only
// Scoop 函数 body 的调用。
// SYSROOT-DEPS: scoop.runtime.test

package fixtures.runpass

import scoop.core.*

fun main(): Int {
    val first = bar()
    val second = bar()
    if (first != 701) {
        return 1
    }
    if (second != 702) {
        return 2
    }
    return 0
}
