// EXPECT: pass
// RUN-STDOUT: interface_default_method_dispatch_basic.stdout
//
// 回归：interface default method dispatch 继续消费已选中的 default-method slot contract。

import scoop.core.*

interface Ping {
    fun ping(): Int {
        return 7
    }
}

class Box() : Ping

fun read(x: Ping): Int {
    return x.ping()
}

fun main(): Int {
    println(read(Box()))
    return 0
}
