// EXPECT: pass
// RUN-MODE: dump-stackmaps
// RUN-STDOUT-CONTAINS: records:
// RUN-STDOUT-CONTAINS: verify-roots: ok
// RUN-STDOUT-CONTAINS: root-slots:
// RUN-STACKMAPS-RECORDS-GT: 0
//
// 目标：T1503b statepoint pipeline：stackmap records 必须来自 statepoints（以 `scoop_alloc` 为起步）。

import scoop.core.*

fun main(): Int {
    // 强制触发 `Int -> Any` 装箱（heap alloc），确保有 alloc safepoint 可被重写为 statepoint。
    val a: Any = 1
    return 0
}
