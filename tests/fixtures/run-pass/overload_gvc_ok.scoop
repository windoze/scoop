// EXPECT: pass
// EXPECT-EXIT: 110
// P0-T02 baseline from OVERLOAD_RESOLUTION.md 1.3.
// Current bug: typecheck reports ambiguous_overload.
// Expected: the concrete h(Int) overload wins by specificity.

package overload_gvc_ok

import scoop.core.*

fun h<T>(x: T): T {
    return x
}

fun h(x: Int): Int {
    return x + 100
}

fun main(): Int {
    return h(10)
}
