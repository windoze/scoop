// EXPECT: pass
// RUN-STDOUT: name_of_runtime_basic.stdout
//
// 回归：`nameOf<T>()` 继续通过 typed intrinsic contract 降到运行期字符串值。

import scoop.core.*

struct Point(val x: Int)

fun main(): Int {
    val name: String = nameOf<Point>()
    println(name != "")
    return 0
}
