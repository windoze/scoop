package fixtures.run_pass_cone.top_level_val_pattern_multi_file_basic

import scoop.core.*

fun seedTotal(total: Int): Int {
    println("seed-total")
    return total + 5
}

val total: Int = seedTotal(pointTotal)
