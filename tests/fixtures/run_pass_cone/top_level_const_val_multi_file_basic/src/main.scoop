// EXPECT: pass
// RUN-STDOUT: out.stdout.txt
// 目标：非入口文件中的顶层 `const val` 在 cone 多文件 codegen 中可被稳定引用。

package fixtures.run_pass_cone.top_level_const_val_multi_file_basic

import scoop.core.*

const val ENTRY_CHAIN: Int = helperChain() + 1
const val ENTRY_TEXT: String = helperText().concat("-entry")

fun main() {
    println(helperBase())
    println(helperChain())
    println(ENTRY_CHAIN)
    println(ENTRY_TEXT)
}
