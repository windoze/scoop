// EXPECT: pass
// 目标：source-only cone manifest 的 `[entry-points].exports` 支持多个闭合 Pure 导出入口。

package fixtures.run_pass_cone.export_entry_points_pass

import scoop.core.*

public fun export_ok() / Pure! {
    ()
}

public fun export_ok2() / Pure! {
    ()
}

public fun main() / Pure! {
    ()
}
