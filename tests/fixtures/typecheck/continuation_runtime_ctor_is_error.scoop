// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::continuation_not_constructible
// EXPECT-ERROR: compiler-owned interface
// EXPECT-ERROR-AT: 12:14
// 目标：P2-T03 `Continuation` 只能由编译器/runtime 在 handler 边界物化；用户代码不能直接构造它。

package fixtures.typecheck

import scoop.core.*

fun bad(): Unit {
    val _k = Continuation<Int, Unit, eff Pure>()
}
