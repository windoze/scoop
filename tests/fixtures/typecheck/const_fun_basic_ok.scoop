// EXPECT: pass
// 目标：T1201：`const fun` 在 typecheck 阶段可通过（最小规则：仅做标记与声明级门禁）。

package fixtures.typecheck

import scoop.core.*

const fun add(a: Int, b: Int): Int / Pure {
    return a + b
}

fun main(): Int {
    return add(1, 2)
}
