// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::structured_concurrency_deferred
// EXPECT-ERROR: structured concurrency 语法 `spawn`
// EXPECT-ERROR-AT: 12:27
// 目标：T4009c：公开 `spawn` surface 现已明确留待后续 structured concurrency，不再属于当前 `Task` core。

package fixtures.typecheck

import scoop.core.*

fun main(): Int {
    val task: Task<Int> = spawn { 1 }
    return 0
}
