// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::ambiguous_interface_member_impl
// EXPECT-ERROR-AT: 16:13
//
// 目标：T1508c interface dispatch（itable）v0：当实现方存在“按最小形状无法唯一匹配”的候选时应报错，
// 避免 itable slot（name + params_len）映射不稳定。

package fixtures.typecheck

import scoop.core.*

interface I {
    fun ping(x: Int): Int
}

class C() : I {
    fun ping(x: Int): Int { return 1 }
    fun ping(x: String): Int { return 2 }
}
