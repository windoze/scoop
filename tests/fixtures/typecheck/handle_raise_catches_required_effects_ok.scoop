// EXPECT: pass
// 目标：T0606：`handle` 捕获 `Raise.raise` 后，body 内的 `Raise<T>` 不应成为外层函数的 required effects。

package fixtures.typecheck

import scoop.core.*

fun f(): Int / Pure {
    val x = handle {
        Raise.raise(1)
    } with {
        Raise.raise(e) -> e
    }
    x
}

