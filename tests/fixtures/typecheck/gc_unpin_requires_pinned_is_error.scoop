// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::gc_unpin_requires_ref
// EXPECT-ERROR-AT: 13:18
// EXPECT-ERROR: 参数必须是 `Pinned`
// 目标：P6-T01 固定 `GC.unpin` 只接受 `Pinned` token。

package fixtures.typecheck

import scoop.core.*

fun main(): Unit {
    @Unsafe do {
        GC.unpin(1)
    }
}
