// EXPECT: pass
// 目标：T1205 splice 字段访问（`value.[field]`）最小实现：
// - 当 `field` 为字符串字面量时，typecheck 将其视为 `value.<field>` 并推导字段类型；
// - 当 `field` 为包含字面量 `name` 的 FieldMeta 描述符时，同样生成静态字段 contract。

package fixtures.typecheck

import scoop.core.*

struct Point(val x: Int, val y: Int)

fun get_x(p: Point): Int {
    return p.["x"]
}

fun get_by_meta(p: Point): Int {
    return p.[FieldMeta { name: "y" }]
}
