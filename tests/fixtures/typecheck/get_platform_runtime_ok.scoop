// EXPECT: pass
// 回归：`getPlatform()` 可在运行期语境直接调用，并返回 `Platform` 值类型。

package fixtures.typecheck

import scoop.core.*

fun runtimeUse(): String {
    val p: Platform = getPlatform()
    return p.os
}

val a: String = runtimeUse()
