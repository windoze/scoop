// EXPECT: pass
// 目标：T0623/T4009c：`async fun foo(): T` 的调用点类型应为 `Task<T>`，
//      并且函数体内允许直接使用 `await` 与嵌套 `async { ... }`。

package fixtures.typecheck

import scoop.core.*

async fun fetch(): Int {
    val t: Task<Int> = async { 40 }
    val x: Int = await t
    return x + 2
}

fun main(): Int {
    val task: Task<Int> = fetch()
    val value: Task<Int> = async { await task }
    val same: Task<Int> = value
    val same2: Task<Int> = task
    return 0
}
