// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::val_variant_pat_refutable_not_allowed
// EXPECT-ERROR-AT: 16:9
// EXPECT-ERROR: `val` 解构绑定只允许不可失败 pattern
// 目标：variant pattern 在 `val` 解构绑定中一律禁止，不再暴露更细的 variant 名诊断。

package fixtures.typecheck

import scoop.core.*

enum Result {
    Ok(val value: Int),
}

fun f(r: Result) {
    val Result.Foo(x) = r
}
