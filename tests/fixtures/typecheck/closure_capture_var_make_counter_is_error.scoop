// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::closure_var_capture_not_allowed
// EXPECT-ERROR-AT: 14:9
// EXPECT-ERROR: RefCell<T> 共享可变状态、`val snapshot = ...` 获取只读快照，或用 fold / higher-order operators
// 目标：P3-T05 makeCounter 风格 closure 捕获外层 var 必须在前端报错，并提示显式替代方案。

package fixtures.typecheck

import scoop.core.*

fun makeCounter(): () -> Int {
    var current: Int = 0
    return {
        current = current + 1
        current
    }
}
