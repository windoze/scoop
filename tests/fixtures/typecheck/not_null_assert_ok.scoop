// EXPECT: pass
// 目标：T0421a `!!` not-null assertion 的类型规则（`Option<T>` → `T`）。

package fixtures.typecheck

import scoop.core.*

fun unwrap(x: Int?): Int / Raise<RuntimeError> {
    val y: Int = x!!
    return y
}
