// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::handle_arm_return_type_mismatch
// EXPECT-ERROR: handler arm 的返回类型不匹配
//
// T3006（已恢复）：handler arm 的返回类型必须与 `handle` 表达式的结果类型一致。
// 原始意图（T0606）：typecheck pipeline 对此 case 报告 handle_arm_return_type_mismatch。

package fixtures.typecheck

import scoop.core.*

fun f(): Int / Pure {
    val x = handle {
        Raise.raise(1)
        1
    } on {
        Raise.raise(e: Int) -> "oops"
    }
    x
}
