// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::handle_arm_return_type_mismatch
// EXPECT-ERROR: handler arm 的返回类型不匹配
// EXPECT-ERROR-AT: 16:32
// 目标：T0606：handler arm 的返回类型必须与 `handle` 表达式的结果类型一致。

package fixtures.typecheck

import scoop.core.*

fun f(): Int / Pure {
    val x = handle {
        Raise.raise(1)
        1
    } with {
        Raise.raise(e: Int) -> "oops"
    }
    x
}
