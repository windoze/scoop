// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::const_fun_call_forbidden
// EXPECT-ERROR: const fun 只能调用 const fun/编译器 intrinsic
// EXPECT-ERROR-AT: 14:12
// 目标：T1211：`const fun` 禁止调用非 `const fun`（即使对方是 Pure）。

package fixtures.typecheck

import scoop.core.*

fun add(a: Int, b: Int): Int { return a + b }

const fun bad(): Int {
    return add(1, 2)
}
