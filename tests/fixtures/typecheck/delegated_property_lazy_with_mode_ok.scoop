// IGNORE-UNTIL-FIX: owner-eff/标准委托端到端未完成，Fact 重构批 4（T4-03/T4-04）恢复
// EXPECT: pass
// 目标：T1217 标准 delegates 表面：`lazy(mode)` 可用于 delegated property。

package fixtures.typecheck

import scoop.core.*
import scoop.delegates.*

class Example {
    val config: String by lazy(LazyThreadSafetyMode.None) { "" }
}

