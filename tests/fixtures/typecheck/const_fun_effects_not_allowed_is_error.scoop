// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::const_fun_effects_not_allowed
// EXPECT-ERROR: const fun 不允许声明非 Pure 的 effect row
// EXPECT-ERROR-AT: 13:24
// 目标：T4015c：`const fun` 的声明级 effect contract 已定稿；
// - 只允许省略 effect row，或显式写 `/ Pure` / `/ Pure!`
// - non-`Pure` effect row 必须在 header phase 直接报错

package fixtures.typecheck

import scoop.core.*

const fun bad(): Int / Raise<Int> { return 0 }
