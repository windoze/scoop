// EXPECT: pass
// 目标：`val` 解构绑定允许 refutable variant pattern，并正确注入 payload binder 类型。

package fixtures.typecheck

import scoop.core.*

enum Result {
    Ok(val value: Int),
    Err(val error: String),
}

fun use(opt: Option<Int>) {
    val Some(x) = opt
    val ix: Int = x

    val r: Result = Ok(1)
    val Result.Ok(v) = r
    val iv: Int = v
}
