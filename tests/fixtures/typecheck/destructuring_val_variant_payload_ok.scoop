// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::val_variant_pat_refutable_not_allowed
// 目标：`val` 解构绑定只允许不可失败 pattern；variant pattern 必须改用 `when`。

package fixtures.typecheck

import scoop.core.*

enum Result {
    Ok(val value: Int),
    Err(val error: String),
}

fun use(opt: Option<Int>) {
    val Some(x) = opt
    val ix: Int = x

    val r: Result = Ok(1)
    val Result.Ok(v) = r
    val iv: Int = v
}
