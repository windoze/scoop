package fixtures.typecheck

import scoop.core.*

interface NativeIface {
    fun ping(): Int
}

@Intrinsic
struct NativeStruct() : NativeIface {
    @Extern("native_ping")
    override fun ping(): Int
}
