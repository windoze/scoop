// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::gc_handle_drop_requires_handle
// EXPECT-ERROR-AT: 13:23
// EXPECT-ERROR: 参数必须是 `GcHandle`
// 目标：P6-T01 固定 `GC.handleDrop` 只接受稳定 handle token。

package fixtures.typecheck

import scoop.core.*

fun main(): Unit {
    @Unsafe do {
        GC.handleDrop(1)
    }
}
