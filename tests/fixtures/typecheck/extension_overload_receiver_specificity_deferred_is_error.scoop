// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::ambiguous_overload
// EXPECT-ERROR: Any.tag()
// EXPECT-ERROR: String.tag()
// 目标：P5-T01R Phase A-C 不提前做 receiver specificity；`Any.tag` / `String.tag` 同时 applicable 时先报 ambiguity。

package fixtures.typecheck

import scoop.core.*

fun Any.tag(): Any {
    return this
}

fun String.tag(): String {
    return this
}

val s: String
val x: String = s.tag()
