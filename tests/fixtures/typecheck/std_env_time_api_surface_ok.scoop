// EXPECT: pass
//
// 目标：T1318a `scoop.env` / `scoop.time` 的最小 API 表面可被解析、解析绑定并通过类型检查。

package fixtures.typecheck

import scoop.core.*
import scoop.env.*
import scoop.time.*

fun main() {
    val v: String? = getOrNull("SCOOP_TEST_ENV_KEY")
    val t: Int = nowUnixMillis()
}
