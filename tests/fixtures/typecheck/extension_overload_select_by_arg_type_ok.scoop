// EXPECT: pass
// 目标：P5-T01 Phase A-C：参数类型使同名 extension overload 只剩一个 applicable candidate。

package fixtures.typecheck

import scoop.core.*

fun Any.pick(x: Int): Int {
    return x
}

fun Any.pick(x: String): String {
    return x
}

val a: Any
val x: Int = a.pick(1)
val y: String = a.pick("hi")
