// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::closure_var_capture_not_allowed
// EXPECT-ERROR-AT: 15:13
// EXPECT-ERROR: RefCell<T> 共享可变状态、`val snapshot = ...` 获取只读快照，或用 fold / higher-order operators
// 目标：P3-T05R 确认嵌套 closure 捕获外层 closure 内的局部 var 也必须报错。

package fixtures.typecheck

import scoop.core.*

fun main(): Int {
    val outer: () -> Int = {
        var current: Int = 0
        val inner: () -> Int = {
            current = current + 1
            current
        }
        inner()
    }
    return outer()
}
