// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::with_update_field_type_mismatch
// EXPECT-ERROR: 更新值类型不匹配
// EXPECT-ERROR-AT: 14:32
// 目标：T0415 `with` 更新字段类型不匹配应报错并指向 value。

package fixtures.typecheck

import scoop.core.*

struct Point(val x: Int)

fun use(p: Point) {
    val q: Point = p with { x: "oops" }
}

