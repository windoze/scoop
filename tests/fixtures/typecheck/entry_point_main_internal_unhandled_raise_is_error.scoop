// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::required_effect_not_declared
// EXPECT-ERROR: 缺少效果声明
// EXPECT-ERROR-AT: 12:5
// 目标：T0610：entry point（`main`）是 program boundary，必须是 Pure；即使是 internal 也不能推断出 Raise。

package fixtures.typecheck

import scoop.core.*

internal fun main() {
    Raise.raise(1);
}
