// EXPECT: pass
// 目标：T4013 `@Inline` 成为唯一的内联提示 surface，并保持为纯 compile-time marker。

package fixtures.typecheck

@Inline
fun fastPath(x: Int): Int {
    return x
}

fun f(): Int {
    return fastPath(1)
}
