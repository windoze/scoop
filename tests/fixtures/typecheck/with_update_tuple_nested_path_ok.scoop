// EXPECT: pass
// 目标：T4010a1 `with` 支持 tuple 与 struct+tuple 混合嵌套路径。

package fixtures.typecheck

import scoop.core.*

struct Point(val x: Int, val y: Int)

fun use(pair: (Point, (Int, Int))) {
    val updated: (Point, (Int, Int)) = pair with { 0.x: 1, 1.0: 3 }
}
