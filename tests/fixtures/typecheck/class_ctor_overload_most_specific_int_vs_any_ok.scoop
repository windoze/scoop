// EXPECT: pass
// 目标：T1321 Kotlin 风格重载决议：class ctor overload 在多匹配时选择 most-specific（Int 优先于 Any）。

package fixtures.typecheck

import scoop.core.*

class C() {
    constructor(x: Any) : this() {}
    constructor(x: Int) : this() {}
}

val c: C = C(1)

