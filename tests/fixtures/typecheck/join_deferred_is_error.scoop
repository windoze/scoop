// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::parse::structured_concurrency_deferred
// EXPECT-ERROR: structured concurrency 语法 `join`
// EXPECT-ERROR: 不能进入 HIR
// EXPECT-ERROR-AT: 14:22
// 目标：T4009c：公开 `join` surface 现已明确留待后续 structured concurrency，不再属于当前 `Task` core。

package fixtures.typecheck

import scoop.core.*

fun main(): Int {
    val task: Task<Int> = async { 1 }
    val value: Int = join task
    return 0
}
