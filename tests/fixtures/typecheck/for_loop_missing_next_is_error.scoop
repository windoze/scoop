// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::for_missing_next_method
// EXPECT-ERROR-AT: 20:15
// EXPECT-ERROR: next()
// 目标：T1328 iterator() 存在但缺少 `next(): Option<T>` 时应报错。

package fixtures.typecheck

import scoop.core.*

interface BadIterator<T> {
    fun hasNext(): Bool
}

interface HasIterator<T> {
    fun iterator(): BadIterator<T>
}

fun use(xs: HasIterator<Int>) {
    for (x in xs) {
        println(x)
    }
}

