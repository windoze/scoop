// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::val_variant_pat_not_enum
// EXPECT-ERROR-AT: 12:9
// EXPECT-ERROR: variant pattern 只能用于 enum
// 目标：variant pattern 在 `val` 解构绑定中允许，但 initializer 仍必须是 enum / Option。

package fixtures.typecheck

import scoop.core.*

fun f() {
    val Some(x) = 1
}
