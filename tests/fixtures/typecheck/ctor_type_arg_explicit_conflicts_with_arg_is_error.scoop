// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::no_matching_overload
// EXPECT-ERROR: 没有匹配的重载

// P4-T01h：显式 ctor type args 与 ctor arg 反推 T 冲突时仍报 `no_matching_overload`
// （不引入新错误码；仅由现有重载选择路径走）。

import scoop.core.*

class Box<T>(val seed: T)

fun main(): Int {
    val b: Box<Int> = Box<Int>("hi")
    return 0
}
