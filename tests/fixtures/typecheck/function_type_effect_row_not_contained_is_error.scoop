// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::return_type_mismatch
// EXPECT-ERROR: 返回类型不匹配
// EXPECT-ERROR-AT: 12:12
// 目标：T0435 effect row containment：不能把需要更多效果的函数当作 Pure。

package fixtures.typecheck

import scoop.core.*

fun bad_effects(f: () -> Int / (Raise<RuntimeError>)): () -> Int / Pure {
    return f
}

