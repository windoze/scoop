// EXPECT: pass
// 目标：T0424 `with` 支持嵌套字段路径更新（a.b.c）。

package fixtures.typecheck

import scoop.core.*

struct Point(val x: Int, val y: Int)
struct Line(val start: Point, val end: Point)

fun use(line: Line) {
    val line2: Line = line with {
        start.x: 1,
        start.y: 2,
    }
}

