// EXPECT: pass
// T0130: where 约束 bound 驱动的方法分发——泛型函数体内通过约束调用接口方法应通过。

package fixtures.typecheck

import scoop.core.*

interface Show {
    fun show(): String
}

fun <T> display(x: T): String where T: Show {
    return x.show()
}
