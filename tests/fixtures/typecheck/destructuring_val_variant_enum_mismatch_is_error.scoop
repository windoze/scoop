// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::val_variant_pat_refutable_not_allowed
// EXPECT-ERROR-AT: 20:9
// EXPECT-ERROR: `val` 解构绑定只允许不可失败 pattern
// 目标：variant pattern 在 `val` 解构绑定中一律禁止，不再继续检查 enum 前缀细节。

package fixtures.typecheck

import scoop.core.*

enum Result {
    Ok(val value: Int),
}

enum Other {
    Ok(val value: Int),
}

fun f(r: Result) {
    val Other.Ok(v) = r
}
