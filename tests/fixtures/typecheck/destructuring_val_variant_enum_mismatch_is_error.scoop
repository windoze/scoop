// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::val_variant_pat_enum_mismatch
// EXPECT-ERROR-AT: 20:9
// EXPECT-ERROR: enum 前缀不匹配
// 目标：variant pattern 在 `val` 解构绑定中允许后，显式 enum 前缀必须匹配 initializer 类型。

package fixtures.typecheck

import scoop.core.*

enum Result {
    Ok(val value: Int),
}

enum Other {
    Ok(val value: Int),
}

fun f(r: Result) {
    val Other.Ok(v) = r
}
