// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::link::missing_entry
// linking 错误 fixture（M5-2 白名单）：本程序只定义辅助函数 helper，无 entry `main`。
// driver/link 阶段报 `scoop::link::missing_entry`（入口缺失归链接层——非 MIR 诊断，
// C5 边界：MIR 之后唯一允许的用户错误是 linking 白名单）。
//
// 注：materialize 的 no_template 仍是单态化内部错误通道（ICE 级）；面向用户的
// 缺入口错误统一为 link 白名单码。materialize_missing_template_errors 单测保留
// 覆盖内部路径。

package fixtures.mir2_fail

fun helper(): Int {
  42
}
