// EXPECT: fail
// EXPECT-ERROR: 顶层声明
// EXPECT-ERROR-CODE: scoop::parse::expected
// EXPECT-ERROR-AT: 9:1
// 目标：package-level `comptime if` 不再进入 ScoopIR 导出前裁剪路径。

package fixtures.scoopir

comptime if (true) {
    public fun <T> chosen(x: T): T { x }
} else {
    public fun <T> notChosen(x: T): T { x }
}
