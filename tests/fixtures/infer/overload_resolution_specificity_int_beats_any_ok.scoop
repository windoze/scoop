// EXPECT: pass
// 目标：P5-T02 Phase D specificity；`f(Int)` / `f(Any)` 同时 applicable 时选择更具体的 `f(Int)`。

package fixtures.infer

import scoop.core.*

fun f(x: Any): Any {
    return x
}

fun f(x: Int): Int {
    return x
}

fun ok() {
    val x: Int = f(1)
}
