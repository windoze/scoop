// EXPECT: pass
// 目标：T0513 most-specific candidate：
// - `f(1)` 在 `f(Int)` / `f(Any)` 同时可行时，应选择 `f(Int)`。

package fixtures.infer

import scoop.core.*

fun f(x: Any): Any {
    return x
}

fun f(x: Int): Int {
    return x
}

fun ok() {
    val x: Int = f(1)
}

