// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::ambiguous_overload
// EXPECT-ERROR: f(Any) | f(Int)
// 目标：P5-T01R Phase A-C 不提前做 specificity；`f(Int)` / `f(Any)` 同时 applicable 时先报 ambiguity。

package fixtures.infer

import scoop.core.*

fun f(x: Any): Any {
    return x
}

fun f(x: Int): Int {
    return x
}

fun ok() {
    val x: Int = f(1)
}
