// EXPECT: fail
// EXPECT-ERROR-CODE: scoop::typecheck::initializer_type_mismatch
// EXPECT-ERROR-AT: 16:18
// EXPECT-ERROR: 得到 Int | String
// 目标：T0514 `when` 分支类型合并：分支类型不一致时不再无脑退化到 Any，而是构造受限 union。

package fixtures.infer

import scoop.core.*

fun f() {
    val x = when (1) {
      1 -> 1
      else -> "hi"
    }
    val y: Int = x
}
