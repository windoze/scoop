## 当前轮次执行计划

### 约束与目标
- 本轮只处理 `TODO.md` 中第一个未完成任务，完成后停止。
- 在开始任何代码/命令执行前，先把计划写入本文件。
- 执行过程中如果计划变化、发现阻塞、完成关键步骤，立即更新本文件。

### 初始判断摘要
- 需要先检查最新提交信息，确认是否提到了遗留问题；若有，先修复这些问题，再继续任务。
- 然后读取 `TODO.md`，定位第一个未完成任务。
- 如果该任务过大，需要先拆分任务，并同步更新 `PLAN.md` 与 `TODO.md`。
- 之后实现、测试、更新文档状态、提交 git commit，并停止。

### 分步执行计划
1. 查看最新一次 git 提交的提交信息与上下文，确认是否提到需要先处理的遗留问题。
2. 读取 `TODO.md` 与 `PLAN.md`，确认当前任务顺序与计划状态。
3. 如最新提交提到遗留问题，先定位并修复，补充测试。
4. 识别 `TODO.md` 中第一个未完成任务。
5. 判断任务规模与依赖：
   - 若可直接完成，则进入实现。
   - 若过大或依赖缺失，则拆分/重排任务，并更新 `TODO.md`、`PLAN.md`。
6. 实施当前任务所需代码改动。
7. 运行相关验证，至少覆盖：
   - 受影响范围的测试；
   - 必要时运行 `cargo fmt`；
   - `cargo clippy --all-targets -- -D warnings`；
   - 如任务影响整体行为，再运行更高层测试。
8. 更新文档与任务状态：
   - 在 `TODO.md` 标记本轮任务完成，或在阻塞场景下调整任务顺序但保持 `[TODO]`。
   - 在 `PLAN.md` 反映当前状态。
   - 在本文件记录关键决策与结果。
9. 使用清晰的提交信息提交本轮改动。
10. 停止，不继续下一个任务。

### 风险与检查点
- 若工作区存在非本轮改动，不回退它们，只在必要时避开或兼容。
- 若发现缺失语言特性或库支持，不强行实现；改为研究、重排 `TODO.md`、更新 `PLAN.md` 后提交并停止。
- 若测试或 lint 暴露历史问题，需要先判断是否属于“最新提交提到的遗留问题”或当前任务影响范围；属于范围内则一并修复。

### 执行日志
- 已完成：初始化本计划文件。
- 已完成：检查最新提交、`TODO.md`、`PLAN.md`。最新提交信息未额外标注需优先修复的遗留问题。
- 已完成：识别到原 `T2003b` 同时覆盖 block / branch / while，单轮实现风险过高。
- 已完成：决定将 `T2003b` 拆为 `T2003b1`（nested block）、`T2003b2`（if/branch）、`T2003b3`（while + 诊断收口），并将本轮目标固定为 `T2003b1`。
- 下一步：修改 LLVM immediate-resume lowering，支持 nested block 中的单 direct perform 恢复，并补相应回归。
