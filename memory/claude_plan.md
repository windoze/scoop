# 本轮执行计划

## 目标

完成 `TODO.md` 中当前已实现但尚未提交收尾的首个任务 `T4016T1d3`，完成验证、文档同步与 Git 提交，然后立即停止，不进入下一项任务。

## 已知上下文

- 最新提交 `844be45c [T4016T1d3] Queue task Scoop blockers before migration` 未附带需要优先修复的额外历史问题。
- `T4016T1d3` 的代码实现已经完成，相关测试此前已通过。
- `TODO.md` 与 `PLAN.md` 已更新为：`T4016T1d3` 完成，下一项为 `T4016T1d4`。
- 当前剩余工作是收尾：格式检查、必要时格式化、确认工作树、提交 commit。

## 执行步骤

1. 检查 `cargo fmt --check`。
2. 若格式检查失败，运行 `cargo fmt` 修复格式，并根据受影响范围决定是否补跑必要验证。
3. 检查 `git status`，确认工作树仅包含本轮相关改动。
4. 更新本文件，记录“验证通过、准备提交”或对应实际状态。
5. 使用与任务对应的提交信息提交本轮改动。
6. 停止，不继续处理 `T4016T1d4`。

## 当前执行结果

- `cargo fmt --check` 初次发现若干 Rust 文件存在格式化差异，随后已执行 `cargo fmt` 修复。
- 再次执行 `cargo fmt --check` 已通过。
- 工作树检查显示改动仍集中在本轮 `T4016T1d3` 相关源码、fixture、`TODO.md`、`PLAN.md` 和本文件，没有发现额外无关改动。
- 本轮实现相关验证已具备：此前已通过 parser 单测、fixture 测试、`cargo run -p scoop -- test`、`cargo test --all`、`cargo clippy --all-targets -- -D warnings`；当前仅补齐格式检查收尾。
- 下一步是提交 commit，提交后立即停止。

## 风险与约束

- 不得回退用户或其他流程已有改动。
- 不得将未解决问题带入下一任务；若收尾过程中发现新的既有问题，需要先处理或按规则调整 `TODO.md`/`PLAN.md` 后再停下。
- 本轮只允许完成一个任务，因此提交后必须停止。
