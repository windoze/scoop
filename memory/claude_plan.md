## 当前目标

本次只处理 `TODO.md` 中第一个未完成任务，并在完成后停止。

## 执行思路摘要

1. 先检查最新一次 Git 提交，确认是否提到任何已知遗留问题。
2. 若最新提交中提到需要先修复的既有问题，则优先一并处理。
3. 阅读 `TODO.md`，定位第一个未完成任务。
4. 如该任务过大，先拆分为更小的子任务，并同步更新 `PLAN.md` 与 `TODO.md`。
5. 实现本次实际要做的首个任务或首个子任务。
6. 运行相关格式化、静态检查与测试，修复发现的问题，目标是无编译告警、无 clippy 告警、相关测试通过。
7. 更新文档状态：勾选 `TODO.md` 中已完成项，并更新 `PLAN.md` 的当前进度。
8. 进行 Git 提交，提交信息应清晰描述本次完成内容。
9. 停止，不继续处理下一个任务。

## 详细执行步骤

1. 查看最新提交信息与改动摘要。
2. 阅读 `TODO.md` 与 `PLAN.md`，建立当前任务上下文。
3. 判断首个未完成任务是否需要拆分：
   - 若不需要，直接实现。
   - 若需要，先改计划文件，再只做第一个子任务。
4. 阅读相关源码、测试与文档，确定修改范围。
5. 开始代码修改，并在关键节点回写本文件说明最新进展。
6. 运行必要验证：
   - `cargo fmt --check` 或 `cargo fmt`
   - `cargo clippy --all-targets -- -D warnings`
   - 与本任务相关的 `cargo test` / fixture 测试
7. 若测试失败，立即修复直到通过。
8. 更新 `TODO.md`、`PLAN.md`、必要的 `README.md` 或注释。
9. 检查 Git 状态，确保提交内容只包含本次任务所需修改。
10. 提交并停止。

## 进度记录

- 已创建本计划文件，接下来将检查最新提交与任务列表。
- 已检查最新提交：提交信息本身未提及额外遗留 bug；当前需要继续处理的 Float 剩余工作已在 `TODO.md` 中体现为 `T0148d`。
- 已确认首个未完成任务原为 `T0148d`，但其同时覆盖 `comptime`、多文件与审计，范围偏大，不适合单轮直接完成。
- 已决定先把 `T0148d` 拆为三个子任务：
  1. `T0148d-1`：Float comptime 值模型与基础常量折叠。
  2. `T0148d-2`：多文件 / 非入口源文件 Float literal 回归。
  3. `T0148d-3`：剩余转换、边角语义与审计。
- 当前执行目标将切换为 `T0148d-1`。
