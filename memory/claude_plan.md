执行计划（可审计摘要）

我会维护这里作为进度记录；不写入私有推理链，只记录可执行计划、关键决策和验证结果。

1. 读取 TODO.md，按标题是否带 [DONE] 找到第一个未完成任务。
2. 检查最近提交是否明确提到与该任务直接相关的未完成问题。
3. 阅读该任务涉及的 PLAN.md、源码、测试和夹具上下文，仅限当前任务所需范围。
4. 实现第一个未完成任务；若发现阻塞当前任务的真实前置缺口，则按要求更新 TODO.md 并停止。
5. 运行任务要求的验证和相关测试，必要时修复失败。
6. 将任务标题标记为 [DONE]，更新 TODO.md 的完成记录；只有阶段计划变化时才更新 PLAN.md。
7. 检查 git 状态和差异，提交本次任务的全部相关改动，然后停止。

当前状态：P2-T01 已在 `TODO.md` 与 `TODO-3.md` 标记为 `[DONE]` 并写入完成记录；验证已通过。下一步执行最终状态检查、暂存相关文件并提交 `[P2-T01] Add HIR facts crate skeleton`。
