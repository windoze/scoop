本轮执行计划

1. 读取 TODO.md，按文件顺序找出第一个标题未带 [DONE] 的任务。
2. 查看该任务的具体要求、依赖、验证要求和完成记录。
3. 检查最新提交是否明确提到与该任务直接相关的未完成问题；只处理会阻塞当前任务的问题。
4. 针对当前任务读取必要代码、测试和文档，确认实现边界。
5. 按最小正确改动实现当前任务；如遇到不可绕过的缺失功能或阻塞缺陷，则在 TODO.md 插入最小前置任务并停止。
6. 运行当前任务要求的验证命令和相关测试；任何新观察到且未被明确排期的失败都必须修复或排入 TODO.md。
7. 任务完成后，在 TODO.md 中给任务标题加 [DONE] 并更新完成记录；只有阶段级计划变化才更新 PLAN.md。
8. 检查 git 状态和差异，提交本轮所有相关改动，然后停止，不继续下一个任务。

当前状态：P7-T04-a 实现、验证和 TODO 完成记录均已完成；已通过指定验证、effect_lowered fixture、默认 LLVM layout 测试、clippy 与 diff check。下一步提交本轮相关改动并停止。
