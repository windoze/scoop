# B-08 - MIR 成员存取 / 赋值合法性

本 bucket entry 数：4
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：P7-A2 retired FrontendReject rows；InternalBugSentinel rows remain active

## Symptom

- Active inventory entries: 4; 一级类 `B`; routes: `RawMirLlvm`=4.
- P7-A2 retired 2 FrontendReject rows (`UMB-1131`, `UMB-1142`) after typecheck assignment/member-store gates were confirmed.
- This table is copied from `audit/UMB_inventory.csv` and intentionally lists every active entry assigned to this bucket.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-1132 | `crates/scoopc/src/llvm/codegen/mir_body/member.rs` | 148 | pass MIR member store value type | `RawMirLlvm` | `type` | `InternalBugSentinel` |
| UMB-1133 | `crates/scoopc/src/llvm/codegen/mir_body/member.rs` | 154 | pass MIR member store operand type | `RawMirLlvm` | `type` | `InternalBugSentinel` |
| UMB-1146 | `crates/scoopc/src/llvm/codegen/mir_body/member.rs` | 316 | pass MIR member store receiver place | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-1148 | `crates/scoopc/src/llvm/codegen/mir_body/member.rs` | 352 | pass MIR packed struct member store | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-1132 / UMB-1133 `crates/scoopc/src/llvm/codegen/mir_body/member.rs:148`

```text
  146 |         let _value_cg = self.cg_ty_of_mir_type(mir_types, value_ty).ok_or(
> 148 |             LlvmEmitError::UnsupportedMainBody {
  149 |                 kind: "pass MIR member store value type",
  150 |                 at: span.into(),
  151 |             },
  152 |         )?;
  153 |         let _operand_cg = self.mir_operand_cg_ty(body, mir_types, value).ok_or(
> 154 |             LlvmEmitError::UnsupportedMainBody {
```

#### Excerpt 2: UMB-1146 `crates/scoopc/src/llvm/codegen/mir_body/member.rs:316`

```text
  314 |         let (field_idx, field_cg) = self.lookup_struct_field(struct_ty, field_fqn, span)?;
  315 |         let crate::mir::Operand::Local(local) = receiver else {
> 316 |             return Err(LlvmEmitError::UnsupportedMainBody {
  317 |                 kind: "pass MIR member store receiver place",
  318 |                 at: span.into(),
```

#### Excerpt 3: UMB-1148 `crates/scoopc/src/llvm/codegen/mir_body/member.rs:352`

```text
  350 |         {
  351 |             if require_writable {
> 352 |                 return Err(LlvmEmitError::UnsupportedMainBody {
  353 |                     kind: "pass MIR packed struct member store",
  354 |                     at: span.into(),
```

## Root Cause Hypothesis

- Active root cause: value type, operand type, receiver place, and packed struct store shape drift still require MIR/member-store verifier coverage.
- Retired frontend root cause: non-writable targets and immutable class member stores are now rejected by typecheck before LLVM codegen.
- Upstream gate recorded in inventory: `typecheck assignment/member-store gate: target writability and value type are valid`.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part3.md#2-变量绑定与赋值` | 4 |
| `docs/spec/language_spec-part3.md#8-属性` | 4 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | P7-A2 retired 2 rows to `audit/UMB_retired.csv` |
| `InternalBugSentinel` | 4 | remains for later verifier task |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- P7-A2 replaced the two user-facing writability fallbacks with upstream-gated `unreachable!` invariants.
- Later B-08 internal work must retire value/operand type drift, receiver place drift, and packed struct store sentinels through MIR/member-store verifier coverage.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-08-member-store/`.
- P7-A2 active fixtures cover non-writable assignment targets, immutable class member stores, property gate diagnostics, and writable local/member stores.
- Retired IDs are tracked in `audit/UMB_retired.csv`; active fixture `COVERS` fields reference only active inventory IDs.

## Open Questions

- Remaining rows are verifier/internal contract work and should be retired by the later B-08 internal task, not by weakening frontend diagnostics.
