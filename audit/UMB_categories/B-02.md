# B-02 - MIR local / member 类型推断不完整

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：P7-B2.1 / retired

## Symptom

- Active inventory entries: 0; retired ledger covers the former 6 `InternalBugSentinel` rows.
- The table below is retained as a historical pre-retirement snapshot from `audit/UMB_inventory_initial.csv`.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0859 | `crates/scoopc/src/llvm/codegen/main/expr_value.rs` | 17 | unknown local value | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-0899 | `crates/scoopc/src/llvm/codegen/main/gc_locals.rs` | 103 | local slot gep source type | `RawMirLlvm` | `type` | `InternalBugSentinel` |
| UMB-0921 | `crates/scoopc/src/llvm/codegen/main/immut_value.rs` | 39 | val without initializer | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-1191 | `crates/scoopc/src/llvm/codegen/mir_body/string.rs` | 15 | MIR unresolved name source type | `RawMirLlvm` | `type` | `InternalBugSentinel` |
| UMB-1221 | `crates/scoopc/src/llvm/codegen/mir_body/types.rs` | 44 | pass MIR local member field type drift | `RawMirLlvm` | `type` | `InternalBugSentinel` |
| UMB-1222 | `crates/scoopc/src/llvm/codegen/mir_body/types.rs` | 74 | pass MIR local type | `RawMirLlvm` | `type` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-1221 `crates/scoopc/src/llvm/codegen/mir_body/types.rs:44`

Why representative: represents route `RawMirLlvm`, surface `type`, kind `pass MIR local member field type drift`; file frequency in this bucket is 2 and kind frequency is 1.

```text
  34 |                 ) {
  35 |                     continue;
  36 |                 }
  37 |                 let Ok(field_cg) =
  38 |                     self.mir_member_field_cg_ty(stmt.span, body, mir_types, receiver, member)
  39 |                 else {
  40 |                     continue;
  41 |                 };
  42 |                 if let Some(previous) = member_field_cg {
  43 |                     if !self.cg_ty_layout_equivalent(previous, field_cg) {
> 44 |                         return Err(LlvmEmitError::UnsupportedMainBody {
  45 |                             kind: "pass MIR local member field type drift",
  46 |                             at: stmt.span.into(),
  47 |                         });
  48 |                     }
  49 |                 } else {
  50 |                     member_field_cg = Some(field_cg);
  51 |                 }
  52 |             }
  53 |         }
  54 |         if let Some(field_cg) = member_field_cg
```

#### Excerpt 2: UMB-0899 `crates/scoopc/src/llvm/codegen/main/gc_locals.rs:103`

Why representative: represents route `RawMirLlvm`, surface `type`, kind `local slot gep source type`; file frequency in this bucket is 1 and kind frequency is 1.

```text
   93 |             return Err(LlvmEmitError::UnsupportedMainBody {
   94 |                 kind: "local slot gep base pointer type",
   95 |                 at: at.into(),
   96 |             });
   97 |         };
   98 |         let base_ptr =
   99 |             self.rematerialize_ptr_in_current_block(at, base_ptr, &format!("{name}_base"))?;
  100 | 
  101 |         let source_ty =
  102 |             inst.get_gep_source_element_type()
> 103 |                 .map_err(|_| LlvmEmitError::UnsupportedMainBody {
  104 |                     kind: "local slot gep source type",
  105 |                     at: at.into(),
  106 |                 })?;
  107 |         match source_ty {
  108 |             BasicTypeEnum::StructType(struct_ty) => {
  109 |                 let mut indices = inst.get_indices();
  110 |                 if indices.is_empty() {
  111 |                     for operand_index in 1..inst.get_num_operands() {
  112 |                         let Some(operand) =
  113 |                             inst.get_operand(operand_index).and_then(|op| op.value())
```

#### Excerpt 3: UMB-1222 `crates/scoopc/src/llvm/codegen/mir_body/types.rs:74`

Why representative: represents route `RawMirLlvm`, surface `type`, kind `pass MIR local type`; file frequency in this bucket is 2 and kind frequency is 1.

```text
  64 |             && matches!(local.source, crate::mir::LocalSourceKind::CompilerTemporary)
  65 |             && (local_cg.is_none()
  66 |                 || matches!(local_cg, Some(CgTy::Ref))
  67 |                 || matches!(assigned_cg, CgTy::Enum(_))
  68 |                 || self.mir_type_contains_param(mir_types, local.ty)
  69 |                 || local_cg
  70 |                     .is_some_and(|local_cg| self.cg_ty_layout_equivalent(local_cg, assigned_cg)))
  71 |         {
  72 |             return Ok(assigned_cg);
  73 |         }
> 74 |         local_cg.ok_or(LlvmEmitError::UnsupportedMainBody {
  75 |             kind: "pass MIR local type",
  76 |             at: local.span.into(),
  77 |         })
  78 |     }
  79 | 
  80 |     pub(in crate::llvm::codegen) fn mir_local_assignment_cg_ty(
  81 |         &self,
  82 |         body: &crate::mir::Body,
  83 |         mir_types: &TypeStore,
  84 |         local_id: crate::mir::LocalId,
```

## Root Cause Hypothesis

- Root cause: MIR materialize 未把 local、member source type 和 codegen type 作为必填契约发布，导致 LLVM lowering 在 slot、name、initializer 或 member field 处重新推断类型。缺失的不变量是 MIR local/member type table 对所有可达引用 total。
- Upstream gate(s) recorded in inventory:
- `MIR materialize: local/member source and codegen types are complete` (6 entries).
- Dominant files: `crates/scoopc/src/llvm/codegen/mir_body/types.rs` (2), `crates/scoopc/src/llvm/codegen/main/expr_value.rs` (1), `crates/scoopc/src/llvm/codegen/main/gc_locals.rs` (1), `crates/scoopc/src/llvm/codegen/main/immut_value.rs` (1), `crates/scoopc/src/llvm/codegen/mir_body/string.rs` (1).
- Dominant kind labels: `unknown local value` (1), `local slot gep source type` (1), `val without initializer` (1), `MIR unresolved name source type` (1), `pass MIR local member field type drift` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part2.md#1-类型总览` | 6 |
| `docs/spec/language_spec-part3.md#17-类型推断` | 6 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired by P7-B2.1 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

- Post-fix planning counts sum to 0, matching the active bucket entry count 0.
- Current CSV expected_class distribution: none; retired ledger records `InternalBugSentinel`=6.

## Fix Strategy Outline

- P7-B2.1 puts local/member type completeness into MIR production and materialized validation; LLVM lowering now treats these states as internal invariants.
- Retired IDs: `UMB-0859`, `UMB-0899`, `UMB-0921`, `UMB-1191`, `UMB-1221`, `UMB-1222`.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-02-mir-local-type/`.
- P7-B2.1 activated B-02 fixtures; retired IDs are covered by `audit/UMB_retired.csv`, so active fixture `COVERS` headers no longer reference B-02 IDs.
- Direct coverage requirement is closed by retired ledger coverage and active positive/negative fixtures in this directory.

## Open Questions

- No active B-02 blocker remains.
