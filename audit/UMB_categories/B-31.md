# B-31 - 标量扩展方法 (Float/Int/Char/Bool/String)

本 bucket entry 数：12  
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：未分配 / body completed by U2-T02

## Symptom

- Inventory entries: 12; 一级类 `B`; routes: `Helper`=12.
- This table is copied from `audit/UMB_inventory.csv` and intentionally lists every entry assigned to this bucket.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0575 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 529 | String.byteLength arity mismatch | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0576 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 545 | String.byteLength load type | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0577 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 561 | String.getByte arity mismatch | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0578 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 567 | String.getByte named arg | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0579 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 579 | String.getByte index value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0580 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 584 | String.getByte index type | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0584 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 761 | Char.hash receiver value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0585 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 767 | Char.hash receiver type | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0586 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 797 | Int.hash receiver value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0587 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 803 | Int.hash receiver type | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0591 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 894 | Float.toInt return value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0592 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 899 | Float.toInt return type | `Helper` | `intrinsic` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-0575 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:529`

Why representative: represents route `Helper`, surface `intrinsic`, kind `String.byteLength arity mismatch`; file frequency in this bucket is 12 and kind frequency is 1.

```text
  519 | 
  520 |         match method_name {
  521 |             // T0120: String.byteLength() — 0 args → Int (inline LLVM IR: read ScoopString.len)
  522 |             "byteLength" => {
  523 |                 let recv_ptr = self.materialize_gc_sensitive_string_method_receiver(
  524 |                     receiver.span,
  525 |                     method_name,
  526 |                     deferred_recv.clone(),
  527 |                 )?;
  528 |                 if !args.is_empty() {
> 529 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  530 |                         kind: "String.byteLength arity mismatch",
  531 |                         at: span.into(),
  532 |                     });
  533 |                 }
  534 |                 let scoop_str_ty = self.llvm_scoop_string_type();
  535 |                 let len_ptr = self.builder.build_struct_gep(
  536 |                     scoop_str_ty,
  537 |                     recv_ptr,
  538 |                     1,
  539 |                     "str_byte_length_gep",
```

#### Excerpt 2: UMB-0576 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:545`

Why representative: represents route `Helper`, surface `intrinsic`, kind `String.byteLength load type`; file frequency in this bucket is 12 and kind frequency is 1.

```text
  535 |                 let len_ptr = self.builder.build_struct_gep(
  536 |                     scoop_str_ty,
  537 |                     recv_ptr,
  538 |                     1,
  539 |                     "str_byte_length_gep",
  540 |                 )?;
  541 |                 let len_val =
  542 |                     self.builder
  543 |                         .build_load(self.context.i64_type(), len_ptr, "str_byte_length")?;
  544 |                 let BasicValueEnum::IntValue(iv) = len_val else {
> 545 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  546 |                         kind: "String.byteLength load type",
  547 |                         at: span.into(),
  548 |                     });
  549 |                 };
  550 |                 Ok(CgValue::int(
  551 |                     iv,
  552 |                     IntTy {
  553 |                         bits: 64,
  554 |                         signed: true,
  555 |                     },
```

#### Excerpt 3: UMB-0577 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:561`

Why representative: represents route `Helper`, surface `intrinsic`, kind `String.getByte arity mismatch`; file frequency in this bucket is 12 and kind frequency is 1.

```text
  551 |                     iv,
  552 |                     IntTy {
  553 |                         bits: 64,
  554 |                         signed: true,
  555 |                     },
  556 |                 ))
  557 |             }
  558 |             // T0120: String.getByte(index) — 1 Int arg → Int (inline LLVM IR: bounds-checked byte read)
  559 |             "getByte" => {
  560 |                 if args.len() != 1 {
> 561 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  562 |                         kind: "String.getByte arity mismatch",
  563 |                         at: span.into(),
  564 |                     });
  565 |                 }
  566 |                 let hir::CallArg::Positional(idx_expr) = &args[0] else {
  567 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  568 |                         kind: "String.getByte named arg",
  569 |                         at: span.into(),
  570 |                     });
  571 |                 };
```

## Root Cause Hypothesis

- Root cause: String/Float/Int/Char/Bool 扩展方法仍在 LLVM intrinsic path 检查 arity、receiver、index/value 和 return type，说明 scalar-extension resolver 未完全验证方法 schema。
- Upstream gate(s) recorded in inventory:
- `typecheck scalar-extension gate: receiver and return scalar types are valid` (12 entries).
- Dominant files: `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` (12).
- Dominant kind labels: `String.byteLength arity mismatch` (1), `String.byteLength load type` (1), `String.getByte arity mismatch` (1), `String.getByte named arg` (1), `String.getByte index value` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part2.md#3-内建标量类型` | 12 |
| `docs/spec/language_spec-part3.md#7-extension-函数` | 12 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 12 | from current inventory expected_class |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

- Post-fix planning counts sum to 12, matching the bucket entry count 12.
- Current CSV expected_class distribution: `InternalBugSentinel`=12.

## Fix Strategy Outline

- U4 应把 scalar extension receiver type、argument schema 和 return type 前置到 typecheck/resolver，并让 LLVM 只调用已解析 intrinsic。
- Detailed landing steps, exact verifier names and fixture activation state remain owned by U4-T01/U5; this document fixes the U2 root-cause input and classification boundary.

## Fixture Set Pointer

- Planned fixture directory: `tests/fixtures/umb_fix/B-31-scalar-methods/`.
- U5 should add `_README.md` and fixture rows that link back to `B-31` and the inventory ids listed in the Symptom table above.

## Open Questions

- 无缺失 gate；U4 需确认 String byte operations 与 general sysroot bridge 的边界。
- All bucket/class/spec/gate values are concrete in the current inventory for this bucket.
