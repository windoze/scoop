# B-31 - 标量扩展方法 (Float/Int/Char/Bool/String)

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.1 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.1 retired all 12 InternalBugSentinel rows (`UMB-0575`..`UMB-0580`, `UMB-0584`..`UMB-0587`, `UMB-0591`..`UMB-0592`) after scalar method receiver, arity, value, and return contracts were verified before LLVM lowering.

## Root Cause Hypothesis

- Retired root cause: String byte-level helpers and scalar `hash`/`toInt` lowering previously rechecked receiver value shape, argument shape, index type, and runtime return type in LLVM codegen.
- Typecheck now owns user-facing scalar method receiver/arity/type validation, and LLVM lowering treats residual mismatches as internal verifier invariant failures rather than user diagnostics.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part2.md#3-内建标量类型` | retired 12 |
| `docs/spec/language_spec-part3.md#7-extension-函数` | retired 12 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 12 rows in P7-B3.1 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- Typecheck owns String `byteLength`/`getByte` arity and index contracts plus scalar method receiver and return type contracts.
- LLVM builtin lowering now uses verifier-backed `panic_verified_builtin_contract` invariants instead of `UnsupportedMainBody` diagnostics for this bucket.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-31-scalar-methods/`.
- B-31 fixtures are active after P7-B3.1; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.

## Open Questions

- No active B-31 blocker remains. B-30 named/unsafe intrinsic rows in the same source file remain active for P7-B3.5.
