# B-32 - print / panic / sysroot 桥接

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.1 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.1 retired all 10 InternalBugSentinel rows (`UMB-0543`..`UMB-0552`) after sysroot print/panic bridge argument and String value contracts were verified before LLVM lowering.

## Root Cause Hypothesis

- Retired root cause: print/println/panic sysroot bridge lowering previously rechecked arity, named args, runtime FQN, String value materialization, and pointer shape in LLVM codegen.
- Typecheck and sysroot bridge resolution now own user-facing call shape validation, while LLVM lowering treats residual bridge drift as an internal invariant.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part1.md#8-标准库排除说明` | retired 10 |
| `docs/spec/language_spec-part5.md#18-intrinsic-与-sysroot-声明` | retired 10 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 10 rows in P7-B3.1 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- Typecheck and sysroot bridge lowering own print/println/panic argument shape and String materialization contracts.
- LLVM builtin lowering now uses verifier-backed `panic_verified_builtin_contract` invariants instead of `UnsupportedMainBody` diagnostics for this bucket.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-32-print-panic-sysroot/`.
- B-32 fixtures are active after P7-B3.1; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.

## Open Questions

- No active B-32 blocker remains. B-30 named intrinsic fixtures that share the spec area remain active for P7-B3.5.
