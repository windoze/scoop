# B-32 - print / panic / sysroot 桥接

本 bucket entry 数：10  
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：未分配 / body completed by U2-T02

## Symptom

- Inventory entries: 10; 一级类 `B`; routes: `Helper`=10.
- This table is copied from `audit/UMB_inventory.csv` and intentionally lists every entry assigned to this bucket.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0543 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 14 | sysroot panic arity mismatch | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0544 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 21 | sysroot panic named arg | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0545 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 30 | sysroot panic message value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0546 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 36 | sysroot panic message type | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0547 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 77 | sysroot print/println String arg value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0548 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 107 | __scoop_print_string fqn | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0549 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 115 | __scoop_print_string arity | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0550 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 122 | __scoop_print_string named arg | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0551 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 131 | __scoop_print_string arg value | `Helper` | `intrinsic` | `InternalBugSentinel` |
| UMB-0552 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 137 | __scoop_print_string arg type | `Helper` | `intrinsic` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-0543 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:14`

Why representative: represents route `Helper`, surface `intrinsic`, kind `sysroot panic arity mismatch`; file frequency in this bucket is 10 and kind frequency is 1.

```text
   4 | use crate::mir;
   5 | 
   6 | impl<'a, 'ctx> MainCodegen<'a, 'ctx> {
   7 |     pub(in crate::llvm::codegen) fn codegen_sysroot_panic(
   8 |         &mut self,
   9 |         span: crate::span::Span,
  10 |         callee_span: crate::span::Span,
  11 |         args: &[hir::CallArg],
  12 |     ) -> Result<CgValue<'ctx>, LlvmEmitError> {
  13 |         if args.len() != 1 {
> 14 |             return Err(LlvmEmitError::UnsupportedMainBody {
  15 |                 kind: "sysroot panic arity mismatch",
  16 |                 at: span.into(),
  17 |             });
  18 |         }
  19 | 
  20 |         let hir::CallArg::Positional(message_expr) = &args[0] else {
  21 |             return Err(LlvmEmitError::UnsupportedMainBody {
  22 |                 kind: "sysroot panic named arg",
  23 |                 at: span.into(),
  24 |             });
```

#### Excerpt 2: UMB-0544 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:21`

Why representative: represents route `Helper`, surface `intrinsic`, kind `sysroot panic named arg`; file frequency in this bucket is 10 and kind frequency is 1.

```text
  11 |         args: &[hir::CallArg],
  12 |     ) -> Result<CgValue<'ctx>, LlvmEmitError> {
  13 |         if args.len() != 1 {
  14 |             return Err(LlvmEmitError::UnsupportedMainBody {
  15 |                 kind: "sysroot panic arity mismatch",
  16 |                 at: span.into(),
  17 |             });
  18 |         }
  19 | 
  20 |         let hir::CallArg::Positional(message_expr) = &args[0] else {
> 21 |             return Err(LlvmEmitError::UnsupportedMainBody {
  22 |                 kind: "sysroot panic named arg",
  23 |                 at: span.into(),
  24 |             });
  25 |         };
  26 | 
  27 |         let message_v = self.codegen_expr_in_expected_context(message_expr, Some(CgTy::String))?;
  28 |         let message_v = self.coerce_value(message_expr.span, message_v, CgTy::String)?;
  29 |         let Some(raw) = message_v.value else {
  30 |             return Err(LlvmEmitError::UnsupportedMainBody {
  31 |                 kind: "sysroot panic message value",
```

#### Excerpt 3: UMB-0545 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:30`

Why representative: represents route `Helper`, surface `intrinsic`, kind `sysroot panic message value`; file frequency in this bucket is 10 and kind frequency is 1.

```text
  20 |         let hir::CallArg::Positional(message_expr) = &args[0] else {
  21 |             return Err(LlvmEmitError::UnsupportedMainBody {
  22 |                 kind: "sysroot panic named arg",
  23 |                 at: span.into(),
  24 |             });
  25 |         };
  26 | 
  27 |         let message_v = self.codegen_expr_in_expected_context(message_expr, Some(CgTy::String))?;
  28 |         let message_v = self.coerce_value(message_expr.span, message_v, CgTy::String)?;
  29 |         let Some(raw) = message_v.value else {
> 30 |             return Err(LlvmEmitError::UnsupportedMainBody {
  31 |                 kind: "sysroot panic message value",
  32 |                 at: message_expr.span.into(),
  33 |             });
  34 |         };
  35 |         let BasicValueEnum::PointerValue(message_ptr) = raw else {
  36 |             return Err(LlvmEmitError::UnsupportedMainBody {
  37 |                 kind: "sysroot panic message type",
  38 |                 at: message_expr.span.into(),
  39 |             });
  40 |         };
```

## Root Cause Hypothesis

- Root cause: print/println/panic sysroot bridge 仍检查 arity、named args、message/String value 和 return shape，说明 sysroot bridge gate 未完全 materialize supported values。
- Upstream gate(s) recorded in inventory:
- `sysroot bridge gate: print/panic arguments are materialized as supported values` (10 entries).
- Dominant files: `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` (10).
- Dominant kind labels: `sysroot panic arity mismatch` (1), `sysroot panic named arg` (1), `sysroot panic message value` (1), `sysroot panic message type` (1), `sysroot print/println String arg value` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part1.md#8-标准库排除说明` | 10 |
| `docs/spec/language_spec-part5.md#18-intrinsic-与-sysroot-声明` | 10 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 10 | from current inventory expected_class |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

- Post-fix planning counts sum to 10, matching the bucket entry count 10.
- Current CSV expected_class distribution: `InternalBugSentinel`=10.

## Fix Strategy Outline

- U4 应把 print/panic argument materialization、String value contract 和 runtime symbol signature 前置到 sysroot bridge verifier。
- Detailed landing steps, exact verifier names and fixture activation state remain owned by U4-T01/U5; this document fixes the U2 root-cause input and classification boundary.

## Fixture Set Pointer

- Planned fixture directory: `tests/fixtures/umb_fix/B-32-print-panic-sysroot/`.
- U5 should add `_README.md` and fixture rows that link back to `B-32` and the inventory ids listed in the Symptom table above.

## Open Questions

- 无缺失 gate；U4 需决定 panic message interpolation 与 B-18 literal/string 的共享验证。
- All bucket/class/spec/gate values are concrete in the current inventory for this bucket.
