# B-35 - unsafe / NoGC / 边界

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv；retired ledger：audit/UMB_retired.csv
生成时间：2026-05-18  
负责人/状态：P7-B2.7 / retired

## Symptom

- Active inventory entries: 0; retired ledger covers the initial 5 entries; 一级类 `B/C`; routes: `RawMirLlvm`=5.
- The table below is the retired snapshot retained for traceability; live rows now live in `audit/UMB_retired.csv`.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0827 | `crates/scoopc/src/llvm/codegen/main/declare.rs` | 569 | spill slot explicit frame mirrors | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-0828 | `crates/scoopc/src/llvm/codegen/main/declare.rs` | 576 | spill slot/frame slot count mismatch | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-0881 | `crates/scoopc/src/llvm/codegen/main/frame.rs` | 446 | spill slot/frame slot count mismatch | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-0885 | `crates/scoopc/src/llvm/codegen/main/frame.rs` | 716 | value/frame slot count mismatch | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-1209 | `crates/scoopc/src/llvm/codegen/mir_body/terminator.rs` | 559 | value primitive boundary payload requires published contract | `RawMirLlvm` | `terminator` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-0828 `crates/scoopc/src/llvm/codegen/main/declare.rs:576`

Why representative: represents route `RawMirLlvm`, surface `rvalue`, kind `spill slot/frame slot count mismatch`; file frequency in this bucket is 2 and kind frequency is 2.

```text
  566 |             .explicit_frame_slot_mirrors_for(slot)
  567 |             .map(|slots| slots.to_vec());
  568 |         if explicit_frame_enabled && frame_slots.is_none() {
  569 |             return Err(LlvmEmitError::UnsupportedMainBody {
  570 |                 kind: "spill slot explicit frame mirrors",
  571 |                 at: at.into(),
  572 |             });
  573 |         }
  574 |         let frame_slots = frame_slots.unwrap_or_default();
  575 |         if explicit_frame_enabled && frame_slots.len() != gc_leaf_slots.len() {
> 576 |             return Err(LlvmEmitError::UnsupportedMainBody {
  577 |                 kind: "spill slot/frame slot count mismatch",
  578 |                 at: at.into(),
  579 |             });
  580 |         }
  581 |         for (index, (slot, value_ptr_ty)) in gc_leaf_slots.into_iter().enumerate() {
  582 |             let frame_slot = frame_slots.get(index).copied().unwrap_or(slot);
  583 |             self.function_cx
  584 |                 .tracked_gc_root_slots
  585 |                 .push(TrackedGcRootSlot {
  586 |                     slot,
```

#### Excerpt 2: UMB-0881 `crates/scoopc/src/llvm/codegen/main/frame.rs:446`

Why representative: represents route `RawMirLlvm`, surface `rvalue`, kind `spill slot/frame slot count mismatch`; file frequency in this bucket is 2 and kind frequency is 2.

```text
  436 |         let Some(frame_slots) = self
  437 |             .explicit_frame_slot_mirrors_for(slot)
  438 |             .map(|slots| slots.to_vec())
  439 |         else {
  440 |             return Ok(Vec::new());
  441 |         };
  442 | 
  443 |         let mut gc_leaf_slots = Vec::new();
  444 |         self.collect_gc_ptr_leaf_slots_in_spill(slot, value_ty, name_prefix, &mut gc_leaf_slots)?;
  445 |         if frame_slots.len() != gc_leaf_slots.len() {
> 446 |             return Err(LlvmEmitError::UnsupportedMainBody {
  447 |                 kind: "spill slot/frame slot count mismatch",
  448 |                 at: at.into(),
  449 |             });
  450 |         }
  451 | 
  452 |         Ok(gc_leaf_slots
  453 |             .into_iter()
  454 |             .zip(frame_slots)
  455 |             .map(|((leaf_slot, value_ptr_ty), frame_slot)| (leaf_slot, value_ptr_ty, frame_slot))
  456 |             .collect())
```

#### Excerpt 3: UMB-0827 `crates/scoopc/src/llvm/codegen/main/declare.rs:569`

Why representative: represents route `RawMirLlvm`, surface `rvalue`, kind `spill slot explicit frame mirrors`; file frequency in this bucket is 2 and kind frequency is 1.

```text
  559 |         self.collect_gc_ptr_leaf_slots_in_spill(slot, value_ty, name_prefix, &mut gc_leaf_slots)?;
  560 |         let explicit_frame_enabled = self
  561 |             .function_cx
  562 |             .explicit_frame_layout
  563 |             .frame_storage
  564 |             .is_some();
  565 |         let frame_slots = self
  566 |             .explicit_frame_slot_mirrors_for(slot)
  567 |             .map(|slots| slots.to_vec());
  568 |         if explicit_frame_enabled && frame_slots.is_none() {
> 569 |             return Err(LlvmEmitError::UnsupportedMainBody {
  570 |                 kind: "spill slot explicit frame mirrors",
  571 |                 at: at.into(),
  572 |             });
  573 |         }
  574 |         let frame_slots = frame_slots.unwrap_or_default();
  575 |         if explicit_frame_enabled && frame_slots.len() != gc_leaf_slots.len() {
  576 |             return Err(LlvmEmitError::UnsupportedMainBody {
  577 |                 kind: "spill slot/frame slot count mismatch",
  578 |                 at: at.into(),
  579 |             });
```

## Root Cause Hypothesis

- Root cause: NoGC/unsafe frame lowering 仍检查 spill slot、frame slot、value slot 和 primitive boundary payload，说明 low-level frame/stackmap boundary contract 未闭合。
- Upstream gate(s) recorded in inventory:
- `unsafe/NoGC gate: low-level boundary, frame, and stackmap invariants hold` (5 entries).
- Dominant files: `crates/scoopc/src/llvm/codegen/main/declare.rs` (2), `crates/scoopc/src/llvm/codegen/main/frame.rs` (2), `crates/scoopc/src/llvm/codegen/mir_body/terminator.rs` (1).
- Dominant kind labels: `spill slot/frame slot count mismatch` (2), `spill slot explicit frame mirrors` (1), `value/frame slot count mismatch` (1), `value primitive boundary payload requires published contract` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part6.md#2-unsafe-context` | 5 |
| `docs/spec/language_spec-part6.md#4-nogc` | 5 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired by P7-B2.7 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

- Post-fix planning counts sum to 0, matching the active bucket entry count 0.
- Current CSV expected_class distribution: empty; retired ledger covers `InternalBugSentinel=5`.

### Entry-Level Cross-Class Split

| id | entry_class | rationale |
|---|---|---|
| UMB-0827 | `B-invariant` | retired by explicit-frame mirror invariant |
| UMB-0828 | `B-invariant` | retired by explicit-frame slot-count invariant |
| UMB-0881 | `B-invariant` | retired by explicit-frame slot-count invariant |
| UMB-0885 | `B-invariant` | retired by explicit-frame value/slot-count invariant |
| UMB-1209 | `B-invariant` | retired by raw/value primitive boundary contract before codegen |

## Fix Strategy Outline

- P7-B2.7 fixed frame/stackmap slot count, spill mirrors, primitive boundary payload, and NoGC restrictions as verifier/helper invariants before LLVM lowering.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-35-unsafe-nogc-boundary/`.
- U5-T03 coverage source: `tests/fixtures/umb_fix/_index.csv` rows with `bucket=B-35`; each row's `umb_ids` must exactly match the fixture `// COVERS:` header.
- Direct coverage requirement: at least one same-bucket fixture in this directory covers B-35 inventory ids from the Symptom table above.

## Open Questions

- No active B-35 blocker remains; primitive boundary payload is handled as a verifier/helper invariant in this bucket.
- All bucket/class/spec/gate values are concrete in the current inventory for this bucket.
