# B-21 - Struct literal / 字段层

本 bucket entry 数：3
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：P7-A2 retired FrontendReject rows；InternalBugSentinel rows remain active

## Symptom

- Active inventory entries: 3; 一级类 `B`; routes: `RawMirLlvm`=2, `Helper`=1.
- P7-A2 retired 3 FrontendReject rows (`UMB-0750`, `UMB-0863`, `UMB-0962`) after struct field frontend gates were confirmed.
- This table is copied from `audit/UMB_inventory.csv` and intentionally lists every active entry assigned to this bucket.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0864 | `crates/scoopc/src/llvm/codegen/main/expr_value.rs` | 213 | struct field value | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-1141 | `crates/scoopc/src/llvm/codegen/mir_body/member.rs` | 261 | pass MIR class member field index | `RawMirLlvm` | `rvalue` | `InternalBugSentinel` |
| UMB-1272 | `crates/scoopc/src/llvm/codegen/ty.rs` | 347 | struct field type | `Helper` | `type` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-0864 `crates/scoopc/src/llvm/codegen/main/expr_value.rs:213`

```text
  209 |             let raw = match materialized.ty {
  210 |                 CgTy::Unit => self.context.i8_type().const_int(0, false).into(),
  211 |                 _ => materialized
  212 |                     .value
> 213 |                     .ok_or(LlvmEmitError::UnsupportedMainBody {
  214 |                         kind: "struct field value",
  215 |                         at: field_span.into(),
```

#### Excerpt 2: UMB-1141 `crates/scoopc/src/llvm/codegen/mir_body/member.rs:261`

```text
  259 |             if receiver_cg == CgTy::Ref {
  260 |                 let field = class.fields.get(field_idx as usize).ok_or(
> 261 |                     LlvmEmitError::UnsupportedMainBody {
  262 |                         kind: "pass MIR class member field index",
  263 |                         at: span.into(),
```

#### Excerpt 3: UMB-1272 `crates/scoopc/src/llvm/codegen/ty.rs:347`

```text
  345 |     ) -> Result<CgTy, LlvmEmitError> {
  346 |         let Some(ty_fqn) = ty_fqn else {
> 347 |             return Err(LlvmEmitError::UnsupportedMainBody {
  348 |                 kind: "struct field type",
  349 |                 at: at.into(),
```

## Root Cause Hypothesis

- Active root cause: struct field value materialization, class member layout index drift, and struct field type publication still require verifier/internal contract coverage.
- Retired frontend root cause: unknown struct fields and missing required struct literal fields are now rejected by typecheck before LLVM codegen.
- Upstream gate recorded in inventory: `typecheck struct gate: field names, duplicates, and schema are valid`.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part2.md#51-struct` | 3 |
| `docs/spec/language_spec-part3.md#14-struct-literalclosure-与-do-消歧` | 3 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | P7-A2 retired 3 rows to `audit/UMB_retired.csv` |
| `InternalBugSentinel` | 3 | remains for later aggregate/member schema verifier task |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- P7-A2 replaced unknown/missing field fallbacks with upstream-gated `unreachable!` invariants.
- Later B-21 internal work must retire field value, class member field index, and struct field type sentinels through aggregate/member schema verifier coverage.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-21-struct-fields/`.
- P7-A2 active fixtures cover unknown fields, missing required fields, field type mismatch, and valid struct/with-update paths.
- Retired IDs are tracked in `audit/UMB_retired.csv`; active fixture `COVERS` fields reference only active inventory IDs.

## Open Questions

- Remaining rows overlap with aggregate schema and member layout verifier work and should be retired by the later internal contract task.
