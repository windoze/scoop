# B-21 - Struct literal / 字段层

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
状态：P7-A2 + P7-B2.3 retired

## Symptom

- P7-A2 retired 3 `FrontendReject` rows for unknown/missing struct fields.
- P7-B2.3 retired the remaining 3 `InternalBugSentinel` rows for struct field value materialization, class member field index drift, and struct field type publication.
- Active CSV no longer contains B-21 rows.

## Root Cause Closure

- Typecheck continues to own user-facing unknown/missing/invalid field diagnostics.
- Materialized MIR aggregate/member validation now checks struct field schema, value member target metadata, and pattern/aggregate handoff contracts before LLVM lowering.
- LLVM struct/member lowering now treats field schema drift as an internal invariant rather than emitting user-visible `UnsupportedMainBody` diagnostics.

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | 3 rows retired in P7-A2 |
| `InternalBugSentinel` | 0 | 3 rows retired in P7-B2.3 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-21-struct-fields/`.
- P7-A2 fixtures remain active; retired B-21 IDs live in `audit/UMB_retired.csv`.
- Cross-bucket fixture `COVERS` fields no longer reference retired B-21 IDs.

## Retired IDs

- P7-A2 `FrontendReject`: `UMB-0750`, `UMB-0863`, `UMB-0962`.
- P7-B2.3 `InternalBugSentinel`: `UMB-0864`, `UMB-1141`, `UMB-1272`.
