# B-26 - atomic intrinsic 系列

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.3 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.3 retired all 102 `InternalBugSentinel` rows (`UMB-0255`..`UMB-0273`, `UMB-0287`..`UMB-0295`, `UMB-0298`..`UMB-0314`, `UMB-0492`..`UMB-0540`, `UMB-0775`..`UMB-0780`, `UMB-0784`..`UMB-0785`).
- Retired routes: `Helper`=49, `EffectLoweredLlvm`=45, `RawMirLlvm`=8.

## Root Cause Hypothesis

- Retired root cause: atomic intrinsic lowering previously rechecked call shape, target addressability, target mutability, integer width, ref storage type, LLVM ordering, and return shape in LLVM codegen.
- Typecheck now owns user-facing atomic target addressability/writability where source facts are available; sysroot/type contracts own arity, argument, width, and generic bound validation.
- LLVM lowering now treats residual atomic drift as an internal verified intrinsic contract violation instead of a user-facing `UnsupportedMainBody` diagnostic.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part6.md#15-internal-atomic-value-types` | retired 102 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 102 rows in P7-B3.3 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- Raw atomic intrinsic call sites reject non-addressable first arguments in typecheck.
- Raw atomic store and compare-exchange reject non-writable local/top-level targets in typecheck and preserve class/struct storage invariants in LLVM as internal contracts.
- LLVM HIR and effect-lowered atomic paths use `expect_*`/verified intrinsic panic boundaries for impossible return, storage, ordering, and metadata drift.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-26-atomic-intrinsics/`.
- B-26 fixtures are active after P7-B3.3; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.
- Negative fixtures cover `AtomicValue<T>` bound rejection plus raw atomic target lvalue and mutability gates.

## Open Questions

- No active B-26 blocker remains. B-29 GC and B-30 unsafe/FunPtr intrinsic rows remain active for later tasks.
