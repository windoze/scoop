# B-27 - sync intrinsic 系列

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.2 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.2 retired all 58 `InternalBugSentinel` rows (`UMB-0246`..`UMB-0250`, `UMB-0656`..`UMB-0708`) after sync primitive arity, receiver, create/destroy, block, value, and return contracts were verified before LLVM lowering.

## Root Cause Hypothesis

- Retired root cause: sync primitive lowering previously rechecked receiver nominal/source type, runtime return value/type, destroy dispatch, and call shape in LLVM codegen.
- Typecheck and sysroot intrinsic contracts now own user-facing sync primitive validation, and LLVM lowering treats residual mismatches as internal verifier invariant failures rather than user diagnostics.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part6.md#18-与标准库的关系` | retired 58 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 58 rows in P7-B3.2 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- Typecheck owns sync primitive arity, named argument, receiver, block, and destroy nominal contracts.
- LLVM sync intrinsic lowering now uses verifier-backed internal invariants instead of `UnsupportedMainBody` diagnostics for this bucket.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-27-sync-intrinsics/`.
- B-27 fixtures are active after P7-B3.2; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.

## Open Questions

- No active B-27 blocker remains. B-10 effect-callable adapter rows in `effect_lowered/value.rs` remain active for P7-C5.
