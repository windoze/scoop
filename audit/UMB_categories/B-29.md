# B-29 - GC intrinsic 系列

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.4 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.4 retired all 93 `InternalBugSentinel` rows for `GC.pin`、`GC.unpin`、`GC.handleNew`、`GC.handleGet`、`GC.handleDrop`、GC root/frame reload、write barrier and named runtime GC-ref helpers.
- Retired routes: `Helper`=59, `RawMirLlvm`=24, `EffectLoweredLlvm`=10.

## Root Cause Hypothesis

- Retired root cause: GC intrinsic lowering previously rechecked pin/handle call shape, token/result type, root lifetime metadata, runtime return shape, raw handle field shape, explicit-frame root mirrors, deferred GC refs, write-barrier pointer payloads, and tagged-union GC payload fields inside LLVM codegen.
- Typecheck now owns user-facing `GC.*` signature and unsafe/NoGC gates; MIR production/materialized validation owns GC transport metadata, argument shape, subject type, result token, lifetime, and pairing contracts.
- LLVM lowering now treats residual GC drift as an internal verified intrinsic contract violation instead of a user-facing `UnsupportedMainBody` diagnostic.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part6.md#12-gc-pinning` | retired 93 |
| `docs/spec/language_spec-part6.md#13-stable-gc-handle` | retired 93 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 93 rows in P7-B3.4 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- `GC.pin` / `GC.handleNew` require one positional ref argument and publish `Pinned` / `GcHandle` token metadata before codegen.
- `GC.unpin` / `GC.handleGet` / `GC.handleDrop` require the corresponding token argument and validated root-lifetime/pairing metadata before codegen.
- LLVM HIR, MIR, effect-lowered, named intrinsic, frame reload, and write-barrier paths use `expect_*` / verified intrinsic panic boundaries for impossible internal drift.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-29-gc-intrinsics/`.
- B-29 fixtures are active after P7-B3.4; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.
- Negative fixture covers the frontend/typecheck GC intrinsic gate for non-ref `GC.pin` input.

## Open Questions

- No active B-29 blocker remains. B-30 unsafe/FunPtr/stackmap intrinsic rows remain active for later tasks.
