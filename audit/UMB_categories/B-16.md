# B-16 - 控制流 outside-of-context

本 bucket entry 数：7  
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：未分配 / body completed by U2-T02

## Symptom

- Inventory entries: 7; 一级类 `B`; routes: `RawMirLlvm`=7.
- This table is copied from `audit/UMB_inventory.csv` and intentionally lists every entry assigned to this bucket.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0187 | `crates/scoopc/src/llvm/codegen/control_flow.rs` | 2232 | break outside loop | `RawMirLlvm` | `terminator` | `FrontendReject` |
| UMB-0188 | `crates/scoopc/src/llvm/codegen/control_flow.rs` | 2243 | continue outside loop | `RawMirLlvm` | `terminator` | `FrontendReject` |
| UMB-0191 | `crates/scoopc/src/llvm/codegen/control_flow.rs` | 2346 | break outside loop | `RawMirLlvm` | `terminator` | `FrontendReject` |
| UMB-0192 | `crates/scoopc/src/llvm/codegen/control_flow.rs` | 2358 | continue outside loop | `RawMirLlvm` | `terminator` | `FrontendReject` |
| UMB-0786 | `crates/scoopc/src/llvm/codegen/main/call.rs` | 706 | return outside function with return context | `RawMirLlvm` | `terminator` | `FrontendReject` |
| UMB-1263 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 223 | break outside loop | `RawMirLlvm` | `terminator` | `FrontendReject` |
| UMB-1264 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 235 | continue outside loop | `RawMirLlvm` | `terminator` | `FrontendReject` |

### Representative Source Excerpts

#### Excerpt 1: UMB-0187 `crates/scoopc/src/llvm/codegen/control_flow.rs:2232`

Why representative: represents route `RawMirLlvm`, surface `terminator`, kind `break outside loop`; file frequency in this bucket is 4 and kind frequency is 3.

```text
  2222 |                     self.function_cx.env.pop_scope();
  2223 |                     return Ok(out);
  2224 |                 }
  2225 |                 hir::StmtKind::While { cond, body } => {
  2226 |                     self.codegen_while_stmt(stmt.span, cond, body)?;
  2227 |                     tail_value = None;
  2228 |                 }
  2229 |                 // T0141: break/continue in function-level block (inside a loop).
  2230 |                 hir::StmtKind::Break { break_span } => {
  2231 |                     let loop_ctx = self.function_cx.loop_context_stack.last().ok_or(
> 2232 |                         LlvmEmitError::UnsupportedMainBody {
  2233 |                             kind: "break outside loop",
  2234 |                             at: (*break_span).into(),
  2235 |                         },
  2236 |                     )?;
  2237 |                     self.builder.build_unconditional_branch(loop_ctx.break_bb)?;
  2238 |                     self.function_cx.env.pop_scope();
  2239 |                     return self.default_value(*break_span, declared_return_ty);
  2240 |                 }
  2241 |                 hir::StmtKind::Continue { continue_span } => {
  2242 |                     let loop_ctx = self.function_cx.loop_context_stack.last().ok_or(
```

#### Excerpt 2: UMB-1263 `crates/scoopc/src/llvm/codegen/stmt.rs:223`

Why representative: represents route `RawMirLlvm`, surface `terminator`, kind `break outside loop`; file frequency in this bucket is 2 and kind frequency is 3.

```text
  213 |                 // T0141: return inside loop body — branch to the function return BB.
  214 |                 hir::StmtKind::Return { value } => {
  215 |                     self.codegen_early_return(stmt.span, value.as_ref())?;
  216 |                     // After an unconditional branch, stop processing further stmts.
  217 |                     self.function_cx.env.pop_scope();
  218 |                     return Ok(());
  219 |                 }
  220 |                 // T0141: break — branch to the innermost loop's after-BB.
  221 |                 hir::StmtKind::Break { break_span } => {
  222 |                     let loop_ctx = self.function_cx.loop_context_stack.last().ok_or(
> 223 |                         LlvmEmitError::UnsupportedMainBody {
  224 |                             kind: "break outside loop",
  225 |                             at: (*break_span).into(),
  226 |                         },
  227 |                     )?;
  228 |                     self.builder.build_unconditional_branch(loop_ctx.break_bb)?;
  229 |                     self.function_cx.env.pop_scope();
  230 |                     return Ok(());
  231 |                 }
  232 |                 // T0141: continue — branch to the innermost loop's cond-BB.
  233 |                 hir::StmtKind::Continue { continue_span } => {
```

#### Excerpt 3: UMB-0188 `crates/scoopc/src/llvm/codegen/control_flow.rs:2243`

Why representative: represents route `RawMirLlvm`, surface `terminator`, kind `continue outside loop`; file frequency in this bucket is 4 and kind frequency is 3.

```text
  2233 |                             kind: "break outside loop",
  2234 |                             at: (*break_span).into(),
  2235 |                         },
  2236 |                     )?;
  2237 |                     self.builder.build_unconditional_branch(loop_ctx.break_bb)?;
  2238 |                     self.function_cx.env.pop_scope();
  2239 |                     return self.default_value(*break_span, declared_return_ty);
  2240 |                 }
  2241 |                 hir::StmtKind::Continue { continue_span } => {
  2242 |                     let loop_ctx = self.function_cx.loop_context_stack.last().ok_or(
> 2243 |                         LlvmEmitError::UnsupportedMainBody {
  2244 |                             kind: "continue outside loop",
  2245 |                             at: (*continue_span).into(),
  2246 |                         },
  2247 |                     )?;
  2248 |                     self.builder
  2249 |                         .build_unconditional_branch(loop_ctx.continue_bb)?;
  2250 |                     self.function_cx.env.pop_scope();
  2251 |                     return self.default_value(*continue_span, declared_return_ty);
  2252 |                 }
  2253 |                 hir::StmtKind::Todo(_) => {
```

## Root Cause Hypothesis

- Root cause: break/continue/return 仍可能在缺失 loop/function context 的情况下到达 LLVM lowering，说明 parser/HIR control-flow context gate 没有完全拒绝非法上下文。
- Upstream gate(s) recorded in inventory:
- `parser/HIR control-flow gate: break/continue/return context is valid` (7 entries).
- Dominant files: `crates/scoopc/src/llvm/codegen/control_flow.rs` (4), `crates/scoopc/src/llvm/codegen/stmt.rs` (2), `crates/scoopc/src/llvm/codegen/main/call.rs` (1).
- Dominant kind labels: `break outside loop` (3), `continue outside loop` (3), `return outside function with return context` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part3.md#94-return--break--continue` | 7 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 7 | from current inventory expected_class |
| `InternalBugSentinel` | 0 | none in this bucket |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

- Post-fix planning counts sum to 7, matching the bucket entry count 7.
- Current CSV expected_class distribution: `FrontendReject`=7.

## Fix Strategy Outline

- U4 应将 break/continue/return context 检查固定在 parser/HIR/typecheck 阶段，并用 negative fixture 锁定用户可见诊断。
- Detailed landing steps, exact verifier names and fixture activation state remain owned by U4-T01/U5; this document fixes the U2 root-cause input and classification boundary.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-16-control-flow-context/`.
- U5-T03 coverage source: `tests/fixtures/umb_fix/_index.csv` rows with `bucket=B-16`; each row's `umb_ids` must exactly match the fixture `// COVERS:` header.
- Direct coverage requirement: at least one same-bucket fixture in this directory covers B-16 inventory ids from the Symptom table above.

## Open Questions

- 无缺失 gate；U5 negative fixture 必须验证 frontend reject 文案不提 LLVM/codegen。
- All bucket/class/spec/gate values are concrete in the current inventory for this bucket.
