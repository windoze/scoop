# B-01 - inkwell builder bookkeeping

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：P7-B1 / retired

## Symptom

- Active inventory entries: 0; P7-B1 retired the initial 71 `InternalBugSentinel` rows.
- Retired IDs are recorded in `audit/UMB_retired.csv` with `retired_by=P7-B1`.
- The original rows covered LLVM builder insertion block, parent/current function, entry block, and related helper-only invariants.

## Root Cause Hypothesis

- Root cause: LLVM helper code read builder insertion context in many call sites and used `LlvmEmitError::UnsupportedMainBody` as the internal invariant boundary.
- P7-B1 moved that boundary into shared helper APIs on `MainCodegen`: `expect_insert_block`, `expect_parent_function`, `expect_current_function`, `expect_entry_block`, `expect_basic_value`, and instruction-parent variants for explicit frame storage.
- User input cannot directly construct this state; reaching these helpers means compiler insertion-context bookkeeping drifted.

## Spec Linkage

- `N/A:helper-invariant` applied to the retired rows because this bucket guards internal LLVM builder state rather than a user-visible language feature.

| Spec anchor | Entries |
|---|---:|
| `N/A:helper-invariant` | 0 active; 71 retired |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | P7-B1 retired all 71 active rows to the ledger |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- P7-B1 removed all B-01 `UnsupportedMainBody` constructors and replaced them with centralized helper panic boundaries.
- Future B buckets should reuse the helper style instead of introducing new builder-context fallbacks.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-01-builder-invariant/`.
- Sentinel coverage record: `tests/fixtures/umb_fix/B-01-builder-invariant/_README.md` lists the 71 retired B-01 IDs.
- Rust sentinel anchor: `crates/scoopc/src/audit/sentinel_tests.rs::umb_fix_helper_invariant_sentinel_tests_present`.

## Open Questions

- No active B-01 questions remain after P7-B1.
