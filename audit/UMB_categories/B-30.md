# B-30 - named / unsafe / FunPtr intrinsic

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.5 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.5 retired all 117 `InternalBugSentinel` rows for named intrinsic metadata/runtime calls, unsafe value helpers, builtin scalar/String helpers, `FunPtr` calls, pointer conversions, and stackmap smoke helpers.
- Retired routes: `Helper`=102, `RawMirLlvm`=11, `EffectLoweredLlvm`=4.

## Root Cause Hypothesis

- Retired root cause: named/unsafe intrinsic lowering, `FunPtr` call lowering, `UIntPtr`/function-pointer conversion, and stackmap smoke helpers previously rechecked arity, signature, receiver, value payload, runtime metadata, return type, and statepoint caller shape inside LLVM codegen.
- Typecheck and generic replay gates now enforce `FunPtr<F>` signature purity/native ABI support and unsafe call context; materialized MIR validation enforces FunPtr callee, argument, and return contracts before raw MIR LLVM lowering.
- LLVM lowering now treats residual B-30 drift as an internal verified intrinsic/FunPtr/stackmap contract violation instead of a user-facing `UnsupportedMainBody` diagnostic.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part6.md#6-uintptr-与指针整数转换` | retired 117 |
| `docs/spec/language_spec-part6.md#7-function-pointer` | retired 117 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 117 rows in P7-B3.5 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- Named intrinsic HIR/MIR lowering requires known IR/runtime metadata, validated operand arity, concrete operand payloads, and runtime result shape before LLVM lowering.
- `FunPtr` HIR/MIR calls require a validated pure function signature, positional/native ABI arguments, integer token payload, and deferred return slot contract before LLVM lowering.
- Stackmap smoke helpers require an empty argument list, active caller function, and integer runtime return contract before LLVM lowering.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-30-named-unsafe-funptr/`.
- B-30 fixtures are active after P7-B3.5; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.
- Negative fixture covers the frontend/typecheck `FunPtr` unsafe-call/signature gate.

## Open Questions

- No active B-30 blocker remains. P7-B `InternalBugSentinel` is now fully retired; remaining active rows are P7-C `RealImpl` buckets B-10/B-12/B-13/B-24/B-25.
