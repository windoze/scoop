# B-11 - Pure / plain statement 边界路由

本 bucket entry 数：0  
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：P7-B2.8 / retired

## Symptom

- Active inventory entries: 0.
- P7-B2.8 retired all 14 InternalBugSentinel rows (`UMB-1250`..`UMB-1262`, `UMB-1268`) after typed HIR/MIR statement boundary contracts were verified before LLVM lowering.

## Root Cause Hypothesis

- Retired root cause: plain statement lowering could previously receive anonymous local declarations, missing initializers, unresolved/immutable assignment targets, unsupported assignment left-hand sides, or non-Bool while values and report backend fallback diagnostics.
- The typed HIR completeness verifier now rejects or verifies these statement route contracts before codegen; MIR lowering consumes assignment place contracts for local, top-level, and member stores.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part3.md#1-表达式与语句` | retired 14 |
| `docs/spec/language_spec-part4.md#3-调用效果` | retired 14 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 14 rows in P7-B2.8 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- HIR completeness owns local val symbols/initializers, assignment place contract consistency, mutable target checks, and Bool while condition checks.
- LLVM HIR statement lowering now uses verifier-backed internal invariants instead of `UnsupportedMainBody` diagnostics.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-11-pure-boundary/`.
- B-11 fixtures are active after P7-B2.8; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.

## Open Questions

- No active B-11 blocker remains. B-10 effect adapter IDs referenced by cross-bucket fixtures remain active for the later RealImpl task.
