# B-11 - Pure / plain statement 边界路由

本 bucket entry 数：14  
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：未分配 / body completed by U2-T02

## Symptom

- Inventory entries: 14; 一级类 `B`; routes: `RawMirLlvm`=14.
- This table is copied from `audit/UMB_inventory.csv` and intentionally lists every entry assigned to this bucket.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-1250 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 8 | anonymous val binding | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1251 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 16 | val type | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1252 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 24 | val without initializer | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1253 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 64 | unknown local value | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1254 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 71 | assignment to immutable local | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1255 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 96 | assignment to immutable extern global | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1256 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 103 | extern global type | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1257 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 115 | assignment to non-local | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1258 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 123 | top-level var type | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1259 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 137 | assignment lhs member target | `RawMirLlvm` | `terminator` | `InternalBugSentinel` |
| UMB-1260 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 166 | assignment lhs | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1261 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 173 | assignment to immutable class field | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1262 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 190 | assignment lhs | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |
| UMB-1268 | `crates/scoopc/src/llvm/codegen/stmt.rs` | 286 | while cond value | `RawMirLlvm` | `stmt` | `InternalBugSentinel` |

### Representative Source Excerpts

#### Excerpt 1: UMB-1260 `crates/scoopc/src/llvm/codegen/stmt.rs:166`

Why representative: represents route `RawMirLlvm`, surface `stmt`, kind `assignment lhs`; file frequency in this bucket is 14 and kind frequency is 2.

```text
  156 |                 };
  157 | 
  158 |                 let Some(field_place) = self.defer_class_field_place(
  159 |                     receiver,
  160 |                     member.span,
  161 |                     fqn,
  162 |                     receiver_hir_ty,
  163 |                     "assign_class_field",
  164 |                 )?
  165 |                 else {
> 166 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  167 |                         kind: "assignment lhs",
  168 |                         at: lhs.span.into(),
  169 |                     });
  170 |                 };
  171 | 
  172 |                 if !field_place.writable {
  173 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  174 |                         kind: "assignment to immutable class field",
  175 |                         at: eq_span.into(),
  176 |                     });
```

#### Excerpt 2: UMB-1250 `crates/scoopc/src/llvm/codegen/stmt.rs:8`

Why representative: represents route `RawMirLlvm`, surface `stmt`, kind `anonymous val binding`; file frequency in this bucket is 14 and kind frequency is 1.

```text
   1 | //! 语句 codegen（T0102d：从 `codegen/mod.rs` 拆分）。
   2 | 
   3 | use super::*;
   4 | 
   5 | impl<'a, 'ctx> MainCodegen<'a, 'ctx> {
   6 |     pub(super) fn codegen_val_decl(&mut self, decl: &hir::ValDecl) -> Result<(), LlvmEmitError> {
   7 |         let Some(id) = decl.id else {
>  8 |             return Err(LlvmEmitError::UnsupportedMainBody {
   9 |                 kind: "anonymous val binding",
  10 |                 at: decl.span.into(),
  11 |             });
  12 |         };
  13 | 
  14 |         let target_ty = self
  15 |             .cg_ty_of(decl.ty)
  16 |             .ok_or(LlvmEmitError::UnsupportedMainBody {
  17 |                 kind: "val type",
  18 |                 at: decl.span.into(),
```

#### Excerpt 3: UMB-1259 `crates/scoopc/src/llvm/codegen/stmt.rs:137`

Why representative: represents route `RawMirLlvm`, surface `terminator`, kind `assignment lhs member target`; file frequency in this bucket is 14 and kind frequency is 1.

```text
  127 | 
  128 |                     let gv = self.declare_top_level_var_global(var)?;
  129 |                     let rhs_v = self.codegen_expr_in_expected_context(rhs, Some(cg_ty))?;
  130 |                     let _stored =
  131 |                         self.store_local_value(eq_span, gv.as_pointer_value(), cg_ty, rhs_v)?;
  132 |                     Ok(())
  133 |                 }
  134 |             },
  135 |             hir::ExprKind::MemberAccess { receiver, member } => {
  136 |                 let Some(hir::MemberRef::Value { fqn, .. }) = member.resolved.as_ref() else {
> 137 |                     return Err(LlvmEmitError::UnsupportedMainBody {
  138 |                         kind: "assignment lhs member target",
  139 |                         at: lhs.span.into(),
  140 |                     });
  141 |                 };
  142 | 
  143 |                 // T0125：同 codegen_member_access，使用局部变量的 hir_ty 获取精确泛型类型。
  144 |                 let receiver_hir_ty = if let hir::ExprKind::VarRef(hir::ValueRef::Local {
  145 |                     id,
  146 |                     ..
  147 |                 }) = &receiver.kind
```

## Root Cause Hypothesis

- Root cause: plain statement lowering 仍可能收到 effect-only、anonymous val、assignment lhs 或 unknown local 等不应进入该路径的节点，说明 HIR/MIR boundary 对 pure/plain statement 的路由不完整。
- Upstream gate(s) recorded in inventory:
- `HIR/MIR boundary gate: pure/plain statements are routed before LLVM lowering` (14 entries).
- Dominant files: `crates/scoopc/src/llvm/codegen/stmt.rs` (14).
- Dominant kind labels: `assignment lhs` (2), `anonymous val binding` (1), `val type` (1), `val without initializer` (1), `unknown local value` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part3.md#1-表达式与语句` | 14 |
| `docs/spec/language_spec-part4.md#3-调用效果` | 14 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 14 | from current inventory expected_class |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

- Post-fix planning counts sum to 14, matching the bucket entry count 14.
- Current CSV expected_class distribution: `InternalBugSentinel`=14.

## Fix Strategy Outline

- U4 应把 statement kind routing 前置到 HIR/MIR boundary verifier，保证 plain LLVM statement path 只接收已 materialize 的纯语句。
- Detailed landing steps, exact verifier names and fixture activation state remain owned by U4-T01/U5; this document fixes the U2 root-cause input and classification boundary.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-11-pure-boundary/`.
- U5-T03 coverage source: `tests/fixtures/umb_fix/_index.csv` rows with `bucket=B-11`; each row's `umb_ids` must exactly match the fixture `// COVERS:` header.
- Direct coverage requirement: at least one same-bucket fixture in this directory covers B-11 inventory ids from the Symptom table above.

## Open Questions

- 无缺失 gate；U4 需决定 effect statement fallback 是否应归 B-10 effect adapter。
- All bucket/class/spec/gate values are concrete in the current inventory for this bucket.
