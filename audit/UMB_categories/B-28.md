# B-28 - thread intrinsic 系列

本 bucket entry 数：0
inventory 来源：audit/UMB_inventory.csv
生成时间：2026-05-18
负责人/状态：P7-B3.2 / retired

## Symptom

- Active inventory entries: 0.
- P7-B3.2 retired all 20 `InternalBugSentinel` rows (`UMB-0243`..`UMB-0245`, `UMB-0725`..`UMB-0738`, `UMB-0740`..`UMB-0742`) after thread primitive arity, receiver, callable block, value, and return contracts were verified before LLVM lowering.

## Root Cause Hypothesis

- Retired root cause: thread primitive lowering previously rechecked `threadSpawn`, `Thread.join`, `sleepMillis`, and `currentId` call shape and runtime return type in LLVM codegen.
- Typecheck and sysroot intrinsic contracts now own user-facing thread primitive validation, and LLVM lowering treats residual mismatches as internal verifier invariant failures rather than user diagnostics.

## Spec Linkage

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part6.md#18-与标准库的关系` | retired 20 |

## Expected Post-Fix Class

| Class | Active Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | retired 20 rows in P7-B3.2 |
| `RealImpl` | 0 | none in this bucket |
| `D-pending` | 0 | none in this bucket |

## Fix Strategy Outline

- Typecheck owns thread primitive arity, named argument, callable block, receiver, and return contracts.
- LLVM thread intrinsic lowering now uses verifier-backed internal invariants instead of `UnsupportedMainBody` diagnostics for this bucket.

## Fixture Set Pointer

- Fixture directory: `tests/fixtures/umb_fix/B-28-thread-intrinsics/`.
- B-28 fixtures are active after P7-B3.2; retired IDs are tracked in `audit/UMB_retired.csv` rather than fixture `COVERS` fields.

## Open Questions

- No active B-28 blocker remains. Closure ABI rows for captured/non-scalar closures remain owned by B-12.
