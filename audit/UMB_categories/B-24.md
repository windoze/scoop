# B-24 - Reflection / static metadata intrinsic

本 bucket entry 数：0
retired entry 数：6
inventory 来源：audit/UMB_inventory.csv  
生成时间：2026-05-18  
负责人/状态：P7-C1 / retired

## Symptom

- Active inventory entries: 0; retired ledger covers all 6 initial `RealImpl` entries.
- Historical entries are retained below for traceability; live coverage is now in `audit/UMB_retired.csv`.

| id | file | line | kind | route | surface | expected_class |
|---|---|---:|---|---|---|---|
| UMB-0568 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 326 | sizeOf() arity mismatch | `Helper` | `intrinsic` | `RealImpl` |
| UMB-0569 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 334 | sizeOf() arg type | `Helper` | `intrinsic` | `RealImpl` |
| UMB-0571 | `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` | 429 | reflection intrinsic call binding | `Helper` | `intrinsic` | `RealImpl` |
| UMB-0954 | `crates/scoopc/src/llvm/codegen/mir_body/aggregates.rs` | 101 | pass MIR sizeOf arg type | `RawMirLlvm` | `type` | `RealImpl` |
| UMB-0956 | `crates/scoopc/src/llvm/codegen/mir_body/aggregates.rs` | 160 | pass MIR kindOf arg type | `RawMirLlvm` | `type` | `RealImpl` |
| UMB-0957 | `crates/scoopc/src/llvm/codegen/mir_body/aggregates.rs` | 201 | pass MIR descOf arg type | `RawMirLlvm` | `type` | `RealImpl` |

### Representative Source Excerpts

#### Excerpt 1: UMB-0568 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:326`

Why representative: represents route `Helper`, surface `intrinsic`, kind `sizeOf() arity mismatch`; file frequency in this bucket is 3 and kind frequency is 1.

```text
  316 |     }
  317 | 
  318 |     pub(in crate::llvm::codegen) fn codegen_sysroot_size_of(
  319 |         &mut self,
  320 |         span: crate::span::Span,
  321 |         callee_span: crate::span::Span,
  322 |         args: &[hir::CallArg],
  323 |     ) -> Result<CgValue<'ctx>, LlvmEmitError> {
  324 |         // 语义：`sizeOf(x)` 在 HIR-compatible path 返回静态类型的目标 ABI store size。
  325 |         let [hir::CallArg::Positional(expr)] = args else {
> 326 |             return Err(LlvmEmitError::UnsupportedMainBody {
  327 |                 kind: "sizeOf() arity mismatch",
  328 |                 at: span.into(),
  329 |             });
  330 |         };
  331 | 
  332 |         let arg_cg = self
  333 |             .cg_ty_of(expr.ty)
  334 |             .ok_or(LlvmEmitError::UnsupportedMainBody {
  335 |                 kind: "sizeOf() arg type",
  336 |                 at: callee_span.into(),
```

#### Excerpt 2: UMB-0954 `crates/scoopc/src/llvm/codegen/mir_body/aggregates.rs:101`

Why representative: represents route `RawMirLlvm`, surface `type`, kind `pass MIR sizeOf arg type`; file frequency in this bucket is 3 and kind frequency is 1.

```text
   91 |         })
   92 |     }
   93 | 
   94 |     pub(in crate::llvm::codegen) fn codegen_mir_size_of(
   95 |         &mut self,
   96 |         span: crate::span::Span,
   97 |         mir_types: &TypeStore,
   98 |         value_ty: TypeId,
   99 |     ) -> Result<CgValue<'ctx>, LlvmEmitError> {
  100 |         let arg_cg = self.cg_ty_of_mir_type(mir_types, value_ty).ok_or(
> 101 |             LlvmEmitError::UnsupportedMainBody {
  102 |                 kind: "pass MIR sizeOf arg type",
  103 |                 at: span.into(),
  104 |             },
  105 |         )?;
  106 |         let llvm_ty = self.llvm_basic_type_of(span, arg_cg)?;
  107 |         let bytes = self.store_size_bytes_of_basic_type(llvm_ty);
  108 |         let value_word = IntTy {
  109 |             bits: self.host.word_bit_width(),
  110 |             signed: true,
  111 |         };
```

#### Excerpt 3: UMB-0569 `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs:334`

Why representative: represents route `Helper`, surface `intrinsic`, kind `sizeOf() arg type`; file frequency in this bucket is 3 and kind frequency is 1.

```text
  324 |         // 语义：`sizeOf(x)` 在 HIR-compatible path 返回静态类型的目标 ABI store size。
  325 |         let [hir::CallArg::Positional(expr)] = args else {
  326 |             return Err(LlvmEmitError::UnsupportedMainBody {
  327 |                 kind: "sizeOf() arity mismatch",
  328 |                 at: span.into(),
  329 |             });
  330 |         };
  331 | 
  332 |         let arg_cg = self
  333 |             .cg_ty_of(expr.ty)
> 334 |             .ok_or(LlvmEmitError::UnsupportedMainBody {
  335 |                 kind: "sizeOf() arg type",
  336 |                 at: callee_span.into(),
  337 |             })?;
  338 |         let llvm_ty = self.llvm_basic_type_of(expr.span, arg_cg)?;
  339 |         let bytes = self.store_size_bytes_of_basic_type(llvm_ty);
  340 | 
  341 |         let value_word = IntTy {
  342 |             bits: self.host.word_bit_width(),
  343 |             signed: true,
  344 |         };
```

## Root Cause Hypothesis

- Root cause: sizeOf/kindOf/descOf 等静态反射 intrinsic 与 MIR aggregate path 曾缺少完整 static metadata lowering；当前 entry 均已退休，D 类仅作为 spec 沉默风险保留。
- Upstream gate(s) recorded in inventory:
- `reflection/static-metadata contract: static metadata intrinsics have supported lowering` (6 historical entries; initial CSV keeps original wording for traceability).
- Dominant files: `crates/scoopc/src/llvm/codegen/intrinsics/builtin.rs` (3), `crates/scoopc/src/llvm/codegen/mir_body/aggregates.rs` (3).
- Dominant kind labels: `sizeOf() arity mismatch` (1), `sizeOf() arg type` (1), `reflection intrinsic call binding` (1), `pass MIR sizeOf arg type` (1), `pass MIR kindOf arg type` (1).

## Spec Linkage

- All non-helper entries have explicit spec anchors in inventory; counts below include repeated per-entry linkage.

| Spec anchor | Entries |
|---|---:|
| `docs/spec/language_spec-part5.md#2-静态反射-intrinsic` | 6 |
| `docs/spec/language_spec-part5.md#1-静态反射总览` | 6 |

## Expected Post-Fix Class

| Class | Count | Note |
|---|---:|---|
| `FrontendReject` | 0 | none in this bucket |
| `InternalBugSentinel` | 0 | none in this bucket |
| `RealImpl` | 0 | all initial entries retired by P7-C1 |
| `D-pending` | 0 | none in this bucket |

- Retired ledger distribution: `RealImpl`=6.
- Current active CSV distribution: empty for B-24.

### Entry-Level Cross-Class Split

| id | entry_class | rationale |
|---|---|---|
| UMB-0568 | `C-real-impl` | current entries are supported reflection/static-metadata lowering work |
| UMB-0569 | `C-real-impl` | current entries are supported reflection/static-metadata lowering work |
| UMB-0571 | `C-real-impl` | current entries are supported reflection/static-metadata lowering work |
| UMB-0954 | `C-real-impl` | current entries are supported reflection/static-metadata lowering work |
| UMB-0956 | `C-real-impl` | current entries are supported reflection/static-metadata lowering work |
| UMB-0957 | `C-real-impl` | current entries are supported reflection/static-metadata lowering work |

## Fix Strategy Outline

- P7-C1 implemented supported `sizeOf<T>()`, `kindOf<T>()`, and `descOf<T>()` lowering for HIR/MIR reflection paths.
- `sizeOf` legacy value overloads still consume the value expression's static type without evaluating the value.
- Drift in typed call-site metadata or MIR type lowering now reaches verified intrinsic contract panic boundaries rather than user-visible UMB diagnostics.

## Fixture Set Pointer

- Fixture directory: retired; current coverage uses ordinary reflection and splice fixtures.
- U5-T03/P7-C1 coverage source: `tests/fixtures/umb_fix/_index.csv` rows with `bucket=B-24`; retired ids are covered by `audit/UMB_retired.csv`, so active fixture `// COVERS:` headers use `NONE`.
- `tests/fixtures/run-pass/reflection_kind_desc_basic.scoop` exercises runtime codegen for `sizeOf`/`kindOf`/`descOf`.

## Open Questions

- No active B-24 blocker remains.
