# B-10 修复策略

Bucket: Effect-typed callable adapter / ABI routing  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 109  
Primary class: C / real implementation

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `mir.materialize` |
| Contract form | Effect callable adapters, continuation carriers, resume payloads, and effect ABI routing are implemented. |
| Primary upstream_gate | `effect lowering contract: callable adapter and effect ABI routing are implemented` |
| Gate coverage | 109/109 entries already carry this single gate in CSV; all entries are `RealImpl`. |

## 落地路径

- C. real implementation: implement the adapter and ABI routing paths instead of converting these sites to frontend rejects.
- Minimum happy-path fixture: `tests/fixtures/umb_fix/B-10-effect-callable-adapter/pos_effect_callable_adapter.scoop`; it must be `IGNORE-UNTIL-FIX:B-10` until P7 implements the path.
- Negative fixtures still validate effect-row and handler-contract gates, but they do not replace the required real implementation.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-10-effect-callable-adapter/pos_effect_callable_adapter.scoop`, `tests/fixtures/umb_fix/B-10-effect-callable-adapter/pos_handler_resume_callable_adapter.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-10-effect-callable-adapter/neg_effect_row_gate.scoop`, `tests/fixtures/umb_fix/B-10-effect-callable-adapter/neg_handler_resume_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_fix_fixture_index_in_sync`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target: B-10 inventory count decreases by 109 when ignored happy-path fixtures become active and pass.
