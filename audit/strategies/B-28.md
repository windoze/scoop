# B-28 修复策略

Bucket: thread intrinsic 系列  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 20  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Thread primitive calls match sysroot/runtime arity, named args, receiver, and return contracts. |
| Primary upstream_gate | `intrinsic resolver gate: thread primitive calls match sysroot runtime contract` |
| Gate coverage | 20/20 entries already carry this single gate in CSV; each B-28 entry has one primary gate. |

## 落地路径

- B. upstream contract: intrinsic resolver/typecheck rejects invalid `thread.*` calls and materialization publishes supported values.
- Thread surfaces that remain intentionally unsupported must move to D-class before codegen changes; do not hide them under B-28.
- P7 replaces thread intrinsic sentinels after the resolver gate is active.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-28-thread-intrinsics/pos_thread_intrinsics.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-28-thread-intrinsics/neg_thread_intrinsic_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-28 inventory count decreases by 20 and thread intrinsic diagnostics remain stable.
