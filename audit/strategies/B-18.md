# B-18 修复策略

Bucket: 字面量与字符串  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 4  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` |
| Contract form | Literal backing source, target type, and string materialization are valid before codegen. |
| Primary upstream_gate | `parser/typecheck literal gate: literal target type and backing value are valid` |
| Gate coverage | 4/4 entries already carry this single gate in CSV; each B-18 entry has one primary gate. |

## 落地路径

- B. upstream contract: parse/typecheck should reject malformed literal forms and unresolved target types before codegen.
- Codegen literal lowering should consume source-backed literal slices and concrete scalar/string target types only.
- P7 replaces B-18 sentinels with typed assertions after literal fixtures lock the gate.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-18-literals-strings/pos_lexical_tokens_comments.scoop`, `tests/fixtures/umb_fix/B-18-literals-strings/pos_int_float_char_string_literals.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-18-literals-strings/neg_invalid_lexical_token.scoop`, `tests/fixtures/umb_fix/B-18-literals-strings/neg_literal_parse_or_type_error.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-18 inventory count decreases by 4 and literal diagnostics remain frontend-owned.
