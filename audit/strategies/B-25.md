# B-25 修复策略

Bucket: Platform / RTTI intrinsic  
Inventory: `audit/UMB_inventory.csv`  
Active entry count: 0
Retired entry count: 14
Primary class: B/C, RealImpl retired by P7-C2

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `mir.materialize` |
| Contract form | Platform and runtime type metadata are available for `is`/`as?`, RTTI descriptors, and transport. |
| Primary upstream_gate | `platform/RTTI contract: platform and runtime type metadata are available` |
| Gate coverage | 14/14 initial entries carried this single gate; all are now retired in `audit/UMB_retired.csv`. |

## 落地路径

- C. real implementation completed in P7-C2: runtime type checks/casts/patterns lower through real type descriptors and `getPlatform()` materializes a real `Platform` value from the target triple.
- Happy-path fixture `tests/fixtures/umb_fix/B-25-platform-rtti/pos_platform_rtti_runtime.scoop` is active and covers `getPlatform()`, `is`, `!is`, `as?`, and `is` pattern lowering.
- Malformed RTTI metadata remains a B-class negative gate through frontend/typecheck validation, not a substitute for valid runtime implementation.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-25-platform-rtti/pos_platform_rtti_runtime.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-25-platform-rtti/neg_platform_rtti_metadata_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_fixture_index_in_sync`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement result: B-25 active inventory count is 0; the 14 initial ids are covered by `audit/UMB_retired.csv`.
