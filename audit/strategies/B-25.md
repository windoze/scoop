# B-25 修复策略

Bucket: Platform / RTTI intrinsic  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 14  
Primary class: B/C, currently RealImpl

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `mir.materialize` |
| Contract form | Platform and runtime type metadata are available for `is`/`as?`, RTTI descriptors, and transport. |
| Primary upstream_gate | `platform/RTTI contract: platform and runtime type metadata are available` |
| Gate coverage | 14/14 entries already carry this single gate in CSV; all entries are currently `RealImpl`. |

## 落地路径

- C. real implementation: materialize runtime type descriptors and platform metadata needed by casts and transport.
- Minimum happy-path fixture: `tests/fixtures/umb_fix/B-25-platform-rtti/pos_platform_rtti_runtime.scoop`; it must be `IGNORE-UNTIL-FIX:B-25` until P7 implements the path.
- Keep malformed RTTI metadata as a B-class negative gate, not a substitute for the valid runtime implementation.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-25-platform-rtti/pos_platform_rtti_runtime.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-25-platform-rtti/neg_platform_rtti_metadata_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_fixture_index_in_sync`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target: B-25 inventory count decreases by 14 when platform/RTTI happy paths become active and pass.
