# B-11 修复策略

Bucket: Pure / plain statement 边界路由  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 14 retired in P7-B2.8  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `mir` |
| Contract form | Pure/plain statements are routed before LLVM lowering and illegal boundary crossings are rejected. |
| Primary upstream_gate | `HIR/MIR boundary gate: pure/plain statements are routed before LLVM lowering` |
| Gate coverage | 0 active entries remain; retired ledger covers all 14 B-11 rows. |

## 落地路径

- B. upstream contract: HIR/MIR routing distinguishes expression values, declarations, assignments, and statement boundaries before codegen.
- Typed HIR completeness verifies local val symbol/init shape, assignment place contract consistency, mutable target contracts, and Bool while conditions.
- P7-B2.8 removed B-11 sentinels after boundary routes became explicit and covered.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-11-pure-boundary/pos_plain_statement_boundary.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-11-pure-boundary/neg_statement_boundary_route.scoop`, `tests/fixtures/umb_fix/B-11-pure-boundary/neg_plain_effect_boundary.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B2.8 retirement target met: B-11 inventory count decreased by 14 and pure/plain boundary diagnostics trigger before LLVM lowering.

## P7 完成记录

- P7-B2.8 retired all 14 B-11 InternalBugSentinel rows through typed HIR statement boundary verification and LLVM internal invariants.
