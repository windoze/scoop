# B-16 修复策略

Bucket: 控制流 outside-of-context  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 7  
Primary class: B / frontend reject

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `typecheck` |
| Contract form | Break, continue, and return context validity is resolved before MIR/LLVM lowering. |
| Primary upstream_gate | `parser/HIR control-flow gate: break/continue/return context is valid` |
| Gate coverage | 7/7 entries already carry this single gate in CSV; each B-16 entry has one primary gate. |

## 落地路径

- B. upstream contract: HIR/typecheck rejects `break`, `continue`, and `return` forms outside their valid context.
- Codegen control-flow lowering should assume context-checked statements and keep only impossible-state assertions.
- P7 removes B-16 sentinels after frontend diagnostics are locked by negative fixtures.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-16-control-flow-context/pos_return_break_continue_context.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-16-control-flow-context/neg_control_flow_context.scoop`, `tests/fixtures/umb_fix/B-16-control-flow-context/neg_loop_control_context.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-16 inventory count decreases by 7 and control-flow context rejection happens before codegen.
