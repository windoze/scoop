# B-13 修复策略

Bucket: 数组 / 复合 transport metadata
Inventory: `audit/UMB_inventory.csv`
Active entry count: 0
Retired entry count: 24
Primary class: C / real implementation

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `mir.materialize` |
| Contract form | Composite, array, range/progression, and task transport metadata are descriptor-backed and complete. |
| Primary upstream_gate | `transport lowering contract: composite and array metadata are descriptor-backed` |
| Gate coverage | 24/24 initial entries carried this single gate; all are now retired in `audit/UMB_retired.csv`. |

## 落地路径

- P7-C3 added descriptor-backed lowering for arrays, composite transport payloads, task transport tuple encode/decode, and Float value erasure boxing.
- Minimum happy-path fixtures are active: `tests/fixtures/umb_fix/B-13-composite-transport/pos_array_composite_transport.scoop` and `tests/fixtures/umb_fix/B-13-composite-transport/pos_range_progression_for.scoop`.
- Negative/typecheck transport fixtures remain active and do not replace the required runtime implementation.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-13-composite-transport/pos_array_composite_transport.scoop`, `tests/fixtures/umb_fix/B-13-composite-transport/pos_range_progression_for.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-13-composite-transport/neg_array_transport_metadata.scoop`, `tests/fixtures/umb_fix/B-13-composite-transport/neg_range_progression_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_fix_fixture_index_in_sync`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target met: B-13 inventory count decreased by 24 and composite transport happy paths are active and pass.
