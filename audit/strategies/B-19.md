# B-19 修复策略

Bucket: Top-level / object init / extern global  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 (retired 39 by P7-B2.6)  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `typecheck` |
| Contract form | Top-level object/global initializer metadata and function-value top-level types are complete. |
| Primary upstream_gate | `resolve/typecheck top-level gate: object/global initializer metadata is complete` |
| Gate coverage | retired 39/39 entries; active CSV now has 0 B-19 entries. |

## 落地路径

- B. upstream contract: resolve/typecheck publishes object init, top-level immutable value, var, extern global, and function-value metadata before codegen.
- Add verifier coverage for top-level metadata completeness if MIR materialization owns any part of the shape.
- P7 removes top-level/object-init sentinels once missing metadata becomes an upstream diagnostic.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-19-top-level-object-extern/pos_package_import_scope.scoop`, `tests/fixtures/umb_fix/B-19-top-level-object-extern/pos_top_level_declarations.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-19-top-level-object-extern/neg_unresolved_import_scope.scoop`, `tests/fixtures/umb_fix/B-19-top-level-object-extern/neg_entry_point_effect_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement result: B-19 inventory count decreased by 39 and top-level metadata drift is caught before LLVM lowering.
