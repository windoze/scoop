# B-36 修复策略

Bucket: 未定义/暂未支持的 spec surface  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 58 retired by P7-A4
Primary class: D / spec uncovered, retired through FrontendReject

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `typecheck` |
| Contract form | Undefined or placeholder language surfaces are rejected before codegen until the spec is extended. |
| Primary upstream_gate | `spec/frontend gate: undefined or placeholder language surfaces are rejected before codegen` |
| Gate coverage | P7-A4 retired 58/58 entries; async/await and generator/yield names now fail in resolve when not valid user/imported symbols. |

## 落地路径

- D. spec uncovered: do not implement production lowering in P7 until the relevant spec sections are written and `audit/spec_coverage_matrix.md` no longer marks them intentionally empty or blocked.
- Active negative fixtures lock frontend rejection for async/structured concurrency, generator/yield, and placeholder surfaces without exposing implementation fallback terms.
- If a surface becomes specified, add a new B/C prerequisite and reclassify the affected inventory entries before codegen changes.

## 验证锚

- Positive smoke anchors: `tests/fixtures/umb_fix/B-36-spec-uncovered/pos_spec_meta_minimal_main.scoop`, `tests/fixtures/umb_fix/B-36-spec-uncovered/pos_annotation_surface.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-36-spec-uncovered/neg_async_await_surface_rejected.scoop`, `tests/fixtures/umb_fix/B-36-spec-uncovered/neg_generator_yield_surface_rejected.scoop`, `tests/fixtures/umb_fix/B-36-spec-uncovered/neg_spec_meta_unsupported_surface.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_spec_coverage_matrix_in_sync`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: complete; active B-36 count is 0 and retired IDs are recorded in `audit/UMB_retired.csv`.
