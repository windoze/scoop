# B-32 修复策略

Bucket: print / panic / sysroot 桥接  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 10  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Sysroot print/panic bridge arguments are resolved and materialized as supported values. |
| Primary upstream_gate | `sysroot bridge gate: print/panic arguments are materialized as supported values` |
| Gate coverage | 10/10 entries already carry this single gate in CSV; each B-32 entry has one primary gate. |

## 落地路径

- B. upstream contract: sysroot bridge/typecheck rejects unknown or malformed print/panic calls and publishes string argument values.
- Codegen should only lower supported sysroot bridge calls with concrete argument values and return shape.
- P7 replaces B-32 sentinels after bridge diagnostics are covered.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-32-print-panic-sysroot/pos_sysroot_print_panic_bridge.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-32-print-panic-sysroot/neg_unknown_sysroot_intrinsic.scoop`, `tests/fixtures/umb_fix/B-32-print-panic-sysroot/neg_sysroot_intrinsic_bridge.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-32 inventory count decreases by 10 and sysroot bridge failures happen before codegen.
