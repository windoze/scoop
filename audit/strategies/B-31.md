# B-31 修复策略

Bucket: 标量扩展方法 (Float/Int/Char/Bool/String)
Inventory: `audit/UMB_inventory.csv`
Entry count: 0 active; 12 retired in P7-B3.1
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` |
| Contract form | Scalar extension receiver, named args, arity, value materialization, and return scalar types are valid. |
| Primary upstream_gate | `typecheck scalar-extension gate: receiver and return scalar types are valid` |
| Gate coverage | 0 active entries remain; retired ledger covers all 12 B-31 rows. |

## 落地路径

- B. upstream contract: typecheck rejects invalid scalar extension method calls and publishes concrete receiver/return types.
- Codegen scalar-extension lowering now assumes verified receiver/value/return types and uses internal invariant panics for impossible drift.
- Current active CSV split is empty; retired ledger records all B-31 rows.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-31-scalar-methods/pos_scalar_extension_methods.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-31-scalar-methods/neg_scalar_method_contract.scoop`, `tests/fixtures/umb_fix/B-31-scalar-methods/neg_scalar_extension_method_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B3.1 retirement target met: B-31 active inventory count is 0; retired IDs are tracked in `audit/UMB_retired.csv`.

## P7 完成记录

- P7-B3.1 retired the 12 InternalBugSentinel rows through scalar method typecheck contracts and LLVM internal invariants.
