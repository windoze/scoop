# B-24 修复策略

Bucket: Reflection / static metadata intrinsic  
Inventory: `audit/UMB_inventory.csv`  
Active entry count: 0
Retired entry count: 6
Primary class: C/D, RealImpl retired by P7-C1

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `mir.materialize` |
| Contract form | Reflection/static metadata intrinsics have supported lowering and validated arguments. |
| Primary upstream_gate | `reflection/static-metadata contract: static metadata intrinsics have supported lowering` |
| Gate coverage | 6/6 entries already carry this single gate in CSV; D follow-up is only for future spec gaps. |

## 落地路径

- C. real implementation completed in P7-C1: supported `sizeOf<T>()`, `kindOf<T>()`, and `descOf<T>()` HIR/MIR paths lower to real LLVM values.
- Current happy-path coverage lives in ordinary reflection fixtures such as `tests/fixtures/run-pass/reflection_kind_desc_basic.scoop`; the old B-24 UMB fixture directory has been retired.
- If a future reflection surface proves spec-uncovered, add a D-class prerequisite before changing the implementation path.

## 验证锚

- Positive fixture anchors: `tests/fixtures/run-pass/reflection_kind_desc_basic.scoop`, `tests/fixtures/typecheck/splice_field_access_string_lit_ok.scoop`.
- Negative gate anchors: `tests/fixtures/typecheck/reflection_runtime_fallback_v0.scoop`, `tests/fixtures/typecheck/splice_field_unknown_field_is_error.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_fixture_index_in_sync`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement result: B-24 active inventory count is 0; the six initial ids are covered by `audit/UMB_retired.csv`.
