# B-24 修复策略

Bucket: Reflection / comptime intrinsic  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 6  
Primary class: C/D, currently RealImpl

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `mir.materialize` |
| Contract form | Comptime/reflection static metadata intrinsics have supported lowering and validated arguments. |
| Primary upstream_gate | `comptime/reflection contract: static metadata intrinsics have supported lowering` |
| Gate coverage | 6/6 entries already carry this single gate in CSV; D follow-up is only for future spec gaps. |

## 落地路径

- C. real implementation: implement lowering for supported `sizeOf`, `kindOf`, and reflection/comptime metadata surfaces rather than rejecting valid programs.
- Minimum happy-path fixture: `tests/fixtures/umb_fix/B-24-reflection-comptime/pos_reflection_intrinsic.scoop`; it must be `IGNORE-UNTIL-FIX:B-24` until P7 implements the path.
- If a surface proves spec-uncovered, add a D-class prerequisite before changing the implementation path.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-24-reflection-comptime/pos_reflection_intrinsic.scoop`, `tests/fixtures/umb_fix/B-24-reflection-comptime/pos_comptime_if_for.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-24-reflection-comptime/neg_reflection_intrinsic_contract.scoop`, `tests/fixtures/umb_fix/B-24-reflection-comptime/neg_compile_time_metadata_splice.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_fixture_index_in_sync`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target: B-24 inventory count decreases by 6 when comptime/reflection happy paths become active and pass.
