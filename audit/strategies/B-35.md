# B-35 修复策略

Bucket: unsafe / NoGC / 边界  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 5  
Primary class: B/C, currently InternalBugSentinel

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `strict verifier` + `codegen helper` |
| Contract form | Unsafe/NoGC low-level boundary, frame slots, stackmap slots, and primitive payload invariants hold. |
| Primary upstream_gate | `unsafe/NoGC gate: low-level boundary, frame, and stackmap invariants hold` |
| Gate coverage | 5/5 entries already carry this single gate in CSV; helper cleanup is not a second primary gate. |

- The A-class subset should reuse B-01-style helpers for frame/entry-block access and add a focused frame-slot helper before deleting scattered assertions.

## 落地路径

- B. upstream contract: typecheck and strict verifier reject unsafe/NoGC boundary drift, value/frame slot count mismatches, and unpublished primitive payload contracts.
- A. helper invariant subset: centralize frame/stackmap slot checks so codegen failures are local sentinel tests, not user-visible fallback errors.
- Any RealImpl work for unsafe/NoGC must be split into a prerequisite if P7 proves a valid program lacks representation.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-35-unsafe-nogc-boundary/pos_gc_free_boundary.scoop`, `tests/fixtures/umb_fix/B-35-unsafe-nogc-boundary/pos_nogc_boundary.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-35-unsafe-nogc-boundary/neg_gc_free_boundary_ref_payload.scoop`, `tests/fixtures/umb_fix/B-35-unsafe-nogc-boundary/neg_nogc_boundary_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_helper_invariant_sentinel_tests_present`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-35 inventory count decreases by 5 after unsafe/NoGC verifier and helper sentinel coverage pass.

## P7-B2.7 完成记录

- Retired IDs: `UMB-0827`, `UMB-0828`, `UMB-0881`, `UMB-0885`, `UMB-1209`.
- Contract: explicit-frame spill mirrors, frame/value slot counts, and raw value-primitive boundary payloads are internal verifier/helper invariants before LLVM lowering.
- Status: B-35 active inventory is 0; retired rows are tracked in `audit/UMB_retired.csv`.
