# B-17 修复策略

Bucket: Coercion / 标量运算  
Inventory: `audit/UMB_inventory.csv`  
Active entry count: 0; retired entry count: 47
Primary class: A/B, retired InternalBugSentinel by P7-B2.5

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `codegen helper` |
| Contract form | Scalar coercions, operator operands, string equality, and helper-produced basic values are valid. |
| Primary upstream_gate | `typecheck/operator gate: scalar coercion and operator operands are valid` |
| Gate coverage | 47/47 entries already carry this single gate in CSV; helper cleanup is not a second primary gate. |

- The A-class subset should reuse the B-01 helper pattern where scalar/operator lowering reads call results or basic values, especially `MainCodegen::expect_basic_value`.
- Typecheck remains the primary gate for user-visible scalar/operator mismatches.

## 落地路径

- B. upstream contract: typecheck rejects invalid scalar coercions and unsupported operator operand combinations before LLVM lowering.
- A. helper invariant subset: centralize basic-value extraction and string-operation return checks through shared helper APIs before deleting scattered sentinels.
- P7 must preserve one primary gate per entry and only record helper cleanup as implementation detail.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-17-coercion-scalar/pos_scalar_operator_matrix.scoop`, `tests/fixtures/umb_fix/B-17-coercion-scalar/pos_operator_scalar_coercion.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-17-coercion-scalar/neg_operator_operand_gate.scoop`, `tests/fixtures/umb_fix/B-17-coercion-scalar/neg_boxing_boundary_violation.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_helper_invariant_sentinel_tests_present`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B2.5 result: B-17 inventory count decreased by 47; scalar gate fixtures are active and helper sentinel coverage moved to retired ledger.
