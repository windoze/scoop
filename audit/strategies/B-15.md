# B-15 修复策略

Bucket: When / 模式匹配用户面  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 55  
Primary class: B / frontend reject

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` |
| Contract form | When exhaustiveness, subject type, guards, variant payloads, and arm result type are valid before codegen. |
| Primary upstream_gate | `typecheck when gate: exhaustiveness, subject type, guards, and arm types are valid` |
| Gate coverage | 55/55 entries already carry this single gate in CSV; each B-15 entry has one primary gate. |

## 落地路径

- B. upstream contract: typecheck emits explicit diagnostics for invalid when inputs; codegen receives only a verified decision tree.
- Keep B-15 separate from B-07: B-15 owns when expression semantics and diagnostics, while B-07 owns lowered pattern schema invariants.
- P7 replaces when-lowering sentinels with assertions only after negative fixtures lock the frontend gate.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-15-when-pattern/pos_when_enum_exhaustive.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-15-when-pattern/neg_when_pattern_frontend_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-15 inventory count decreases by 55 and the frontend when diagnostic remains stable.
