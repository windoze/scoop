# B-20 修复策略

Bucket: Class ctor / property / 字段访问  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 (retired 46 by P7-B2.6)  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `typecheck` + `mir.materialize` |
| Contract form | Class ctor params, property metadata, field indexes, and receiver values are fixed before codegen. |
| Primary upstream_gate | `resolve/typecheck class gate: ctor, property, and field access contracts are fixed` |
| Gate coverage | retired 46/46 entries; active CSV now has 0 B-20 entries. |

## 落地路径

- B. upstream contract: resolve/typecheck rejects invalid class/property surfaces and MIR materialization publishes constructor/field metadata.
- Add verifier coverage for constructor parameter ordering, field index availability, and property receiver shape.
- P7 replaces class/property sentinels after upstream metadata is complete.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-20-class-property-field/pos_class_interface_object_members.scoop`, `tests/fixtures/umb_fix/B-20-class-property-field/pos_class_ctor_property_init.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-20-class-property-field/neg_class_ctor_signature_drift.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement result: B-20 inventory count decreased by 46 and class metadata diagnostics are upstream-owned.
