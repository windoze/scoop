# B-14 修复策略

Bucket: Cast / TypeCheck (`as`/`as?`/`is`)  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 27 retired in `audit/UMB_retired.csv` by P7-B2.4
Primary class: B/C, currently InternalBugSentinel

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Cast/type-test target metadata, operand category, and Option result shape are supported before codegen. |
| Primary upstream_gate | `typecheck/runtime-cast gate: cast and type-test target metadata are supported` |
| Gate coverage | 27/27 entries already carry this single gate in CSV; C-class runtime work is not a second primary gate. |

## 落地路径

- B. upstream contract: reject invalid casts and unsupported target metadata in typecheck, then verify MIR cast operands and result shape.
- If P7 discovers a genuinely valid runtime metadata gap, split that RealImpl work into a follow-up before removing B-14 sentinels.
- Current strategy keeps every CSV entry on one primary gate and treats runtime implementation as conditional, not a workaround.

## P7-B2.4 completion

- Retired all 27 B-14 rows.
- Typecheck/materialized MIR validation owns type-test metadata consistency, runtime descriptor shape, `as`/`as?` failure/result contracts, and `as?` Option result shape.
- B-25/B-34 runtime implementation sentinels remain active and were not retired by this verifier task.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-14-cast-typecheck/pos_cast_typecheck_runtime.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-14-cast-typecheck/neg_cast_typecheck_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-14 inventory count decreases by 27 or a concrete C-class prerequisite is inserted before removal.
