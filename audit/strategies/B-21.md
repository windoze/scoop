# B-21 修复策略

Bucket: Struct literal / 字段层  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 6  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `strict verifier` |
| Contract form | Struct fields, duplicates, missing fields, and field value schemas are valid. |
| Primary upstream_gate | `typecheck struct gate: field names, duplicates, and schema are valid` |
| Gate coverage | 6/6 entries already carry this single gate in CSV; all mixed-class entries use this primary gate. |

## 落地路径

- B. upstream contract: typecheck emits frontend diagnostics for unknown/missing/duplicate fields and verifier catches internal schema drift.
- Current CSV split is `FrontendReject=3` and `InternalBugSentinel=3`; keep that split explicit in fixtures and baseline tests.
- P7 replaces codegen struct-field sentinels after the struct gate is covered.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-21-struct-fields/pos_struct_fields.scoop`, `tests/fixtures/umb_fix/B-21-struct-fields/pos_with_copy_update.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-21-struct-fields/neg_struct_unknown_field.scoop`, `tests/fixtures/umb_fix/B-21-struct-fields/neg_with_copy_update_field.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-21 inventory count decreases by 6 with frontend/internal class split preserved.
