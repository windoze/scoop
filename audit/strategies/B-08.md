# B-08 修复策略

Bucket: MIR 成员存取 / 赋值合法性  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 6 retired in P7-A2/P7-B2.8
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `strict verifier` |
| Contract form | Assignment/member-store target writability, receiver place, and value type are known and valid. |
| Primary upstream_gate | `typecheck assignment/member-store gate: target writability and value type are valid` |
| Gate coverage | 0 active entries remain; retired ledger covers all 6 B-08 rows. |

## 落地路径

- B. upstream contract: typecheck owns user-facing rejection for immutable or non-writable targets, and the MIR/materialized MIR verifier owns internal member-store shape drift.
- Current active CSV split is empty; retired ledger records the 2 P7-A2 frontend rows and 4 P7-B2.8 internal rows.
- Codegen member-store sentinels are now verifier-backed internal invariants.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-08-member-store/pos_member_store_assignment.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-08-member-store/neg_member_store_not_writable.scoop`, `tests/fixtures/umb_fix/B-08-member-store/neg_property_assignment_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B2.8 retirement target met: B-08 active inventory count is 0; retired IDs are tracked in `audit/UMB_retired.csv`.

## P7 完成记录

- P7-A2 retired the 2 FrontendReject rows through typecheck assignment/member-store gates.
- P7-B2.8 retired the 4 InternalBugSentinel rows through MIR/materialized MIR receiver/place/value verifier contracts and LLVM internal invariants.
