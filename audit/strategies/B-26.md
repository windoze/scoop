# B-26 修复策略

Bucket: atomic intrinsic 系列  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 102  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Atomic intrinsic arity, target mutability, target width, ordering, and return shape are valid. |
| Primary upstream_gate | `intrinsic resolver gate: atomic intrinsic arity, target, and ordering are valid` |
| Gate coverage | 102/102 entries already carry this single gate in CSV; each B-26 entry has one primary gate. |

## 落地路径

- B. upstream contract: intrinsic resolver/typecheck rejects invalid atomic targets, arity, orderings, and non-mutable lvalues.
- MIR materialization publishes atomic operand and result types consumed by both raw and effect-lowered paths.
- P7 replaces atomic intrinsic sentinels after resolver and verifier coverage is stable.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-26-atomic-intrinsics/pos_atomic_intrinsics.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-26-atomic-intrinsics/neg_atomic_intrinsic_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-26 inventory count decreases by 102 and atomic diagnostics are resolver-owned.
