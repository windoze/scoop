# B-26 修复策略

Bucket: atomic intrinsic 系列
Inventory: `audit/UMB_inventory.csv`
Entry count: 0 active; 102 retired in P7-B3.3
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + sysroot intrinsic bridge + effect-lowered verifier |
| Contract form | Atomic intrinsic arity, target addressability, target mutability, target width, ordering, and return shape are valid. |
| Primary upstream_gate | `intrinsic resolver gate: atomic intrinsic arity, target, and ordering are valid` |
| Gate coverage | 0 active entries remain; retired ledger covers all 102 B-26 rows. |

## 落地路径

- B. upstream contract: typecheck rejects non-addressable raw atomic targets and non-writable write targets where source mutability facts are available.
- Sysroot/typecheck contracts validate `__AtomicInt` width-equivalent integer calls, `__atomicRef*` `AnyRef` generic bounds, arity, and argument types.
- LLVM HIR and effect-lowered atomic lowering now assumes verified target/value/return contracts and uses internal invariant panics for impossible drift.
- Current active CSV split is empty; retired ledger records all B-26 rows.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-26-atomic-intrinsics/pos_atomic_intrinsics.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-26-atomic-intrinsics/neg_atomic_intrinsic_gate.scoop`, `neg_atomic_target_lvalue_gate.scoop`, and `neg_atomic_target_writable_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B3.3 retirement target met: B-26 active inventory count is 0; retired IDs are tracked in `audit/UMB_retired.csv`.

## P7 完成记录

- P7-B3.3 retired the 102 `InternalBugSentinel` rows through atomic intrinsic typecheck/sysroot contracts and LLVM internal invariants.
