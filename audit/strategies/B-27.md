# B-27 修复策略

Bucket: sync intrinsic 系列
Inventory: `audit/UMB_inventory.csv`
Entry count: 0 active; 58 retired in P7-B3.2
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + sysroot intrinsic bridge |
| Contract form | Sync primitive calls match sysroot/runtime arity, receiver, argument, block, create/destroy, and return contracts. |
| Primary upstream_gate | `intrinsic resolver gate: sync primitive calls match sysroot runtime contract` |
| Gate coverage | 0 active entries remain; retired ledger covers all 58 B-27 rows. |

## 落地路径

- B. upstream contract: intrinsic resolver/typecheck rejects invalid sync primitive calls and publishes supported receiver/argument metadata.
- LLVM sync intrinsic lowering now assumes verified receiver/value/return contracts and uses internal invariant panics for impossible drift.
- Current active CSV split is empty; retired ledger records all B-27 rows.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-27-sync-intrinsics/pos_sync_intrinsics.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-27-sync-intrinsics/neg_sync_intrinsic_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B3.2 retirement target met: B-27 active inventory count is 0; retired IDs are tracked in `audit/UMB_retired.csv`.

## P7 完成记录

- P7-B3.2 retired the 58 `InternalBugSentinel` rows through sync primitive typecheck/sysroot contracts and LLVM internal invariants.
