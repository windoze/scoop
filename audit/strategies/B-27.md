# B-27 修复策略

Bucket: sync intrinsic 系列  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 58  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Sync primitive calls match sysroot/runtime arity, receiver, argument, and return contracts. |
| Primary upstream_gate | `intrinsic resolver gate: sync primitive calls match sysroot runtime contract` |
| Gate coverage | 58/58 entries already carry this single gate in CSV; each B-27 entry has one primary gate. |

## 落地路径

- B. upstream contract: intrinsic resolver/typecheck rejects invalid sync primitive calls and publishes supported receiver/argument metadata.
- U5-T03 adds the B-27 negative fixture because U3 originally only listed a positive sync intrinsic path.
- P7 replaces sync intrinsic sentinels after resolver diagnostics and verifier coverage are stable.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-27-sync-intrinsics/pos_sync_intrinsics.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-27-sync-intrinsics/neg_sync_intrinsic_gate.scoop`.
- U5-T03 direct coverage: the negative anchor lists all 58 B-27 ids in `// COVERS:` and `_index.csv`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-27 inventory count decreases by 58 and sync runtime contract drift is caught before codegen.
