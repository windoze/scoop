# B-02 修复策略

Bucket: MIR local / member 类型推断不完整  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 6  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `mir.materialize` + `strict verifier` |
| Contract form | Published local/member source types and codegen types are total before LLVM lowering. |
| Primary upstream_gate | `MIR materialize: local/member source and codegen types are complete` |
| Gate coverage | 6/6 entries already carry this single gate in CSV; no B-02 entry has a secondary primary gate. |

## 落地路径

- B. upstream contract: materialization must reject or repair unresolved locals, missing initializer types, and member source-type drift before codegen.
- Add a strict verifier baseline that walks MIR locals/member sources and asserts every codegen-needed type is present.
- After the verifier is enforced, replace B-02 codegen sentinels with verifier-backed impossible-state assertions.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-02-mir-local-type/pos_name_scope_resolution.scoop`, `tests/fixtures/umb_fix/B-02-mir-local-type/pos_type_inference_locals_branches.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-02-mir-local-type/neg_unresolved_name_scope.scoop`, `tests/fixtures/umb_fix/B-02-mir-local-type/neg_type_inference_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-02 inventory count decreases by 6, and the MIR local/member verifier has a stable failing fixture before any codegen sentinel is removed.
