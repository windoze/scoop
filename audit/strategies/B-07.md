# B-07 修复策略

Bucket: MIR pattern 子句 schema 漂移  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 34  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `strict verifier` |
| Contract form | Pattern subject type, clause schema, literal compatibility, and destructuring arity are validated. |
| Primary upstream_gate | `typecheck pattern gate: pattern subject and clause schema are valid` |
| Gate coverage | 34/34 entries already carry this single gate in CSV; no B-07 entry has a second primary gate. |

## 落地路径

- B. upstream contract: typecheck rejects invalid pattern subject/type combinations and MIR verifier confirms the lowered pattern schema.
- B-07 remains separate from B-15: B-07 validates pattern data shape, while B-15 owns when-expression user semantics.
- P7 replaces constant-pattern codegen sentinels only after the typecheck/verifier pair is covered.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-07-pattern-schema/pos_pattern_destructuring.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-07-pattern-schema/neg_pattern_schema_gate.scoop`.
- U5-T03 direct coverage: both anchors list all 34 B-07 ids in `// COVERS:` and `_index.csv`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-07 inventory count decreases by 34 and pattern schema diagnostics trigger before codegen.
