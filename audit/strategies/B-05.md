# B-05 修复策略

Bucket: MIR CFG / start block / goto target 异常  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 25  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `strict verifier` |
| Contract form | CFG blocks, start block, branch conditions, and terminator targets are valid before codegen. |
| Primary upstream_gate | `MIR strict verifier: CFG blocks and terminator targets are valid` |
| Gate coverage | 25/25 entries already carry this single gate in CSV; no B-05 entry has a second primary gate. |

## 落地路径

- B. upstream contract: strict verifier rejects missing start blocks, invalid goto targets, invalid branch conditions, and terminator shape drift.
- Keep codegen CFG lowering simple: it should assume verified block topology and assert impossible states.
- P7 should update verifier diagnostics first, then replace B-05 codegen sentinels.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-05-mir-cfg/pos_cfg_if_while_for.scoop`, `tests/fixtures/umb_fix/B-05-mir-cfg/pos_if_branch_cfg.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-05-mir-cfg/neg_if_branch_type_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target: B-05 inventory count decreases by 25 and CFG verifier failures remain stable.
