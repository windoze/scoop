# B-29 修复策略

Bucket: GC intrinsic 系列  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 93  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | GC handle, pin, unpin, token, and root contracts are valid and represented before codegen. |
| Primary upstream_gate | `intrinsic resolver gate: GC handle/pin calls observe token and root contracts` |
| Gate coverage | 93/93 entries already carry this single gate in CSV; each B-29 entry has one primary gate. |

## 落地路径

- B. upstream contract: intrinsic resolver/typecheck rejects invalid GC handle/pin/unpin calls and materialization publishes token/root metadata.
- Keep runtime GC semantics out of fixture-only shortcuts; valid GC intrinsic paths must either be supported or explicitly tracked as prerequisites.
- P7 replaces GC intrinsic sentinels after resolver and verifier coverage is stable.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-29-gc-intrinsics/pos_gc_pin_handle_boundary.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-29-gc-intrinsics/neg_gc_intrinsic_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-29 inventory count decreases by 93 and GC intrinsic contract failures happen before LLVM lowering.
