# B-22 修复策略

Bucket: Enum 布局 / niche / Option  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 (retired 40 by P7-B2.6)  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` + `strict verifier` |
| Contract form | Enum variants, payload arity, niche layout, and Option payload contracts are valid and published. |
| Primary upstream_gate | `layout/typecheck enum gate: variant, niche, and Option payload contracts are valid` |
| Gate coverage | retired 40/40 entries; active CSV now has 0 B-22 entries. |

## 落地路径

- B. upstream contract: typecheck validates enum variant use and materialization publishes niche/Option layout to codegen.
- Strict verifier should fail on payload arity, missing variant, or impossible niche representation before LLVM lowering.
- P7 replaces enum/niche sentinels with verified layout assertions.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-22-enum-niche-option/pos_enum_option_niche.scoop`, `tests/fixtures/umb_fix/B-22-enum-niche-option/pos_nullable_option_niche.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-22-enum-niche-option/neg_enum_variant_contract.scoop`, `tests/fixtures/umb_fix/B-22-enum-niche-option/neg_nullable_option_exhaustiveness.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement result: B-22 inventory count decreased by 40 and enum/Option layout drift is verifier-owned.
