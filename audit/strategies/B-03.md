# B-03 修复策略

Bucket: MIR direct/closure/funptr 调用 ABI 漂移  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 56 retired in `audit/UMB_retired.csv` by P7-B2.4
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `strict verifier` + `mir.materialize` |
| Contract form | Callable ABI, arity, callee form, argument binding, and return storage are fixed before LLVM. |
| Primary upstream_gate | `MIR strict verifier: callable ABI, arity, and callee form are fixed` |
| Gate coverage | 56/56 entries already carry this single gate in CSV; each B-03 entry has one primary gate. |

## 落地路径

- B. upstream contract: strict verification rejects arity drift, callee-form drift, missing return storage, and closure/funptr ABI mismatches.
- MIR materialization should publish any hidden sret or function-value ABI metadata consumed by call lowering.
- P7 can then turn call-lowering sentinels into verifier-backed assertions and remove the B-03 `UnsupportedMainBody` sites.

## P7-B2.4 completion

- Retired all 56 B-03 rows.
- Materialized MIR validation owns callable ABI, arity/binding, callee form, return transport, and deferred return/storage invariants before LLVM.
- LLVM call lowering now uses verifier-backed internal invariants instead of B-03 `UnsupportedMainBody` diagnostics.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-03-call-abi/pos_function_type_call_abi.scoop`, `tests/fixtures/umb_fix/B-03-call-abi/pos_call_abi_direct_closure_funptr.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-03-call-abi/neg_call_abi_arity_drift.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`.
- P7 retirement target: B-03 inventory count decreases by 56 and callable ABI verifier failures are reported before LLVM lowering.
