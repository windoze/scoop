# B-33 修复策略

Bucket: Extern global / FunPtr 顶层  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 8  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `typecheck` |
| Contract form | Extern global metadata, initializer contracts, and top-level FunPtr metadata are complete. |
| Primary upstream_gate | `top-level FFI gate: extern global and FunPtr metadata are complete` |
| Gate coverage | 8/8 entries already carry this single gate in CSV; each B-33 entry has one primary gate. |

## 落地路径

- B. upstream contract: typecheck/FFI gate rejects invalid extern global initializers and incomplete FunPtr metadata.
- MIR materialization may verify top-level store target mutability/type before codegen consumes it.
- P7 replaces B-33 sentinels after FFI metadata diagnostics are locked.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-33-extern-global-funptr/pos_extern_global_funptr.scoop`, `tests/fixtures/umb_fix/B-33-extern-global-funptr/pos_calling_convention_clayout.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-33-extern-global-funptr/neg_extern_global_initializer_contract.scoop`, `tests/fixtures/umb_fix/B-33-extern-global-funptr/neg_extern_global_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-33 inventory count decreases by 8 and top-level FFI drift is caught upstream.
