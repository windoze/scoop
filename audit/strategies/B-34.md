# B-34 修复策略

Bucket: RuntimeError / try-catch-finally  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 6  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Try/catch/finally completion payloads and RuntimeError enum layout are coherent. |
| Primary upstream_gate | `effect/runtime-error gate: try/catch/finally and RuntimeError layout are coherent` |
| Gate coverage | 6/6 entries already carry this single gate in CSV; each B-34 entry has one primary gate. |

## 落地路径

- B. upstream contract: effect/typecheck validates try-catch-finally boundaries and materialization publishes RuntimeError payload layout.
- Codegen should consume a verified RuntimeError enum and completion payload shape.
- P7 replaces B-34 sentinels after runtime-error layout diagnostics are covered.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-34-runtime-error-try/pos_try_catch_runtime_error.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-34-runtime-error-try/neg_handler_finally_runtime_boundary.scoop`, `tests/fixtures/umb_fix/B-34-runtime-error-try/neg_runtime_error_layout_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-34 inventory count decreases by 6 and RuntimeError layout drift is upstream-owned.

## P7-B2.7 完成记录

- Retired IDs: `UMB-0215`, `UMB-0216`, `UMB-0217`, `UMB-0218`, `UMB-0947`, `UMB-1047`.
- Contract: effect/runtime lowering consumes verified completion payload types, RuntimeError enum/unit variant layout, and `as` cast ClassCastFailed failure contract.
- Status: B-34 active inventory is 0; retired rows are tracked in `audit/UMB_retired.csv`.
