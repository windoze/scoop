# B-30 修复策略

Bucket: named / unsafe / FunPtr intrinsic  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active / 117 retired
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Named intrinsic, unsafe intrinsic, pointer conversion, and FunPtr signatures/calls are valid. |
| Primary upstream_gate | `unsafe/intrinsic gate: named, FunPtr, and pointer conversion calls are valid` |
| Gate coverage | 117/117 entries already carry this single gate in CSV; each B-30 entry has one primary gate. |

## 落地路径

- B. upstream contract: unsafe/intrinsic resolver rejects unknown named intrinsics, invalid FunPtr signatures, and pointer conversion drift.
- MIR materialization publishes FunPtr metadata and call ABI details consumed by codegen.
- P7-B3.5 replaced B-30 sentinels after typecheck, generic replay, intrinsic, and materialized MIR verifier checks were stable.

## P7 完成记录

- P7-B3.5 retired all 117 B-30 `InternalBugSentinel` rows into `audit/UMB_retired.csv`.
- LLVM lowering now uses `panic_verified_intrinsic_contract`, `expect_*` helpers, or explicit internal panic boundaries for residual named intrinsic, unsafe value, FunPtr, pointer-conversion, and stackmap smoke drift.
- Active B-30 inventory count is 0; retired ledger is the source of ID coverage.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-30-named-unsafe-funptr/pos_named_intrinsic_funptr.scoop`, `tests/fixtures/umb_fix/B-30-named-unsafe-funptr/pos_funptr_call_boundary.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-30-named-unsafe-funptr/neg_funptr_signature_gate.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-30 inventory count decreased by 117 and unsafe/FunPtr drift is caught upstream.
