# B-12 修复策略

Bucket: Closure / lambda / capture 表达
Inventory: `audit/UMB_inventory.csv`
Active entry count: 0
Retired entry count: 50
Primary class: C / real implementation

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `hir` + `mir.materialize` |
| Contract form | Closure capture environment layout, capture value typing, and function-value ABI are published. |
| Primary upstream_gate | `closure lowering contract: capture environment layout and function value ABI are published` |
| Gate coverage | 50/50 initial entries carried this single gate; all are now retired in `audit/UMB_retired.csv`. |

## 落地路径

- P7-C4 completed closure environment layout, aggregate capture storage/loading, materialized MIR closure callable lookup, function-value call ABI, and lambda return lowering.
- Minimum happy-path fixture is active: `tests/fixtures/umb_fix/B-12-closure-capture/pos_lambda_closure_capture.scoop`.
- Negative capture-contract fixture remains active as typecheck coverage for untyped lambda params without expected type.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-12-closure-capture/pos_lambda_closure_capture.scoop`.
- Negative gate anchor: `tests/fixtures/umb_fix/B-12-closure-capture/neg_lambda_capture_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_fix_fixture_index_in_sync`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target met: B-12 inventory count decreased by 50 and closure/lambda fixtures are active and pass.
