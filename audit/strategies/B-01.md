# B-01 修复策略

Bucket: inkwell builder bookkeeping  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 71  
Primary class: A / helper invariant

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `codegen helper` |
| Contract form | Internal LLVM insertion-context invariant; user input cannot directly create this state. |
| Primary upstream_gate | `codegen helper invariant: active LLVM insertion context` |
| Gate coverage | 71/71 entries already carry this single gate in CSV. |

- P7 should introduce the shared helper API before removing scattered sentinels: `MainCodegen::expect_insert_block`, `MainCodegen::expect_parent_function`, `MainCodegen::expect_entry_block`, and `MainCodegen::expect_basic_value`.
- These helpers own the panic/sentinel boundary and make each current `UnsupportedMainBody` site an impossible state after a valid insertion context is established.

## 落地路径

- A. helper invariant: add the helper API in one module, migrate all B-01 builder/current-function/entry-block/basic-value reads through it, then replace the former error paths with `unreachable!` or `expect` carrying the helper name.
- Keep no frontend fixture requirement for B-01; user code should never be able to target this invariant.
- Do not change production codegen during this doc-and-test task; the implementation belongs to P7.

## 验证锚

- U6 sentinel anchor: `crates/scoopc/src/audit/sentinel_tests.rs::umb_fix_helper_invariant_sentinel_tests_present`.
- U5 directory anchor: `tests/fixtures/umb_fix/B-01-builder-invariant/` documents sentinel-only coverage.
- U5 sentinel coverage record: `tests/fixtures/umb_fix/B-01-builder-invariant/_README.md` `SENTINEL-COVERS` lists the 71 B-01 inventory ids.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_inventory_class_distribution`, and `umb_fix_every_inventory_id_is_covered`.
- P7 retirement target: B-01 inventory count decreases by 71 and the stale production count drops by the same migrated-site count while `cargo run -p scoopc --bin umb-audit -- diff` stays clean.
