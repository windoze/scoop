# B-01 修复策略

Bucket: inkwell builder bookkeeping  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active / 71 retired by P7-B1
Primary class: A / helper invariant

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `codegen helper` |
| Contract form | Internal LLVM insertion-context invariant; user input cannot directly create this state. |
| Primary upstream_gate | `codegen helper invariant: active LLVM insertion context` |
| Gate coverage | 71/71 initial entries retired to `audit/UMB_retired.csv`. |

- P7-B1 introduced the shared helper API before removing scattered sentinels: `MainCodegen::expect_insert_block`, `MainCodegen::expect_parent_function`, `MainCodegen::expect_current_function`, `MainCodegen::expect_entry_block`, and `MainCodegen::expect_basic_value`.
- These helpers own the panic/sentinel boundary and make each former B-01 `UnsupportedMainBody` site an impossible state after a valid insertion context is established.

## 落地路径

- A. helper invariant: P7-B1 added the helper API in one module, migrated all B-01 builder/current-function/entry-block/basic-value reads through it, and replaced the former error paths with helper panic messages carrying the helper name.
- Keep no frontend fixture requirement for B-01; user code should never be able to target this invariant.
- Production codegen migration is complete for B-01; future work should not add new `UnsupportedMainBody` sites for insertion-context bookkeeping.

## 验证锚

- Sentinel anchor: `crates/scoopc/src/audit/sentinel_tests.rs::umb_fix_helper_invariant_sentinel_tests_present`.
- Directory anchor: `tests/fixtures/umb_fix/B-01-builder-invariant/` documents sentinel-only coverage.
- Sentinel coverage record: `tests/fixtures/umb_fix/B-01-builder-invariant/_README.md` `SENTINEL-COVERS` lists the 71 retired B-01 inventory ids.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_inventory_class_distribution`, and `umb_fix_every_inventory_id_is_covered`.
- P7-B1 retirement result: B-01 active inventory decreased by 71 and `cargo run -p scoopc --bin umb-audit -- diff` stays clean.
