# B-06 修复策略

Bucket: MIR struct/tuple/enum 字面量 schema 漂移  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 43 retired in P7-B2.3
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `strict verifier` + `mir.materialize` |
| Contract form | Aggregate literal schemas match the lowered type layout and all element/field types are known. |
| Primary upstream_gate | `MIR strict verifier: aggregate literal schema matches lowered type layout` |
| Gate coverage | 43/43 entries already carry this single gate in CSV; each B-06 entry has one primary gate. |

## 落地路径

- B. upstream contract: materialize tuple, struct, and enum literal metadata before codegen and verify field count, field order, and element type compatibility.
- Frontend/typecheck diagnostics own user-facing schema errors; codegen only receives verified aggregate payloads.
- P7 replaces aggregate schema sentinels with verifier-backed assertions.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-06-literal-schema/pos_tuple_literal_access.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-06-literal-schema/neg_aggregate_literal_schema.scoop`, `tests/fixtures/umb_fix/B-06-literal-schema/neg_struct_literal_schema.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7-B2.3 retirement result: B-06 inventory count decreased by 43 and aggregate verifier coverage owns schema drift.

## P7 Completion

- Retired by: P7-B2.3.
- Retired IDs: see `audit/UMB_retired.csv` rows with `bucket=B-06` and `retired_by=P7-B2.3`.
- Contract owner: materialized MIR aggregate schema verifier plus typecheck tuple/struct literal gates.
- Active fixtures: `tests/fixtures/umb_fix/B-06-literal-schema/`.
