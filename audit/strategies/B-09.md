# B-09 修复策略

Bucket: Cross-TypeStore equivalence 不闭合  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 0 active; 13 retired in `audit/UMB_retired.csv` by P7-B2.4
Primary class: B/C, currently InternalBugSentinel

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `mir.materialize` + `strict verifier` |
| Contract form | Cross-TypeStore codegen type lookup is total and equivalent for generics, aliases, funptrs, and carriers. |
| Primary upstream_gate | `TypeStore equivalence gate: cross-store codegen type lookup is total` |
| Gate coverage | 13/13 entries already carry this single gate in CSV; C-class follow-up is not a second primary gate. |

## 落地路径

- B. upstream contract first: add verifier coverage that proves every TypeId crossing stores has a corresponding codegen type.
- C. real implementation is only allowed if verifier coverage shows a missing representation, not a malformed input; keep that as a notes-level follow-up until proven.
- P7 should replace codegen type-lookup sentinels after the equivalence verifier is active.

## P7-B2.4 completion

- Retired all 13 B-09 rows.
- Cross-TypeStore codegen lookup is centralized through equivalence helpers and treated as a verifier-backed invariant at former B-09 sites.
- No C-class prerequisite was discovered for these rows; remaining runtime metadata implementation work stays tracked in B-25 and other active buckets.

## 验证锚

- Positive fixture anchors: `tests/fixtures/umb_fix/B-09-type-equivalence/pos_generic_typestore_equivalence.scoop`, `tests/fixtures/umb_fix/B-09-type-equivalence/pos_typealias_equivalence.scoop`.
- Negative gate anchors: `tests/fixtures/umb_fix/B-09-type-equivalence/neg_cross_typestore_equivalence.scoop`, `tests/fixtures/umb_fix/B-09-type-equivalence/neg_generic_equivalence_drift.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_class_distribution`, `umb_fix_every_inventory_id_is_covered`, and `umb_fix_spec_coverage_matrix_in_sync`.
- P7 retirement target: B-09 inventory count decreases by 13 and any remaining C-class work is tracked separately before codegen changes proceed.
