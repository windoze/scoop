# B-23 修复策略

Bucket: Member access - 通用  
Inventory: `audit/UMB_inventory.csv`  
Entry count: 24  
Primary class: B / upstream contract

## 上游契约

| Item | Value |
|---|---|
| Responsible stage | `typecheck` + `mir.materialize` |
| Contract form | Member receiver type, target member, dispatch metadata, and value-box receiver shape are resolved. |
| Primary upstream_gate | `typecheck member-access gate: receiver and target member are resolved` |
| Gate coverage | 24/24 entries already carry this single gate in CSV; each B-23 entry has one primary gate. |

## 落地路径

- B. upstream contract: typecheck rejects unresolved member access, and materialization publishes dispatch/member metadata consumed by codegen.
- Add a bucket-driven positive fixture in U5-T03 because the U3 matrix currently only points B-23 at shared/property negatives.
- P7 removes B-23 sentinels after receiver and target metadata are verified.

## 验证锚

- Positive fixture anchor: `tests/fixtures/umb_fix/B-23-member-access/pos_member_access_dispatch.scoop` (planned by U5-T03).
- Negative gate anchor: `tests/fixtures/umb_fix/B-23-member-access/neg_member_access_contract.scoop`.
- Baseline tests: `umb_inventory_csv_in_sync`, `umb_inventory_buckets_total`, `umb_fix_every_bucket_has_at_least_one_pos_and_one_neg`, and `umb_fix_no_forbidden_terms_in_neg_messages`.
- P7 retirement target: B-23 inventory count decreases by 24 and member-access metadata drift is caught upstream.
