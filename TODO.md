# Test Infrastructure Cleanup TODO 索引

> 生成时间：2026-05-26
> 计划基线：[`PLAN.md`](./PLAN.md)
> 设计基线：[`TEST_INFRA_CLEANUP.md`](./TEST_INFRA_CLEANUP.md)
> 归档前置基线：
> - [`docs/archive/designs/PIPELINE_REFACTOR.md`](./docs/archive/designs/PIPELINE_REFACTOR.md)
> - [`docs/archive/designs/PIPELINE-CLEANUP.md`](./docs/archive/designs/PIPELINE-CLEANUP.md)
> - [`docs/archive/plans/PLAN-pipeline-refactor.md`](./docs/archive/plans/PLAN-pipeline-refactor.md)
> - [`docs/archive/plans/TODO-pipeline-refactor.md`](./docs/archive/plans/TODO-pipeline-refactor.md)
> 当前状态：P0-P4 全部 [TODO]；上一轮 Pipeline Refactor（P0-P10）已全部完成并归档。

## 总原则

- `PLAN.md` 是当前执行计划基线；如果实现时发现阶段边界、命令面契约或 fixture 发现规则需要改变，必须先回写 `TEST_INFRA_CLEANUP.md`，再调整 TODO。
- 所有任务按 `P0 → P1 → P2 → P3 → P4` 顺序推进；P3 严格在 P1+P2 完成之后执行，否则 CI 与开发流会断；P2 内部条目可与 P1 并行。
- 每个实现小阶段后必须紧跟一个独立 review 任务，复审该小阶段的完整变更、阶段目标和约束遵守情况。
- review 任务不是形式检查；如果发现前一任务没有真正完成目标，review 任务必须直接修正或阻塞下一任务。
- 任务完成后必须同时更新 `TODO.md` 中的任务状态与完成记录。
- 所有外部脚本统一用 **python（仅标准库）** 实现，放在 `tools/` 下；不复活 `tools/scoop_tools/`，不新增任何 Rust 工具 crate。
- shell 脚本（`tools/*.sh`）仅用于轻量编排，不实现指令解析 / golden 比对。
- `scoop` / `scoopc` 不得保留任何 `fixture` 命名的模块、CLI、env、API；`tests/fixtures/**` 对编译器而言只是普通源文件。
- `docs/archive/**` 整体冻结；`TODO*-pipeline-refactor.md` / `PLAN-pipeline-refactor.md` 等已归档文件中的 “验证通过：…” 行属于历史审计记录，不动。

## 任务索引

| 任务 | 状态 | 目标 |
| --- | --- | --- |
| [DONE] P0-T01 | [DONE] | 盘点 `EXPECT-*` 指令清单（语法/语义/参数）并落地 `docs/fixtures.md` 或 `tools/README.md` 一节 |
| [DONE] P0-T01R | [DONE] | Review `EXPECT-*` 指令清单完整度（覆盖现有 expectations.rs 全部指令） |
| [DONE] P0-T02 | [DONE] | 盘点 fixture 发现规则（phase router / `plan_targets` / `is_run_pass_cone_case_root` 等子目录约定） |
| [DONE] P0-T02R | [DONE] | Review fixture 发现规则盘点结果（python 平迁可读性 + 现有 fixture 全覆盖） |
| [DONE] P0-T03 | [DONE] | 冻结编译器对外命令的 stdout/stderr/exit-code 契约（`scoopc dump-*` / `emit-artifact` / `build-single-cone` / `link-cone` / `scoop build` / `scoop run`） |
| [DONE] P0-T03R | [DONE] | Review 命令面契约冻结结果（外部 runner 需消费的字段已稳定） |
| [DONE] P0-T04 | [DONE] | 删除 fixture-runner 自检 fixture（依赖 `EXPECT-ERROR-CODE: scoop::fixtures::*` 的 4 条：`timeout_should_fail.scoop` / `exit_code_mismatch.scoop` / `stderr_mismatch_distinguishable.scoop` / `gc_runner_stdout_mismatch_diagnostic_is_stable.scoop`）；同步复核 scoopc 内部确无 cfg(test) 旁路或文件名特判 |
| [DONE] P0-T04R | [DONE] | Review fixture-runner 自检 fixture 删除结果（`grep -rn "scoop::fixtures::" tests/fixtures/ --include="*.scoop"` 无命中；旧 runner 在剩余 fixture 集合上仍跑通；checks 计数下降量与删除条数一致） |
| [DONE] P1-T00 | [DONE] | 新增通用 `scoopc check-source` 命令面（非 fixture API）：支持 `parse` / `resolve` / `typecheck` / `infer` 的 phase-only 校验，支持单文件与 cone project 输入、`--source <path>` 选择项目内源文件、`--target-platform <id>` 覆盖；stdout/stderr/exit-code 契约写入 `docs/fixtures.md`，供外部 runner 避免用 `dump-*` / `build-*` 工作绕过 typecheck-only 语义 |
| [DONE] P1-T00R | [DONE] | Review `scoopc check-source` 命令面：确认它不引入 fixture 概念、不使用 `fixture` 命名 API/env，且能覆盖当前 `resolve` / `typecheck` / `infer` 单文件、多文件、cone case 的 phase-only 诊断需求 |
| [DONE] P1-T01 | [DONE] | `tools/run_fixtures.py`：fixture runner（依赖 P1-T00R；phase 发现 + `EXPECT-*` 解析 + 子进程驱动 + golden 比对 + 多进程调度 + 超时/SIGKILL） |
| [DONE] P1-T01R | [DONE] | Review `run_fixtures.py` 与旧 `scoop test` 在 `tests/fixtures/**` 上 pass/fail 集合与 checks 计数等价性 |
| [DONE] P1-T02 | [DONE] | `tools/spec_fixtures.py {sync,check}`：替代 `scoop_tools spec-fixtures` |
| [DONE] P1-T02R | [DONE] | Review `spec_fixtures.py` 与旧实现语义等价性 |
| [DONE] P1-T03 | [DONE] | `tools/fixtures_matrix.py {check,stdlib}`：替代 `scoop_tools fixtures-matrix` |
| [DONE] P1-T03R | [DONE] | Review `fixtures_matrix.py` 与旧实现语义等价性 |
| [DONE] P1-T04 | [DONE] | `tools/safepoint_baseline.py`：替代 `scoop_tools safepoint-baseline` |
| [DONE] P1-T04R | [DONE] | Review `safepoint_baseline.py` 与旧实现语义等价性 |
| [DONE] P1-T05 | [DONE] | `tools/dependency_gate.py`：替代 `scoop_tools dependency-gate`（建议 `cargo metadata --format-version 1` JSON 驱动） |
| [DONE] P1-T05R | [DONE] | Review `dependency_gate.py` 在当前仓库状态下与旧 Rust 版本结论一致 |
| [DONE] P1-T06 | [DONE] | `tools/audit_spec_coverage.py`：从 `crates/scoopc/src/audit/spec_coverage.rs` 平迁 |
| [DONE] P1-T06R | [DONE] | Review `audit_spec_coverage.py` 与旧 `#[cfg(test)]` 模块输出等价性 |
| [DONE] P1-T07 | [DONE] | `tools/audit_pipeline_gap.py`：从 `crates/scoopc/src/pipeline_gap_audit.rs` 平迁 |
| [DONE] P1-T07R | [DONE] | Review `audit_pipeline_gap.py` 与旧 `#[cfg(test)]` 模块输出等价性 |
| [DONE] P1-T08 | [DONE] | `tools/audit_user_visible_failure_policy.py`：从 `crates/scoopc/src/pipeline_user_visible_failure_policy.rs` 平迁 |
| [DONE] P1-T08R | [DONE] | Review `audit_user_visible_failure_policy.py` 与旧 `#[cfg(test)]` 模块输出等价性 |
| [DONE] P2-T01 | [DONE] | `.github/workflows/ci.yml` 切换（`scoop_tools` 调用 → `python3 tools/spec_fixtures.py check`；`cargo run -p scoop -- test` → `python3 tools/run_fixtures.py`） |
| [DONE] P2-T01R | [DONE] | Review CI 切换结果（CI 跑通 + 不再出现旧入口） |
| [DONE] P2-T02A | [DONE] | 修复 `tools/run_run_pass_gc_scan.sh` 暴露的 GC verify-roots 失败（`class_ctor_named_default_and_delegation_basic.scoop` / `class_init_super_ctor_args_eval_order_basic.scoop` / `class_secondary_ctor_delegation_this_and_super_basic.scoop` / `inherited_member_call_multi_level_chain_basic.scoop` / `inherited_member_field_access_basic.scoop`）；P2-T02 验证前置 |
| [DONE] P2-T02 | [DONE] | `tools/run_fixture_scan.sh` / `tools/run_run_pass_gc_scan.sh` / `tools/gc_microbench.sh` 内部调用串切换（依赖 P2-T02A） |
| [DONE] P2-T02R | [DONE] | Review shell 脚本切换结果（端到端跑通） |
| [DONE] P2-T03 | [DONE] | `AGENTS.md` 更新（[`TEST_INFRA_CLEANUP.md` §6](./TEST_INFRA_CLEANUP.md#6-文档更新) 列点） |
| [DONE] P2-T03R | [DONE] | Review `AGENTS.md` 更新（无旧入口残留） |
| [DONE] P2-T04 | [DONE] | `README.md` 更新（fixture runner 描述 + 命令示例） |
| [DONE] P2-T04R | [DONE] | Review `README.md` 更新（无旧入口残留） |
| [DONE] P2-T05 | [DONE] | `tools/README.md` 整体重写为 python 脚本列表 |
| [DONE] P2-T05R | [DONE] | Review `tools/README.md` 重写结果（覆盖所有 P1 脚本） |
| [DONE] P2-T06 | [DONE] | `PROMPT.md` 更新（fixture-suite 命令与超时说明） |
| [DONE] P2-T06R | [DONE] | Review `PROMPT.md` 更新（无旧入口残留） |
| [DONE] P2-T07 | [DONE] | `SCOOP_FULL_SPEC.md` 更新（spec 内 `cargo run -p scoop -- test` / `scoop test` 调用串） |
| [DONE] P2-T07R | [DONE] | Review `SCOOP_FULL_SPEC.md` 更新（无旧入口残留） |
| [DONE] P2-T08 | [DONE] | `tests/fixtures/umb_fix/B-15-when-pattern/_README.md` 与其他 `tests/fixtures/**/_README.md` 调用串替换 |
| [DONE] P2-T08R | [DONE] | Review fixture README 替换结果（grep 无旧入口） |
| [DONE] P2-T09 | [DONE] | `docs/safepoint_baseline.md` 内 `safepoint-baseline` 调用切换 |
| [DONE] P2-T09R | [DONE] | Review `docs/safepoint_baseline.md` 切换结果 |
| [DONE] P3-T01 | [DONE] | 删除 `crates/scoopc/src/fixtures/{mod,expectations,run_pass}.rs` 与 `crates/scoopc/src/fixture_cli.rs` |
| [DONE] P3-T01R | [DONE] | Review fixture runner 引擎删除结果（编译器内部不再持有 fixture 概念） |
| [DONE] P3-T02 | [DONE] | 删除 `scoopc` `driver_cli.rs` 中 `CompilerCli::TestFixtures` 变体 / 路由 / `parse_test_fixtures` / USAGE；删除 `bin/scoopc.rs` dispatch；删除 `lib.rs` 模块导出 |
| [DONE] P3-T02R | [DONE] | Review `scoopc` CLI 表面删除结果（`scoopc test-fixtures` 报未知子命令） |
| [DONE] P3-T03 | [DONE] | 删除 `scoop` `cli.rs` `Command::Test { .. }` 变体与 `test_command_parses_*` 单测；删除 `commands/test.rs`；删除 `commands/mod.rs` dispatch 分支；同步清理 `p7_default_pipeline.rs` 中仍调用 `scoop test` 的旧 fixture harness integration tests |
| [DONE] P3-T03R | [DONE] | Review `scoop test` 子命令删除结果（`scoop test` 报未知子命令） |
| [DONE] P3-T04 | [DONE] | 删除 `crates/scoopc/src/audit/{mod,spec_coverage}.rs` / `pipeline_gap_audit.rs` / `pipeline_user_visible_failure_policy.rs`；删除 `lib.rs` `#[cfg(test)] mod` 挂载点 |
| [DONE] P3-T04R | [DONE] | Review 审计模块删除结果（`scoopc` 不再 grep 文件） |
| [DONE] P3-T05 | [DONE] | 删除 `tools/scoop_tools/` 整个 crate；从 workspace 根 `Cargo.toml` 删除 `"tools/scoop_tools",` 成员条目 |
| [DONE] P3-T05R | [DONE] | Review `tools/scoop_tools/` 删除结果（`cargo metadata` 不再列出该 crate） |
| [DONE] P3-T06 | [DONE] | 清理 `crates/scoop/tests/p8_docs_cleanup.rs` 对 `tools/scoop_tools/src/fixtures_matrix.rs` 的源路径引用 |
| [DONE] P3-T06R | [DONE] | Review `p8_docs_cleanup.rs` 清理结果（按测试实际职责整体删除或更新路径） |
| [DONE] P3-T07 | [DONE] | 清理 P3-T01/P3-T04 删除后产生的死代码、孤立常量、`SCOOP_FIXTURE_*` env 名字常量等 |
| [DONE] P3-T07R | [DONE] | Review P3 残留清理结果（`crate::fixtures::` / `crate::fixture_cli::` / `SCOOP_FIXTURE_*` 全仓 grep 在源码内无命中） |
| [DONE] P4-T01 | [DONE] | 全仓 grep 验证（[`TEST_INFRA_CLEANUP.md` §7 步骤 5](./TEST_INFRA_CLEANUP.md#7-实施顺序建议) token 清单），白名单：`docs/archive/**` 与 `TODO*-pipeline-refactor.md` / `PLAN-pipeline-refactor.md` 历史 “验证通过：…” 行 |
| [DONE] P4-T02 | [DONE] | `cargo metadata --format-version 1 \| grep scoop_tools` 无命中验证 |
| [DONE] P4-T03 | [DONE] | `python3 tools/run_fixtures.py` 与旧 `scoop test` 在最近基线上 pass/fail 集合 + checks 计数一致；如有 diff 必须定位修复 |
| P4-T04 | [TODO] | `python3 tools/{spec_fixtures,fixtures_matrix,safepoint_baseline,dependency_gate,audit_*}.py` 全部跑通；`dependency-gate` 与旧 Rust 版本结论一致 |
| P4-T05 | [TODO] | CI 在切换后跑通；本地 `cargo fmt` / `cargo clippy --all-targets -- -D warnings` / `cargo test --all --all-targets` 通过 |
| P4-T06 | [TODO] | `tools/run_fixture_scan.sh` / `tools/run_run_pass_gc_scan.sh` 用新入口跑通 |
| P4-T07R | [TODO] | Review P4 全包完成度（[`TEST_INFRA_CLEANUP.md` §8](./TEST_INFRA_CLEANUP.md#8-验证清单) 验收清单逐项核对） |

## 调整记录

- 2026-05-26：执行 P1-T01 前验证发现现有公开命令面无法 spec-correct 地平迁 resolve/typecheck-only fixture 语义：`dump-hir` 会漏掉旧 typecheck runner 覆盖的诊断，`build-single-cone` 又会运行到 HIR/lowering/codegen 并误伤通过的 typecheck fixture。因此在 P1-T01 前新增 P1-T00/P1-T00R，先落地通用非 fixture 的 `scoopc check-source` phase-only 校验入口；P1-T01 显式依赖 P1-T00R。
- 2026-05-27：执行 P2-T02 时，`tools/run_run_pass_gc_scan.sh` 切到 `python3 tools/run_fixtures.py` 后仍暴露 5 条 `SCOOP_GC_VERIFY_ROOTS=1` run-pass 失败；同一批 fixture 通过旧 `target/debug/scoop test --fixtures` 入口也失败，确认不是新入口切换引入，但该失败未被精确排期。因此在 P2-T02 前新增 P2-T02A，先修复这些 GC verify-roots 失败，再完成脚本切换验收。
- 2026-05-27：执行 P3-T02 验证时，`cargo test --all --all-targets` 暴露 `crates/scoop/tests/p7_default_pipeline.rs::{single_effect_pipeline_test_fixtures_cli_works,single_pipeline_fixture_harness_has_no_hidden_legacy_fallback}` 仍通过 `scoop test` 旧 wrapper 间接调用已删除的 `scoopc test-fixtures`。该 failure 属于 P3-T03 的 `scoop test` 子命令删除范围，已在 P3-T03 任务目标中精确补充。

## 完成记录

> 任务完成后在此追加 `[DONE] PX-TNN：…（YYYY-MM-DD）` 行，附核心验证命令与产出。

- [DONE] P0-T01：新增 `docs/fixtures.md`，从 `crates/scoopc/src/fixtures/expectations.rs` 盘点当前 22 个 directive 前缀，并记录语法、参数、解析边界与各 phase 语义。验证：`python3` parser/doc 覆盖检查通过（22/22）；`cargo fmt --check` 通过。（2026-05-26）
- [DONE] P0-T01R：复审 `docs/fixtures.md` 与 `crates/scoopc/src/fixtures/expectations.rs`，确认 22 个 `strip_prefix` directive 前缀、`EXPECT: pass|ok|fail` 语义、header 扫描规则、共享输入与 parse/build/run-pass 消费语义均已覆盖，无需补充文档。验证：自定义 `python3` 前缀覆盖检查（22/22）；`cargo fmt --check`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`。（2026-05-26）
- [DONE] P0-T02：扩展 `docs/fixtures.md`，盘点 `plan_targets` 目标规划、phase router、`is_run_pass_cone_case_root`、multi/cone case 子目录约定、sysroot overlay 发现与 `umb_fix` 子路由，并给出可供 python runner 平迁的伪代码。验证：自定义 `python3` 文档 token 覆盖检查；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`。（2026-05-26）
- [DONE] P0-T02R：复审 `docs/fixtures.md` 与 `crates/scoopc/src/fixtures/mod.rs`，确认 target planning、phase routing、case-root predicates、sysroot overlay 跳过、`umb_fix` 子路由与现有 fixture 目录均已覆盖；补充记录 manifest-only `tests/fixtures/cone/` 不产生 fixture target。验证：自定义 `python3` fixture discovery 覆盖检查（1455 ordinary targets；resolve/typecheck/run_pass cone/multi cases 全覆盖）；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1536 checks）。（2026-05-26）
- [DONE] P0-T03：在 `docs/fixtures.md` 冻结外部 runner 允许调用的 `scoopc dump-*` / `dump-rtti` / `dump-stackmaps` / `emit-artifact` / `build-single-cone` / `link-cone` 与 `scoop build` / `scoop run` stdout、stderr、exit-code、数据产物契约；新增 `p8_docs_cleanup` 文档守卫测试防止命令面契约遗漏。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1536 checks）。（2026-05-26）
- [DONE] P0-T03R：复审 `docs/fixtures.md` 与实际 `scoopc` tool CLI / `scoop` facade 命令面，补强 `scoop run` 的程序退出码、stdin 继承与诊断流边界，并把 `scoop build` / `scoop run` 的 `--entry-package` 稳定参数纳入契约守卫。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1536 checks）。（2026-05-26）
- [DONE] P0-T04：删除 4 条依赖 `EXPECT-ERROR-CODE: scoop::fixtures::*` 的 fixture-runner 自检 `.scoop` fixture，并同步删除仅供这些 fixture 使用的 golden stdout/stderr 文件；复核 `tests/fixtures/**/*.scoop` 已无 `scoop::fixtures::` 期望，`crates/scoopc/src/**/*.rs` 已无 `timeout_should_fail` / `TIMEOUT_SHOULD_FAIL` 特判命中。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1532 checks，较 1536 基线减少 4）；`grep -rn --include='*.scoop' 'scoop::fixtures::' tests/fixtures/` 无命中；`grep -Ern --include='*.rs' 'timeout_should_fail|TIMEOUT_SHOULD_FAIL' crates/scoopc/src` 无命中。（2026-05-26）
- [DONE] P0-T04R：复审 P0-T04 删除结果，确认最新提交删除 4 条 fixture-runner 自检 `.scoop` fixture 及其专用 golden 文件，`tests/fixtures/**/*.scoop` 无 `scoop::fixtures::` 期望残留，旧 `scoop test` runner 在剩余集合上通过且 checks 计数为 1532，符合较 1536 基线减少 4 条的预期。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1532 checks）；`rg 'scoop::fixtures::' tests/fixtures --glob '*.scoop'` 无命中。（2026-05-26）
- [DONE] P1-T00：新增非 fixture 的 `scoopc check-source` 命令面，支持 `parse` / `resolve` / `typecheck` / `infer` phase-only 校验、单文件输入、cone project 输入、`--source <path>` 选择项目 source graph 内源文件、`--target-platform <id>` 平台覆盖，并把 stdout/stderr/exit-code 契约写入 `docs/fixtures.md`。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`target/debug/scoopc check-source` 单文件/target-platform/cone-project smoke；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T00R：复审 `scoopc check-source` 命令面，确认新增入口本身不持有 fixture 概念且未使用 `SCOOP_FIXTURE_*` 等 fixture API/env；修复 `--source` 选择项目内单个 typecheck 源文件时被未选中失败 sibling body 诊断阻塞的问题，使多文件与 cone case 可按源文件逐项比对 pass/fail。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；新增 `tool_commands::tests::check_source_typecheck_source_selection_does_not_check_unselected_bodies`；`target/debug/scoopc check-source` mixed typecheck_multi/typecheck_cone smoke；`cargo test --all --all-targets`；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T01：新增 `tools/run_fixtures.py`，以 python 标准库平迁 fixture target planning、`EXPECT-*` 解析、golden 比对、子进程驱动、多进程调度与 run-pass timeout/SIGKILL；同步补强 `scoopc check-source` 诊断定位输出、sysroot overlay typecheck 与 `dump-ir` materialized MIR 输出，使外部 runner 可覆盖现有 1532 checks。验证：`cargo fmt --check`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1532 checks）；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T01R：复审 `tools/run_fixtures.py` 与旧 `scoop test` / `scoopc test-fixtures` runner，确认 target planning、phase routing、`EXPECT-*` directive 解析、run-pass golden/timeout/env/exit handling、summary/check 计数均保持等价；`tests/fixtures/**` 上新旧 runner 均为 1503 PASS targets、0 failures、1532 checks，排序后的 target/status/check 列表完全一致。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1532 checks）；`cargo run -p scoop -- test`（1532 checks）；自定义日志解析比对确认 target/status/check 列表一致。（2026-05-26）
- [DONE] P1-T02：新增 `tools/spec_fixtures.py`，以 python 标准库平迁 `spec-fixtures sync` / `check` / `check --fix` 的 fenced code block 抽取、`// FIXTURE:` 路径校验、重复路径检测、`tests/fixtures/spec_doctest/` 写入/比对与 stale `.scoop` 清理语义；当前 spec doctest 生成集合与旧 `scoop_tools spec-fixtures` 保持一致（1 个 fixture）。验证：`python3 -m py_compile tools/spec_fixtures.py`；`python3 tools/spec_fixtures.py check`；`cargo run -p scoop_tools -- spec-fixtures check`；临时 spec 的新旧 sync 输出 diff；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1532 checks）；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T02R：复审 `tools/spec_fixtures.py` 与旧 `scoop_tools spec-fixtures` 实现，确认 `sync` / `check` / `check --fix` 的抽取、路径校验、重复路径、stale `.scoop` 清理和当前 spec doctest 输出语义等价；修复 Python 端对重复前导 `//` 注释对的 directive 解析，使其匹配旧 Rust `trim_start_matches("//")` 行为。验证：`python3 -m py_compile tools/spec_fixtures.py`；新旧临时 spec parity smoke（含 sync/check/check --fix/重复路径/非法路径/未闭合 fence/重复前导 `//`）；`python3 tools/spec_fixtures.py check`；`cargo run -q -p scoop_tools -- spec-fixtures check`；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1532 checks）；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T03：新增 `tools/fixtures_matrix.py`，以 python 标准库平迁 `fixtures-matrix check` / `stdlib` 的 spec 章节 doctest 覆盖统计、`// FIXTURE:` / `EXPECT:` 头部解析、缺失/unknown 归类、stdlib 领域前缀矩阵与报告渲染；当前仓库 `check` 与 `stdlib` 输出和退出码均与旧 `scoop_tools fixtures-matrix` 完全一致。验证：`python3 -m py_compile tools/fixtures_matrix.py`；`python3 tools/fixtures_matrix.py check` / `stdlib` 分别与 `cargo run -q -p scoop_tools -- fixtures-matrix check` / `stdlib` diff 无差异；临时 spec + run-pass 目录的新旧 parity smoke；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1532 checks）；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T03R：复审 `tools/fixtures_matrix.py` 与旧 `scoop_tools fixtures-matrix` 实现，确认 `check` / `stdlib` 的当前仓库输出完全一致，spec 章节解析、重复章节去重、重复 `//` 注释前缀、缺失 fixture 归类、未闭合 fenced code block 报错、run-pass stdlib 前缀矩阵与报告渲染语义等价；无需修改实现。验证：`python3 -m py_compile tools/fixtures_matrix.py`；当前仓库 `python3 tools/fixtures_matrix.py check` / `stdlib` 分别与 `cargo run -q -p scoop_tools -- fixtures-matrix check` / `stdlib` diff 无差异；临时 spec + run-pass 目录的新旧 parity smoke；未闭合 fenced code block 新旧均失败并报告 `未闭合`；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1532 checks）；`cargo run -p scoop -- test`（1532 checks）。（2026-05-26）
- [DONE] P1-T04：新增 `tools/safepoint_baseline.py`，以 python 标准库平迁 `scoop_tools safepoint-baseline` 的内置 workload 构建、LLVM IR `statepoint` / `gc-live` roots 统计与 Markdown 报告渲染；同步修复旧 Rust 工具引用已删除 async/task handoff fixture 的直接阻塞问题，改用当前可构建的 `safepoint_root_pressure_loop_basic.scoop` workload，并更新 safepoint baseline 文档快照。验证：`python3 -m py_compile tools/safepoint_baseline.py`；`cargo run -q -p scoop_tools -- safepoint-baseline` 与 `python3 tools/safepoint_baseline.py` 输出 diff 无差异；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-26）
- [DONE] P1-T04R：复审 `tools/safepoint_baseline.py` 与旧 `scoop_tools safepoint-baseline` 实现，确认内置 workload 列表、`O0` / `O2` 构建流程、LLVM IR statepoint 与 `gc-live` roots 统计、Markdown 报告渲染、stderr 输出与退出码语义均等价；无需修改实现。验证：`python3 -m py_compile tools/safepoint_baseline.py`；`cargo run -q -p scoop_tools -- safepoint-baseline` 与 `python3 tools/safepoint_baseline.py` 输出 diff 无差异；`cargo fmt --check`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-26）
- [DONE] P1-T05：新增 `tools/dependency_gate.py`，以 python 标准库平迁 `scoop_tools dependency-gate` 的 pipeline crate 依赖门禁与 source-boundary residual 扫描；依赖图改由 `cargo metadata --format-version 1` JSON 驱动，当前仓库输出与旧 Rust 版本完全一致。验证：`python3 -m py_compile tools/dependency_gate.py`；`python3 tools/dependency_gate.py` 与 `cargo run -q -p scoop_tools -- dependency-gate` 输出 diff 无差异；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T05R：复审 `tools/dependency_gate.py` 与旧 `tools/scoop_tools/src/dependency_gate.rs`，确认 crate 分类、直接/传递依赖边界、source-boundary/source-tree residual 扫描、报告渲染与当前仓库结论均保持一致；无需修改实现。验证：`python3 -m py_compile tools/dependency_gate.py`；`python3 tools/dependency_gate.py` 与 `cargo run -q -p scoop_tools -- dependency-gate` stdout/stderr diff 无差异；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T06：新增 `tools/audit_spec_coverage.py`，以 python 标准库平迁 `crates/scoopc/src/audit/spec_coverage.rs` 的 UMB fixture index、fixture header、bucket 覆盖、archive sentinel、spec coverage matrix 与 negative diagnostic 守卫；当前输出为 `spec coverage audit: ok (fixtures=141, buckets=35, checks=7)`，与旧 Rust audit 当前结论一致。验证：`python3 -m py_compile tools/audit_spec_coverage.py`；`python3 tools/audit_spec_coverage.py`；`cargo test -p scoopc audit::spec_coverage -- --nocapture`；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T06R：复审 `tools/audit_spec_coverage.py` 与旧 `crates/scoopc/src/audit/spec_coverage.rs`，确认 fixture index CSV 解析、fixture header 扫描、bucket 覆盖、B-01 archive sentinel、spec coverage matrix 路径、negative diagnostic forbidden terms 与当前仓库输出语义等价；Python 版本仅将 Rust fail-fast panic 改为 standalone audit 的失败聚合报告，未发现语义漂移，无需修改实现。验证：`python3 -m py_compile tools/audit_spec_coverage.py`；`python3 tools/audit_spec_coverage.py`；`cargo test -p scoopc audit::spec_coverage -- --nocapture`；独立 code-review agent 复核无 semantic drift；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T07：新增 `tools/audit_pipeline_gap.py`，以 python 标准库平迁 `crates/scoopc/src/pipeline_gap_audit.rs` 的 active audit roots、inventory classification rule names、exit conditions、LegacyOnly 与 legacy reason residual 扫描、codegen scope-drift baseline、closed/re-scoped production blocker 守卫；当前输出为 `pipeline gap audit: ok (roots=3, rules=4, exit_conditions=3, legacy_bucket_hits=0, legacy_reason_hits=0, scope_drift=20, closed_rescoped_blockers=0, checks=5)`，与旧 Rust audit 当前结论一致。验证：`python3 -m py_compile tools/audit_pipeline_gap.py`；`python3 tools/audit_pipeline_gap.py`；`cargo test -p scoopc pipeline_gap_audit -- --nocapture`；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T07R：复审 `tools/audit_pipeline_gap.py` 与旧 `crates/scoopc/src/pipeline_gap_audit.rs`，确认 active roots、classification rule names、exit conditions、LegacyOnly/legacy reason residual 扫描、codegen scope-drift baseline、closed/re-scoped production blocker 守卫与当前输出语义等价；同步修复 `parse_gap_heading` 对空 markdown heading 的鲁棒性，使其与 Rust `split_whitespace().next()?` 一样跳过 malformed heading 而非抛出 `IndexError`。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`python3 -m py_compile tools/audit_pipeline_gap.py`；`python3 tools/audit_pipeline_gap.py`；`cargo test -p scoopc pipeline_gap_audit -- --nocapture`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T08：新增 `tools/audit_user_visible_failure_policy.py`，以 python 标准库平迁 `crates/scoopc/src/pipeline_user_visible_failure_policy.rs` 的 failure-policy audit files、classification rules、post-HIR allowed failure classes、frontend reject surface/marker guards、upstream validation guard ledger、stale `Unsupported*` marker guard、production `todo!` 禁止项与 internal bug sentinel 精确基线；当前输出为 `user-visible failure policy audit: ok (files=78, rules=5, failure_keywords=4, allowed_post_hir_classes=3, frontend_rejects=17, upstream_guards=11, stale_unsupported=0, internal_bug_sentinels=370, checks=6)`，与旧 Rust audit 当前结论一致。验证：`python3 -m py_compile tools/audit_user_visible_failure_policy.py`；`python3 tools/audit_user_visible_failure_policy.py`；`cargo test -p scoopc pipeline_user_visible_failure_policy -- --nocapture`；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P1-T08R：复审 `tools/audit_user_visible_failure_policy.py` 与旧 `crates/scoopc/src/pipeline_user_visible_failure_policy.rs`，确认 failure-policy audit file scope、classification rules、post-HIR allowed classes、frontend reject marker guards、upstream guard ledger、stale `Unsupported*` marker guard、production `todo!` 禁止项、internal bug sentinel 基线与输出指标语义等价；Python 版本仅按 standalone CLI 形式用 stderr/exit-code 表达 Rust `#[test]` pass/fail 结果，未发现语义漂移，无需修改实现。验证：`python3 -m py_compile tools/audit_user_visible_failure_policy.py`；`python3 tools/audit_user_visible_failure_policy.py`；`cargo test -p scoopc pipeline_user_visible_failure_policy -- --nocapture`；独立 code-review agent 复核无 semantic drift；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py tests/fixtures`（1533 checks）；`cargo run -p scoop -- test`（1533 checks）。（2026-05-27）
- [DONE] P2-T01：`.github/workflows/ci.yml` 切换为 `python3 tools/spec_fixtures.py check` 与 `python3 tools/run_fixtures.py`，CI 不再调用旧 `scoop_tools` 或 `scoop test` fixture runner 入口。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures' .github/workflows/ci.yml` 无命中；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/spec_fixtures.py check`；`python3 tools/run_fixtures.py`（1533 checks）。（2026-05-27）
- [DONE] P2-T01R：复审 `.github/workflows/ci.yml` 与最新 P2-T01 提交，确认 CI 已调用 `python3 tools/spec_fixtures.py check` 与 `python3 tools/run_fixtures.py`，且不再出现 `scoop_tools`、`cargo run -p scoop -- test`、`scoop test` 或 `test-fixtures` 旧入口；本地 CI 等价验证通过。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures' .github/workflows/ci.yml` 无命中；`rg 'python3 tools/(spec_fixtures|run_fixtures)\.py' .github/workflows/ci.yml` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/spec_fixtures.py check`；`python3 tools/run_fixtures.py`（1533 checks）。（2026-05-27）
- [DONE] P2-T02A：修复 class ctor call / delegation / inherited receiver 相关 run-pass fixture 在 `SCOOP_GC_VERIFY_ROOTS=1` + GC move/stress 下暴露的 root 失效：构造器目标对象与构造器实参在后续实参/default/super/delegation 求值期间通过 managed root homes 延迟保存并在调用前 reload，避免移动 GC 后继续使用旧指针；同步更新 user-visible failure policy audit 的 internal bug sentinel 精确基线。验证：5 条指定 fixture 分别以 `SCOOP_GC_VERIFY_ROOTS=1 python3 tools/run_fixtures.py -j 1 --exit-on-failure --gc-move --gc-stress <fixture>` 通过；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`python3 tools/audit_user_visible_failure_policy.py`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`tools/run_run_pass_gc_scan.sh`（total=412, pass=412, fail=0, timeout=0）。（2026-05-27）
- [DONE] P2-T02：确认 `tools/run_fixture_scan.sh` 与 `tools/run_run_pass_gc_scan.sh` 内部已通过 `python3 tools/run_fixtures.py` 驱动 fixture，`tools/gc_microbench.sh` 无旧 fixture-runner 调用；验证时修复 GC STW 下线程 register/unregister 在已请求 STW 期间以 `Running` 状态等待导致的间歇性 deadlock，使 run-pass GC 扫描可稳定通过。验证：旧入口模式搜索（`target/debug|release scoop test` / `scoop test` / `cargo run -p scoop -- test` / `scoopc test-fixtures`）在 `tools/*.sh` 无命中；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`SCOOP_GC_VERIFY_ROOTS=1 python3 tools/run_fixtures.py -j 1 --exit-on-failure --gc-move --gc-stress tests/fixtures/run-pass/delegated_property_lazy_thread_safety_synchronized_once.scoop` 连续 20 次通过；`tools/run_fixture_scan.sh --no-build --timeout-secs 60 --out-dir target/fixture-scan/p2-t02-parse tests/fixtures/parse`（total=133, pass=133, fail=0, timeout=0）；`tools/run_run_pass_gc_scan.sh target/run-pass-gc-scan/p2-t02-fixed`（total=412, pass=412, fail=0, timeout=0）；`tools/gc_microbench.sh throughput --rounds 1 --batch 10`；`tools/gc_microbench.sh fragmentation --initial 100 --pin-stride 10`。（2026-05-27）
- [DONE] P2-T02R：复审 shell 脚本切换结果，确认 `tools/run_fixture_scan.sh` 与 `tools/run_run_pass_gc_scan.sh` 已通过 `python3 tools/run_fixtures.py` 驱动 fixture，`tools/gc_microbench.sh` 不含旧 fixture-runner 调用，`tools/*.sh` 旧入口模式搜索无命中；端到端验证全部通过。验证：旧入口模式搜索（`scoop test` / `cargo run -p scoop -- test` / `test-fixtures` / `scoopc test-fixtures` / `target/debug|release scoop test`）在 `tools/*.sh` 无命中；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`tools/run_fixture_scan.sh --no-build --timeout-secs 60 --out-dir target/fixture-scan/p2-t02r-parse tests/fixtures/parse`（total=133, pass=133, fail=0, timeout=0）；`tools/run_run_pass_gc_scan.sh target/run-pass-gc-scan/p2-t02r`（total=412, pass=412, fail=0, timeout=0）；`tools/gc_microbench.sh throughput --rounds 1 --batch 10`；`tools/gc_microbench.sh fragmentation --initial 100 --pin-stride 10`。（2026-05-27）
- [DONE] P2-T03：更新 `AGENTS.md`，移除 `scoop` 作为 fixture runner 与 `scoop_tools` Rust 工具箱的旧描述，替换为 `python3 tools/spec_fixtures.py check` / `python3 tools/run_fixtures.py` 示例，并补充 fixture 套件由 `tools/run_fixtures.py` 驱动、fixture 工具位于 `tools/*.py` 的说明。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' AGENTS.md` 无命中；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且上一条完成记录已给出完整绿色结果。（2026-05-27）
- [DONE] P2-T03R：复审 `AGENTS.md` 与最新 P2-T03 提交，确认 `TEST_INFRA_CLEANUP.md` §6 对 `AGENTS.md` 列出的 driver CLI 描述、`tools/*.py` 工具说明、spec doctest 命令、fixture suite 命令、spec sync/check 命令和 Testing Guidelines 补充均已落地；旧 fixture runner 入口模式无残留。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' AGENTS.md` 无命中；`rg 'python3 tools/(spec_fixtures|run_fixtures)\.py|tools/run_fixtures\.py|tools/\*\.py' AGENTS.md` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T04：更新 `README.md`，将 fixture suite 示例切换为 `python3 tools/run_fixtures.py`，将 Safepoint 基线示例切换为 `python3 tools/safepoint_baseline.py`，并说明 `tests/fixtures/` 由外部 Python runner 驱动、无后端构建不再包含 fixture runner。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' README.md` 无命中；`rg 'python3 tools/(run_fixtures|safepoint_baseline)\.py|tools/run_fixtures\.py' README.md` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T04R：复审 `README.md` 与最新 P2-T04 提交，确认 `TEST_INFRA_CLEANUP.md` §6 对 `README.md` 的三项要求均已落地：无后端构建描述不再包含 fixture runner，fixture suite 示例切换为 `python3 tools/run_fixtures.py`，`tests/fixtures/` 目录说明明确由 `tools/run_fixtures.py` 驱动；同步确认 Safepoint 基线示例已切换为 `python3 tools/safepoint_baseline.py`，root `README.md` 无旧入口残留。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' README.md` 无命中；`rg 'python3 tools/(run_fixtures|safepoint_baseline)\.py|tools/run_fixtures\.py|tests/fixtures' README.md` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T05：整体重写 `tools/README.md`，删除旧 Rust 工具箱与旧内置 fixture runner 描述，改为覆盖当前 8 个 `tools/*.py` 的 Python 脚本清单，并补充 3 个 shell helper 的当前用途与 `python3 tools/run_fixtures.py <fixture>` 调用形态。验证：旧入口模式搜索（`scoop_tools` / `cargo run -p scoop -- test` / `scoop test` / `test-fixtures` / `target/debug|release scoop test` / `cargo run -p scoopc -- test-fixtures`）在 `tools/README.md` 无命中；`tools/(run_fixtures|spec_fixtures|fixtures_matrix|safepoint_baseline|dependency_gate|audit_spec_coverage|audit_pipeline_gap|audit_user_visible_failure_policy)\.py` 在 `tools/README.md` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T05R：复审 `tools/README.md` 重写结果，确认当前 8 个 `tools/*.py` 均已在 Python Scripts 清单与常用命令中覆盖，3 个 `tools/*.sh` shell helper 均已描述，且旧 Rust 工具箱与旧 fixture runner 入口无残留；无需修改 `tools/README.md`。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' tools/README.md` 无命中；`rg --line-number 'tools/(run_fixtures|spec_fixtures|fixtures_matrix|safepoint_baseline|dependency_gate|audit_spec_coverage|audit_pipeline_gap|audit_user_visible_failure_policy)\.py' tools/README.md` 命中全部 8 个 P1 Python 脚本；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T06：更新 `PROMPT.md`，将完整 fixture-suite 示例从旧 `cargo run -p scoop -- test` 切换为 `python3 tools/run_fixtures.py`，并保留至少 30 分钟超时要求；全文未发现其他旧 `scoop` / `scoopc` fixture runner 入口残留。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' PROMPT.md` 无命中；`rg 'python3 tools/run_fixtures\.py|Full fixture-suite|timeout of at least 30 minutes' PROMPT.md` 命中新入口与超时说明；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T06R：复审 `PROMPT.md` 与最新 P2-T06 提交，确认 `TEST_INFRA_CLEANUP.md` §6 对 `PROMPT.md` 的 fixture-suite 命令与超时说明要求已落地；旧 `scoop` / `scoopc` fixture runner 入口搜索无残留，`PROMPT.md` 无需修改。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' PROMPT.md` 无命中；`rg 'python3 tools/run_fixtures\.py|Full fixture-suite|timeout of at least 30 minutes' PROMPT.md` 命中新入口与超时说明；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T07：更新 `SCOOP_FULL_SPEC.md` 的 spec doctest fixture 说明，将旧 `tools/scoop_tools`、`cargo run -p scoop_tools -- spec-fixtures check` 与 `cargo run -p scoop -- test` 调用串替换为 `python3 tools/spec_fixtures.py {sync,check}` 和 `python3 tools/run_fixtures.py tests/fixtures/spec_doctest`。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' SCOOP_FULL_SPEC.md` 无命中；`rg 'python3 tools/(spec_fixtures|run_fixtures)\.py' SCOOP_FULL_SPEC.md` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`python3 tools/spec_fixtures.py check`；`python3 tools/run_fixtures.py tests/fixtures/spec_doctest`；`cargo test --all --all-targets` 与完整 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T07R：复审 `SCOOP_FULL_SPEC.md` 与最新 P2-T07 提交，确认 spec doctest fixture 说明已切换为 `python3 tools/spec_fixtures.py sync/check` 与 `python3 tools/run_fixtures.py tests/fixtures/spec_doctest`，旧 `scoop_tools`、`cargo run -p scoop -- test`、`scoop test`、`test-fixtures`、`scoopc test-fixtures` 等入口无残留；无需修改 spec 正文。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|target/(debug|release)/scoop test|cargo run -p scoopc -- test-fixtures' SCOOP_FULL_SPEC.md` 无命中；`rg 'python3 tools/(spec_fixtures|run_fixtures)\.py' SCOOP_FULL_SPEC.md` 命中新入口；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`python3 tools/spec_fixtures.py check`；`python3 tools/run_fixtures.py tests/fixtures/spec_doctest`；`cargo test --all --all-targets` 与完整 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T08：更新 `tests/fixtures/umb_fix/B-15-when-pattern/_README.md` 与其他 `tests/fixtures/**/_README.md` runner 调用说明，将旧 `scoop test` / `cargo run -p scoop -- test` 入口替换为 `python3 tools/run_fixtures.py`。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|--fixtures' tests/fixtures --glob '**/_README.md'` 无命中；`rg 'python3 tools/run_fixtures\.py' tests/fixtures --glob '**/_README.md'` 命中 35 个 fixture README；`cargo fmt --check`；`cargo clippy --all-targets -- -D warnings`；`git diff --check`；`cargo test --all --all-targets` 与完整 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T08R：复审 fixture README 替换结果，确认 `tests/fixtures/**/_README.md` 已无旧 fixture runner 入口，且 35 个 fixture README 保留 `python3 tools/run_fixtures.py` 新入口；无需修改 fixture README。验证：`rg 'scoop_tools|cargo run -p scoop -- test|scoop test|test-fixtures|cargo run -p scoopc -- test-fixtures|target/(debug|release)/scoop test|--fixtures' tests/fixtures --glob '**/_README.md'` 无命中；`rg 'python3 tools/run_fixtures\.py' tests/fixtures --glob '**/_README.md'` 命中 35 个 fixture README；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets` 与完整 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T09：更新 `docs/safepoint_baseline.md`，将重跑方法与当前快照来源从旧 `cargo run -p scoop_tools -- safepoint-baseline` 切换为 `python3 tools/safepoint_baseline.py`。验证：`rg 'cargo run -p scoop_tools -- safepoint-baseline|safepoint_baseline\.py|safepoint-baseline' docs/safepoint_baseline.md` 仅命中新入口；`python3 tools/safepoint_baseline.py`；`cargo fmt --check`；`cargo clippy --all-targets -- -D warnings`；`git diff --check`；`cargo test --all --all-targets` 与完整 `python3 tools/run_fixtures.py` 未重跑，因为本任务仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P2-T09R：复审 `docs/safepoint_baseline.md` 切换结果，确认重跑方法与当前快照来源均使用 `python3 tools/safepoint_baseline.py`，旧 `cargo run -p scoop_tools -- safepoint-baseline` / `safepoint-baseline` 调用无残留，且文档中的新命令可生成 safepoint baseline 报告；无需修改 `docs/safepoint_baseline.md`。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`git diff --check`；`rg 'cargo run -p scoop_tools -- safepoint-baseline|scoop_tools safepoint-baseline|safepoint-baseline' docs/safepoint_baseline.md` 无命中；`rg 'python3 tools/safepoint_baseline\.py' docs/safepoint_baseline.md` 命中 2 处；`python3 -B tools/safepoint_baseline.py`；`cargo test --all --all-targets` 与完整 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 markdown/task bookkeeping，且自最近完整绿色结果以来无代码变更。（2026-05-27）
- [DONE] P3-T01：删除 `crates/scoopc/src/fixtures/{mod,expectations,run_pass}.rs` 与 `crates/scoopc/src/fixture_cli.rs`，并移除删除后会直接悬空的 `scoopc` 模块导出与 dispatch 引用，使编译器 crate 不再编译旧 fixture runner engine。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`rg 'fixture_cli|crate::fixtures|scoopc::fixtures|pub mod fixtures|pub mod fixture_cli|SCOOP_FIXTURE' crates/scoopc/src` 无命中；`crates/scoopc/src/fixtures/**` 无文件；`cargo run -q -p scoopc -- test-fixtures` 报 `未知 scoopc 子命令或参数：test-fixtures`。（2026-05-27）
- [DONE] P3-T01R：复审 P3-T01 fixture runner engine 删除结果，确认 `crates/scoopc/src/fixture_cli.rs` 与 `crates/scoopc/src/fixtures/` 已不存在，`scoopc` 源码内无 `crate::fixtures` / `crate::fixture_cli` / `SCOOP_FIXTURE_*` / `test-fixtures` / `TestFixtures` / `parse_test_fixtures` 等旧 runner API 残留，编译器 crate 不再持有旧 fixture runner engine。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`test ! -e crates/scoopc/src/fixture_cli.rs`；`test ! -d crates/scoopc/src/fixtures`；`rg 'crate::fixtures|crate::fixture_cli|mod fixtures|fixture_cli|SCOOP_FIXTURE_|test-fixtures|TestFixtures|parse_test_fixtures' crates/scoopc --glob '*.rs'` 无命中。（2026-05-27）
- [DONE] P3-T02：完成 `scoopc` CLI 表面删除确认：`driver_cli.rs` 已无 `CompilerCli::TestFixtures` 变体、`test-fixtures` 路由、`parse_test_fixtures` 或 USAGE 项，`bin/scoopc.rs` 已无 dispatch，`lib.rs` 已无 fixture 模块导出；新增 `driver_cli::tests::test_fixtures_subcommand_is_removed` 回归覆盖。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test -p scoopc --all-targets`；`cargo run -q -p scoopc -- test-fixtures` 退出 1 并报 `未知 scoopc 子命令或参数：test-fixtures`；`python3 tools/run_fixtures.py`（1533 checks）。完整 `cargo test --all --all-targets` 已运行并停在 P3-T03 精确排期的 `scoop test` wrapper 残留测试：`single_effect_pipeline_test_fixtures_cli_works` / `single_pipeline_fixture_harness_has_no_hidden_legacy_fallback`。（2026-05-27）
- [DONE] P3-T02R：复审 `scoopc` CLI 表面删除结果，确认 `driver_cli.rs` 无 `CompilerCli::TestFixtures` 变体、`test-fixtures` 路由、`parse_test_fixtures` 或 USAGE 项，`bin/scoopc.rs` 无对应 dispatch，`lib.rs` 无 `fixtures` / `fixture_cli` 模块导出；`scoopc test-fixtures` 仅作为删除回归单测输入保留，并按预期报未知子命令。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test -p scoopc --all-targets`；`cargo run -q -p scoopc -- test-fixtures` 退出 1 并报 `未知 scoopc 子命令或参数：test-fixtures`；`rg 'CompilerCli::TestFixtures|parse_test_fixtures|fixture_cli|pub mod fixtures|pub mod fixture_cli|crate::fixtures|scoopc::fixtures|SCOOP_FIXTURE_' crates/scoopc --glob '*.rs'` 无命中；`rg 'test-fixtures' crates/scoopc --glob '*.rs'` 仅命中 `driver_cli::tests::test_fixtures_subcommand_is_removed`；`python3 tools/run_fixtures.py`（1533 checks）。完整 `cargo test --all --all-targets --quiet` 已运行，仍仅停在 P3-T03 已精确排期的 `p7_default_pipeline.rs` 两个 `scoop test` wrapper 残留测试。（2026-05-27）
- [DONE] P3-T03：删除 `scoop` facade 的 `Command::Test` parser variant、`commands/test.rs` wrapper、`commands/mod.rs` dispatch branch 与旧 fixture wrapper 单测；同步清理 `p7_default_pipeline.rs` 中调用 `scoop test --fixtures` 的旧 harness integration tests，并新增 `scoop test` unknown-subcommand 回归覆盖。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`cargo run -q -p scoop -- test` 退出 2 并报 `error: unrecognized subcommand 'test'`；`rg 'Command::Test|mod test;|commands/test|TestOptions|DEFAULT_FIXTURE_PROCESSES|FIXTURE_SCOOP_BIN_ENV|SCOOP_FIXTURE_SCOOP_BIN|test-fixtures|fixtures: ok|scoop test' crates/scoop/{src,tests} --glob '*.rs'` 无命中。（2026-05-27）
- [DONE] P3-T03R：复审 `scoop test` 子命令删除结果，确认 `crates/scoop/src/cli.rs` 无 `Command::Test` 变体且保留 unknown-subcommand 回归单测，`crates/scoop/src/commands/mod.rs` 无 `test` module/dispatch，`crates/scoop/src/commands/test.rs` 不存在，`crates/scoop/tests/p7_default_pipeline.rs` 不再包含旧 fixture harness wrapper 测试；`scoop test` 仅作为删除回归输入保留，并按预期报未知子命令。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`cargo run -q -p scoop -- test` 退出 2 并报 unknown subcommand；`rg 'Command::Test|mod test;|commands/test|TestOptions|DEFAULT_FIXTURE_PROCESSES|FIXTURE_SCOOP_BIN_ENV|SCOOP_FIXTURE_SCOOP_BIN|test-fixtures|fixtures: ok|scoop test' crates/scoop --glob '*.rs'` 无命中。（2026-05-27）
- [DONE] P3-T04：删除 `crates/scoopc/src/audit/mod.rs`、`crates/scoopc/src/audit/spec_coverage.rs`、`crates/scoopc/src/pipeline_gap_audit.rs`、`crates/scoopc/src/pipeline_user_visible_failure_policy.rs`，并移除 `crates/scoopc/src/lib.rs` 中对应 `#[cfg(test)] mod` 挂载点，使这些旧 Rust 审计基线不再参与 `scoopc` 编译。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`rg '\bmod audit\b|\bpipeline_gap_audit\b|\bpipeline_user_visible_failure_policy\b|spec_coverage' crates/scoopc/src` 无命中；`crates/scoopc/src/audit/**` 无文件。（2026-05-27）
- [DONE] P3-T04R：复审 P3-T04 审计模块删除结果，确认 `crates/scoopc/src/audit/**` 已无文件，`crates/scoopc/src/lib.rs` 已无审计模块 `#[cfg(test)] mod` 挂载点，`crates/scoopc/src` 中无 `mod audit` / `pipeline_gap_audit` / `pipeline_user_visible_failure_policy` / `spec_coverage` 残留；无需修改源码。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`rg '\bmod audit\b|\bpipeline_gap_audit\b|\bpipeline_user_visible_failure_policy\b|spec_coverage' crates/scoopc/src` 无命中；`crates/scoopc/src/audit/**` 无文件。（2026-05-27）
- [DONE] P3-T05：删除 `tools/scoop_tools/` Rust 工具 crate 的全部 tracked 文件，并从 workspace 根 `Cargo.toml` 移除 `"tools/scoop_tools"` 成员；`Cargo.lock` 已随 cargo metadata/clippy 更新，不再包含 `scoop_tools` package。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo metadata --format-version 1 --no-deps` 确认 `scoop_tools` / `tools/scoop_tools` 不再出现在 workspace packages；`rg 'scoop_tools' Cargo.lock` 无命中；`tools/scoop_tools/**` 无 tracked 文件；`python3 tools/run_fixtures.py`（1533 checks）。`cargo test --all --all-targets` 已运行，当前仅停在 P3-T06 已精确排期的 `crates/scoop/tests/p8_docs_cleanup.rs::legacy_pipeline_docs_removed_spec_and_tool_indexes_drop_deleted_async_task_surface`，原因是该测试仍读取本任务删除的 `tools/scoop_tools/src/fixtures_matrix.rs` 路径。（2026-05-27）
- [DONE] P3-T05R：复审 P3-T05 删除结果，确认根 `Cargo.toml` workspace members 已无 `"tools/scoop_tools"`，`Cargo.lock` 无 `scoop_tools` package，`git ls-files tools/scoop_tools` 无 tracked 文件，且 `cargo metadata --format-version 1 --no-deps` 的 workspace packages 中无名为 `scoop_tools` 或 manifest 位于 `tools/scoop_tools` 的 crate。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo metadata --format-version 1 --no-deps --quiet` + JSON 检查通过；`rg 'scoop_tools|tools/scoop_tools' Cargo.toml Cargo.lock` 无命中；`python3 tools/run_fixtures.py`（1533 checks）。`cargo test --all --all-targets` 已运行，仍仅停在 P3-T06 已精确排期的 `crates/scoop/tests/p8_docs_cleanup.rs::legacy_pipeline_docs_removed_spec_and_tool_indexes_drop_deleted_async_task_surface`。（2026-05-27）
- [DONE] P3-T06：将 `crates/scoop/tests/p8_docs_cleanup.rs` 中 async/task surface 守卫读取的旧 `tools/scoop_tools/src/fixtures_matrix.rs` 源路径更新为当前 `tools/fixtures_matrix.py`，保留 tool index 回归覆盖且不再引用已删除 crate 路径。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test -p scoop --test p8_docs_cleanup`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）；`rg 'tools/scoop_tools/src/fixtures_matrix\.rs|tools/fixtures_matrix\.py' crates/scoop/tests/p8_docs_cleanup.rs` 仅命中新路径。（2026-05-27）
- [DONE] P3-T06R：复审 `p8_docs_cleanup.rs` 清理结果，确认该测试除旧源路径外仍承担 async/task surface 文档与 tool index 守卫职责，因此保留测试并读取当前 `tools/fixtures_matrix.py`；`crates/scoop/tests/p8_docs_cleanup.rs` 已无 `tools/scoop_tools/src/fixtures_matrix.rs` 残留。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test -p scoop --test p8_docs_cleanup`；`rg 'tools/scoop_tools/src/fixtures_matrix\.rs' crates/scoop/tests/p8_docs_cleanup.rs` 无命中；`rg 'tools/fixtures_matrix\.py' crates/scoop/tests/p8_docs_cleanup.rs` 命中新路径。完整 `cargo test --all --all-targets` 与 `python3 tools/run_fixtures.py` 未重跑，因为本 review 仅修改 TODO/进度记录，且最新 P3-T06 提交已在相同源码状态下记录二者通过。（2026-05-27）
- [DONE] P3-T07：清理 P3 删除后的活跃残留引用：移除迁出脚本中的旧 Rust audit 文件名旁路、删除 `dependency_gate.py` 中已移除的 `scoop_tools` crate 名、更新 B-01 sentinel 与文档/注释中指向旧 `scoop test` 或已删除 Rust audit 文件的 active references；确认旧 compiler fixture hook 符号已无残留。验证：`rg 'SCOOP_FIXTURE_WORKER|SCOOP_FIXTURE_OK|SCOOP_FIXTURE_SCOOP_BIN|crate::fixtures::|crate::fixture_cli::|FixturePhase|PlannedFixtureTarget|FixtureCliOptions|FixtureExpectation|RunPassEnvOverrides' crates runtime tools` 无命中；`rg 'scoop_tools|scoop-tools|pipeline_gap_audit\.rs|pipeline_user_visible_failure_policy\.rs|crates/scoopc/src/audit/spec_coverage\.rs|safepoint-baseline|scoop test' crates runtime tools docs/fixtures.md OVERLOAD_RESOLUTION.md audit/_baseline_sampling.md tests/fixtures` 无命中；`python3 -m py_compile tools/audit_spec_coverage.py tools/audit_pipeline_gap.py tools/dependency_gate.py tools/safepoint_baseline.py`；`python3 tools/audit_spec_coverage.py`；`python3 tools/audit_pipeline_gap.py`；`python3 tools/dependency_gate.py`；`python3 tools/safepoint_baseline.py`；`python3 tools/audit_user_visible_failure_policy.py`；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）。（2026-05-27）
- [DONE] P3-T07R：复审 P3 残留清理结果，确认活跃源码路径内 `crate::fixtures::` / `crate::fixture_cli::` / `SCOOP_FIXTURE_*` 及旧 fixture runner/audit/tooling 残留 token 均无命中；验证时完整 fixture suite 暴露两条 STW cross-thread runtime_gc fixture 的 10s `scoop run` timeout 预算在并行负载下过紧，已将两者 `TIMEOUT` 调整为 30000ms（仍低于单 fixture 1 分钟上限）以避免误报。验证：`rg 'crate::fixtures::|crate::fixture_cli::|SCOOP_FIXTURE_' crates runtime tools sysroot tests` 无命中；`rg 'SCOOP_FIXTURE_WORKER|SCOOP_FIXTURE_OK|SCOOP_FIXTURE_SCOOP_BIN|FixturePhase|PlannedFixtureTarget|FixtureCliOptions|FixtureExpectation|RunPassEnvOverrides' crates runtime tools sysroot tests` 无命中；`rg 'scoop_tools|scoop-tools|pipeline_gap_audit\.rs|pipeline_user_visible_failure_policy\.rs|crates/scoopc/src/audit/spec_coverage\.rs|safepoint-baseline|scoop test' crates runtime tools docs/fixtures.md OVERLOAD_RESOLUTION.md audit/_baseline_sampling.md tests/fixtures` 无命中；两条 STW fixture 单独 `python3 tools/run_fixtures.py -j 1 --exit-on-failure` 通过；`python3 tools/run_fixtures.py --exit-on-failure tests/fixtures/runtime_gc` 通过；`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`cargo test --all --all-targets`；`python3 tools/run_fixtures.py`（1533 checks）。（2026-05-27）
- [DONE] P4-T01：完成 §7 步骤 5 残留 token 全仓验证；原始非归档扫描仅命中当前清理计划/任务源文件 `PLAN.md`、`TODO.md`、`TEST_INFRA_CLEANUP.md`，这些文件定义或记录本次清理范围，不属于实现/用户文档残留。排除上述当前计划源文件后，tracked 文件与 working tree 非归档路径均无命中。验证：`git grep -lE <§7.5 token-set> -- . ':(exclude)docs/archive/**'` 仅列出上述 3 个当前计划源文件；`git grep -nE <§7.5 token-set> -- . ':(exclude)docs/archive/**' ':(exclude)TODO.md' ':(exclude)PLAN.md' ':(exclude)TEST_INFRA_CLEANUP.md'` 无命中；`rg -n --hidden <§7.5 token-set> -g '!docs/archive/**' -g '!TODO.md' -g '!PLAN.md' -g '!TEST_INFRA_CLEANUP.md' -g '!.git/**' .` 无命中。完整 Rust/fixture suite 未重跑，因为本任务只更新 markdown/progress bookkeeping，且最新 P3-T07R 已在相同代码状态下记录完整通过。（2026-05-27）
- [DONE] P4-T02：完成 workspace metadata 残留验证，确认 `scoop_tools` crate 不再出现在 `cargo metadata --format-version 1` 输出中。验证：`cargo metadata --format-version 1 | grep scoop_tools` 无命中（脚本区分 `cargo metadata` 失败与预期 grep no-match）。完整 Rust/fixture suite 未重跑，因为本任务只更新 markdown/progress bookkeeping，且最新 P3-T07R 已在相同代码状态下记录完整通过。（2026-05-27）
- [DONE] P4-T03：完成 fixture runner parity 验证：当前 `python3 tools/run_fixtures.py` 通过（1504 targets，1533 checks）；在隔离 worktree 中以最后仍含旧 runner 的 `713ba8f4` 为基线，叠加当前已存在的 `56e265e7` machine-independent continuation 修复和 P3-T07R 的两条 STW fixture timeout-only header 后，旧 `cargo run -q -p scoop -- test` 与同基线 `python3 tools/run_fixtures.py` 均通过（1533 checks）。归一化日志比对确认旧 runner、同基线 Python runner、当前 Python runner 的 1504 个 target pass/fail 状态与逐 target checks 完全一致。验证：`cargo fmt`；`cargo clippy --all-targets -- -D warnings`；`python3 tools/run_fixtures.py`；隔离 worktree `cargo build -q -p scoopc -p scoop` + `cargo run -q -p scoop -- test`；隔离 worktree `python3 tools/run_fixtures.py`；自定义日志解析比对。（2026-05-27）
